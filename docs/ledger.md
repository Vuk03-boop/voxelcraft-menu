
# The batch ledger

**History, one row per batch.** What is next is [`roadmap.md`](roadmap.md); the briefing is
[`../CLAUDE.md`](../CLAUDE.md). **Do not re-plan a shipped batch from this table** -- read the
subsystem document its Reasoning column points at, because that is the copy being maintained.

**Count the rows rather than trusting a sentence about them.** Two earlier versions of this
paragraph asserted a batch count and both were wrong about their own table.

**Read the Control column as a historical claim, not a live measurement, for batches 1-20.**
Those revert binaries do not exist in this tree and cannot be rebuilt -- there was no `.git`
here until the batch-27 documentation pass. What survives as live evidence is the composition
half (checked by `harness implies`), the three test constants that predate their batches, and
the bit-exactness sweeps run from batch 21b onward.

| Batch | What it did | Control | Reasoning |
|---|---|---|---|
| 1-4 | sRGB-correct mips, linear end to end, per-corner light and Minecraft vertex AO, temporal provisions | `--ambient`, `--jitter` | [batch-1-4-shading](shading.md) |
| 5 | atmosphere: closed-form exponential *height* fog, one Henyey-Greenstein lobe shared by the sky and the haze | `--fog-density 0` | [batch-5-atmosphere](sky.md) |
| 6 | temporal anti-aliasing: analytic motion vectors, Halton jitter, YCoCg variance clip | `--no-taa` | [batch-6-taa](temporal.md) |
| 7 | stochastic LOD cross-fade: a per-ray dither across the transition band, resolved by the temporal pass | `--no-fade` | [batch-7-crossfade](lod.md) |
| 8 | water: a flooded world, a second march for what is under the surface, a Fresnel-weighted traced reflection for what is above it | `--no-water` | [batch-8-water](water.md) |
| 9 | biomes: a temperature x humidity field, per-biome terrain amplitude, snow and tree lines | `--no-biomes` | [batch-9-biomes](terrain.md) |
| 10 | clouds: one analytic deck in the sky miss, `sky_color` split from `sky_base`, the haze doing the horizon | `--cloud-cover 0` | [batch-10-clouds](sky.md) |
| 11 | god rays: the haze's own sun lobe weighted by a sampled visibility, the deck shadowing the ground it already lit | `--godray-strength 0`, `--cloud-shadow 0` | [batch-11-godrays](sky.md) |
| 12 | per-biome tint: the biome field evaluated a second time in WGSL, per pixel, from the shaded world position, through a per-texel mask in the atlas's spare alpha channel | `--no-tint` | [batch-12-tint](shading.md) |
| 13 | occupancy: the first register counts ever taken here, `resolve` reshaped to 16x8, and the tint's flag folded at pipeline-compile time by an `override` | none -- it moves zero pixels | [batch-13-occupancy](gpu.md) |
| 14 | non-cube foliage: an in-voxel analytic cross-quad in the two spare normal states, a hashed alpha cutout anchored to the surface, one voxel of ground cover per eligible column | `--no-foliage` | [batch-14-foliage](foliage.md) |
| 15 | foliage specialization: a `march`-side `override` that takes batch 14's cross-quad out of the module when the world has no ground cover, and the first pass other than `resolve` to be specialized | none -- it moves zero pixels | [batch-15-foliage-spec](foliage.md) |
| 16 | water micro-normals: four summed cosine slopes over world XZ, band-limited against the pixel footprint, with the removed slope variance spent as glint width | `--no-waves` | [batch-16-waves](water.md) |
| 17 | `tile_select`'s warp shape: 4x4 to 8x8, one attribute and one divisor, for 2.3x of the pass | none -- it moves zero pixels | [batch-17-tile-select](gpu.md) |
| 18 | the coarse meadow: one repainted surface block standing in for the carpet of ground cover that cannot exist at stride 2, stamped at the apparent cover rather than the column cover | `--no-meadow` | [batch-18-meadow](terrain.md) |
| 19 | the grazing footprint: each wave octave band-limited against the pixel's footprint on the water plane along that octave's own direction, and the rolloff that correction turned out to require | `--no-wave-aniso` | [batch-19-graze](water.md) |
| 20 | shallow-water damping: the wave amplitude ramped by the depth of the water column under the pixel, read with a short vertical ray rather than borrowed from the refraction | `--no-wave-shoal` | [batch-20-shoal](water.md) |
| 21 | **shipped no change.** The sea's diamond weave was diagnosed to the per-block texture repeat, the footprint fix that mechanism implies was built, measured against a converged reference and **rejected** -- it scores monotonically worse. The tree ships pre-batch-21 | none -- the tree is unchanged | [batch-21-water-lattice](water.md) |
| 21b | **a one-token bug fix**: `shade_water` strode `block_faces` by 6 where the table's stride is 8, so the sea's medium colour was `tex::GLASS` from batch 14 on -- its pane border and its `(x + y) % 9` streaks, repeated per water block, being the reported salmon diagonals. The batch as briefed could not have worked and three renders said so. Free; moves up to 532,825 pixels | none -- a bug-compat branch in `resolve` is not worth a millisecond; the guard is a `const _: () = assert!` plus three tests in a new `tests/textures.rs` | [batch-21b-water-layer](water.md) |
| 22 | **a method batch**: the measurement fixture. The vantage set, the crops and the metrics as data rather than as prose; a validation rule for each metric; a converged-reference command that states its own noise floor; and the bit-exactness sweep as one command. Moves zero pixels and makes the build no faster | none -- it moves zero pixels | [batch-22-harness](harness.md) |
| 22b | **the checkup and the refactor batch 22 did not do**: every `clears X too` claim in the control table as a command -- eighteen of them, 56 claim/vantage pairs, all holding -- plus the two fixture defects that had made them unaskable, and `src/main.rs` split four ways | none -- it moves zero pixels | [batch-22b-checkup](harness.md) |
| 22c | **the split**: `CLAUDE.md` cut from 1,518 lines to 1,010 by moving the roadmap, this ledger and the live caveats into `docs/`, merging the two tables that listed every batch twice, and deleting twenty-one lines about a git remote that does not exist. Seven lessons found filed in the wrong section. Moves zero pixels and touches no file under `src/` | none -- it moves zero pixels | this file |
| 23 | **a method batch**: `--anim-time` and `--anim-rate`, the wave and cloud clock a headless render had pinned to zero since batch 8. Both default to 0, so the build is bit-identical to batch 22c's at fourteen vantages and there is no shader change at all. It bought an answer two batches had to leave open: the drifting cloud deck **does not ghost** (0.018 of a code value, max 2, speckle unchanged to four decimals) and the moving sea ghosts 0.34-0.66 while coming out **0.84x quieter** than the still one -- monotone to 0.78x at double the rate, at two vantages agreeing to half a per cent | none -- the defaults move zero pixels | [batch-23-anim-phase](temporal.md) |
| 24 | **the bench protocol, and the four-batch-old emergency it closes**: `--no-wave-shoal` did not cost +0.07-0.11 ms and its premise was false twice over. A **null** experiment analysed the way batch 20 analysed its A/B reports **+0.099 ms**, and the two builds were never byte-identical -- the `SPEC_WAVE_SHOAL=0` fold is **177 SPIR-V words** larger than deleting the code, which the driver's quantised 240128 cannot show. Measured paired and order-alternating the control is +0.04 at the coastline and zero at five other vantages. `harness bench` ships so no session re-derives the protocol | none -- it moves zero pixels | [batch-24-bench-protocol](harness.md) |
| 25 | **the fill wave schedule**: eight wave octaves subdividing the 32-3.14 band instead of four spanning it, at the same total slope variance, so the sea is equally rough and only the spectrum changed. Peak slope-field autocorrelation **0.9988 to 0.7314** on a metric that is a closed form in the shader constants and so has no window, padding or normalisation to leave unpinned -- which is what makes it the first statement of this quantity a later session can quote. Two axes ruled out along the way: octaves added *outside* the band batch 19 passes are worth 0.016, and **no amplitude exponent rescues four octaves** (best 0.979) or lifts six to where eight sit. Costs **+0.11 ms at the coastline and the shore, zero at the default camera** | `--no-wave-fill` | [batch-25-octaves](water.md) |
| 25b | **the cull**: `CLAUDE.md` from 1,075 lines to **669, 38% off** and below where 22c left it, after three batches had grown it back. The two files that left are defined by *when* they are read -- [`pitfalls.md`](pitfalls.md) before you type a command, [`lessons.md`](lessons.md) before you plan a batch -- because batch 25 walked into two documented traps with the document loaded, and made a third line-ending error the document had actively mis-explained. The no-fact-lost check reported four losses and **the check was wrong, not the table** | none -- it moves zero pixels | this file |
| 26 | **the mottle whose reversal kept its documentation**: the water layer's per-block mottling shipped at 0.20 while `textures.rs` said "**0.0 ships**" and `tests/textures.rs` named `build_atlas(0.0)` `shipping`. **Not an oversight** -- batch 21b built that flattening as its candidate fix, measured it moving **0 pixels at fourteen vantages** (the null that revealed the glass-layer stride), abandoned it correctly, put the default back, and left the lever in the roadmap as 21a's free half-measure -- without reverting the fix's prose. Batch 26 took the lever on its own numbers: **under half a code value of MAE and 27-34% of `speckle`** at `coastline` and `open-sea`, one 16x16 tile repeated in phase per block by `face_uv`, and the whole of batch 21's diamond weave. Ruled in by picture, not metric. Guarded by a `Config::default()` read, the join three literal-argument tests could not make | `--water-mottle 0.2` | [batch-26-water-mottle](water.md) |

| 28 | **the documentation cull, and the guard that makes it hold**: `README.md` **331 to 184 lines**, deleting the second copy of the frame, the second copy of the storage model, and the flag paragraph that named **48 flags against the briefing table's 43** -- two overlapping sets, neither a superset, and the prose carried neither the cost nor the clears that make the table worth reading. Four facts had already drifted: **a hand-written test count of 48 in one file against 82 in the other** for the same command -- this row says it that way because the guard below bans the shorter phrasing outright, **0.132 bytes per solid voxel against `PERF.md`'s 0.130**, and *the sea has no waves* sitting in the section a reader trusts most, **eleven batches after the waves shipped**. A fifth was in *both* files -- "`--help` lists every flag" while **six were missing from it** (`--bench-frames`, `--cam-height`, `--cam-yaw`, `--cam-pitch`, `--demo-edits`, `--hud`), and **three of those were documented only in the paragraph being deleted**, so the cull would have taken their last mention with it. The help text was fixed first, then the cull. What makes it hold is four guards in `tests/docs.rs`, the file batch 22c left behind for links: every parsed flag appears in `--help`, no document transcribes the test count, `README.md` quotes no measurement `PERF.md` owns, and `README.md` cannot regrow a flag reference. **Each was negative-tested against the drift it exists to catch** -- a guard that cannot fail is worse than none. The link guard itself was found already red: `DO-NOT-READ-CLAUDE.md`'s deletion left the tree at exactly 20 `.md` files against a `> 20` sanity bound | none -- it moves zero pixels | this file |

| 28b | **the briefing's own rules, applied to itself**: six of the sixteen "invariants" were not invariants -- indirect-dispatch aliasing, storage-plus-sampled bind groups, `u32` shift amounts, the template-argument parse, instance lifetime and the 65535 dispatch cap fire identically in **any** wgpu project, including one with a mesher, so they moved to [`pitfalls.md`](pitfalls.md) and the remaining ten are decisions this engine made. **"Bind group 0 has 19 entries (0-18)" was a transcribed count**, the class batch 28 deleted twice: it is now `render::BIND_GROUP_0_ENTRIES`, pinned in Rust by an array annotation and against the shader by `bind_group_0_matches_the_shader`, which also checks the indices are 0..19 exactly once each -- the "(0-18)" half a bare count misses. **The revert rule lost its ordering requirement**: "copy the exe before you write any of the batch" was the only option during the five batches with no `.git`, and forgetting it was unrecoverable; the parent commit has been reachable since batch 27, so the rule is now *build from the parent commit* and `harness.md` says the same. **A control that should be free and is not moved from the defer list to the fix-now list** -- it is not one bad number but a tax on every measurement taken through it, and `--no-foliage` at **+1.24 to +2.07 ms** is the precedent. `README.md` 184 -> 183: the eleven-line harness paragraph became a pointer to [`harness.md`](harness.md), which owns the bench protocol and states the ramp in numbers the README did not; the pitch stopped quoting a frame rate four lines above the sentence claiming `PERF.md` is the only file that quotes one; and "roughly sixty" flags went the way of the test count | none -- it moves zero pixels | this file |

| 29 | **persistence, and the first batch under a goal rather than a queue**: the edit journal. Every accepted `set_block` is recorded at the one point in that function where a write is known to be real, and replayed into the dense array in `stream::build` **before the tree, the attributes or the lighting read it** -- not through `set_block` after interning, which is what makes a reloaded chunk *identical* to a live-edited one rather than merely similar, and what gets its lighting for free. 16 bytes an edit through `bytemuck`, no serde and no new crate; the header carries the seed and the sea level and **refuses a journal written for another world**, because a hut in mid-air still photographs like a hut. **The default build changed behaviour with no flag on it**: a chunk unloads 240 frames after nothing needs it and used to come back unedited, so edits already evaporated *mid-session* -- replay at generation fixes that too, which is the half no capture can see and which `edits_survive_a_chunk_unload` is the only thing pinning. The round trip -- build through the edit path, save, reload, capture -- is **0 pixels at thirteen of the fourteen vantages**, and **nine of those thirteen are vacuous and say so**: the structure is out of frame there, so side A's hash comes back equal to the batch-28b capture and a journal that replayed nothing would have passed too. The four that carry the claim are `shore`, `terraces`, `underwater` and `cave`, where a loaded journal moves **59,976 to 921,599 pixels** against no journal and **0** against the same edits applied live. The fourteenth is `edits`, whose *own* arguments build the structure, so side B built it twice: **298,841 pixels** of hut on hut, the second floor landing one block up **because** the replay had already put the first one there -- run by hand at that camera with one structure, 0 pixels and MAE 0.0000. **No file under `src/render/` was touched** | `--load-edits` is the replay half; a build with neither flag is bit-identical to batch 28b at fourteen vantages | [batch-29-journal](persistence.md) |

| 30 | **edits at a distance**: the deltas reach every level, and the batch's first act was to correct its own brief. The roadmap said *aggregate children rather than resample*; coarse **terrain** must keep resampling, because `column_field` is one code path with only the stride differing and that is exactly what makes the snow line and the tree line agree across a transition -- aggregating it would cost `8^n` chunk builds to fix nothing. **The deltas were the only thing in the world a coarse level had no route to.** So `journal::apply` aggregates the edits inside each coarse voxel's footprint under three rules, each one `generate_coarse`'s own rule read back: the **topmost occupied block wins** (and a solid edit *below* the terrain's top in that footprint is ignored, or one block placed in a cave would turn eight blocks of hillside into planks); a voxel **clears only when every block it stands for is gone**; and that count is taken **against the coarse heightfield, not against LOD 0**, because the coarse levels have no caves in them and a count from the fine world counts blocks this representation never had. Ties break on a total order -- topmost, then z, then x -- since the map's iteration order must not decide a picture. The second half is that **`apply` reads the journal once, at build time**, so a stand-in interned before an edit never learns of it and it is precisely the one the player is about to walk away into: `record` names the chunk, `absorb_dirty` walks the ancestors, `update` rebuilds the resident ones a few a frame **ahead of missing chunks** -- an absent chunk shows nothing, a stale one shows something wrong -- and `insert_local` swaps each in on arrival, so no frame has a hole. Measured with the shipped LOD rules and no knobs, replaying a saved hut from altitude: **284 pixels at `--cam-height 350`, every one inside a single 25x25 box**, against **0 at 300** where LOD 0 still covers it -- the row that makes the others mean something. **The batch's own detour is the entry worth reading**: reaching the rebuild through a second `settle()` broke batch 29's round trip by 52 pixels at `open-sea` and 56 at `shore`, and three experiments showed it was neither the aggregate nor the rebuild but `update` itself -- **a second settle with no edits anywhere in the run reproduces the same hash**. `rebuild_dirty_now` does the work without advancing a frame; the non-idempotence is at the front of the roadmap, unexplained. **No file under `src/render/` was touched**. Found in passing and fixed, because the file it is in exists to be right about exactly this: the line-ending map listed `docs/sky.md` as CRLF and it is **LF**, with no batch having converted it, and the headline count was *8 against 91* for a tree that now holds **6 against 76** | 14 vantages, **0 pixels**, identical hashes against `voxelcraft-pre30.exe`; `implies` unchanged at 21 claims; batch 29's round trip back to 0 pixels at thirteen of fourteen, hash for hash | [persistence](persistence.md) |
| 31 | **the plan and the camera that reads it**: the batch's whole first act was to measure the premise it was given, and the premise was wrong. The front of the roadmap said `ChunkManager::update` is not idempotent on a settled world -- one extra call worth **52 pixels at `open-sea` and 56 at `shore`** -- and called that a measurement defect sitting under every bit-exactness claim in this table. **It is idempotent**: four extra `update` calls at an unmoved camera are **0 pixels at all fourteen vantages**, identical file hashes. What batch 30 had actually hit is that its extra call came from a second `settle()`, and a settle plans at whatever camera it is handed -- which at that point in the capture path is the eye `find_water` had just chosen, not the spawn eye the world streamed at. So the 52 pixels were **a stale plan being corrected**. `find_cave` and `find_water` cannot choose an eye until the world around the spawn column exists, so the first settle has to run at the spawn camera, and `shot_frame` takes the manager by reference and re-reads that one `render_set` for every accumulated frame -- nothing downstream could notice the eye had moved. Both headless paths now settle a second time once the eye is placed, **guarded on the eye having moved**, so the twelve vantages that never move one are untouched by construction rather than by measurement. The fix reproduces batch 30's two numbers exactly, hash for hash, which is what says it is that mechanism and not a second one. Batch 30's three eliminations were each sound and the conclusion drawn from them was not, because all three varied the edit path and none varied the camera -- that is the entry in [errors](errors.md) worth reading. The residual is real and invisible: the retention sweep is frame-counted, so a settled world goes on reclaiming coarse stand-ins for hundreds of frames, and it can never take a chunk the plan names. **No file under `src/render/` was touched, and no shader** | 2 of 14 vantages differ against `voxelcraft-pre31.exe`, **52 and 56 pixels, by design** -- the two the batch exists to correct, at the two new manifest hashes; the other twelve 0 pixels, hash for hash. `implies` unchanged at 21 claims, `validate` at 16 | [lod](lod.md) |
| 32 | **the player in the header**: a save file that remembers the world and forgets the player is a save file you have to walk back to. Position, look angles and the selected hotbar slot go into a 32-byte block between the header and the records, and **the spare word batch 29 left does the whole compatibility job on its own** -- it becomes `flags`, bit 0 says a player block follows, and a version-1 file wrote that word as a zero it chose itself, so "no player block" is what a version-1 file already says to a version-2 reader and the two versions load through one reader with no branch on the number. The version is bumped anyway, for the other direction only: a pre-32 binary handed a version-2 file names the version instead of failing on a byte count and blaming the edit records. Twenty-four bits of `flags` are still spare. **The half worth reading is who owns the camera, and that the boundary is drawn at the *mode* rather than per field.** The window restores unasked -- nothing there claims a camera, and asking for that with a flag would be asking a save file to be told it is a save file -- while a capture passes `--resume`, because `--cam-height`, `--cam-yaw`, `--cam-pitch` and `--cam-submerge` are measured from `spawn_position` and **all fourteen vantages of the fixture are those flags**: a journal that silently moved the camera would turn every named vantage into "wherever the file says". Splitting it per field instead -- letting the file pick the hotbar while the flags keep the camera -- reads perfectly reasonable and would mean a `--hud` capture quietly changed the moment a journal was loaded. `--resume` also switches off `find_cave` and `find_water`, the only two things that move the camera after the settle. The end-to-end claim is that a capture, saved and resumed, is the **same capture and not a similar one**: at `--cam-height 137 --cam-yaw 211 --cam-pitch -37`, **0 pixels of 921,600 and the same sha256**, against **921,600 and MAE 42.18** for the same journal rendered at the default vantage -- and `--resume` beats a conflicting `--cam-height 400` on the same file at 0 pixels. **The first attempt at that pair was vacuous and the fixture's own rule caught it**: run with the default camera on both sides it also reads 0, and would have read 0 with `--resume` doing nothing at all. `--hud`'s hotbar highlight was a literal `4` passed to `draw_hud` from the capture loop -- a picture claiming a selection no player held; it now reads `player.hotbar`, and `--hotbar` ships at **4** so that every `--hud` capture ever taken still reads the same, which is what makes this control exactly zero rather than nearly zero. Through the HUD the hotbar half is **3,044 pixels** between slot 4 and slot 7 and **0** between `--hotbar 7` and the same slot arriving through the journal, every differing pixel inside the 218x58 strip and none in the rest of the frame. Two refusals carry the seed guard's argument forward: a hotbar slot this build has no room for panics three call sites away, and a non-finite position renders a black frame, which is a frame. **No file under `src/render/` was touched, and no shader** | 14 vantages, **0 pixels**, identical hashes against `voxelcraft-pre32.exe`; `harness implies` unchanged at 21 claims; batch 29's round trip unchanged at 0 pixels and identical hashes at thirteen of fourteen, which is the measurement that says a headless mode writes the player and does not read it | [persistence](persistence.md) |
| 33 | **a world on disk, and a file that grows as you build**: batches 29 to 32 wrote the journal once, at the end of the mode, so everything they built was correct and none of it was safe -- a kill threw the session away, and having a world at all meant naming the file twice and hoping it already existed. `--edits PATH` loads it if it is there, **appends every edit as it lands**, and writes it back compacted on exit. **The design is the order of three writes, and each step has to leave a file the reader can still make sense of**: `FLAG_APPEND` goes in *before* the first record, because a file carrying an uncounted record without that bit is byte-for-byte what a truncation looks like and `load` has to refuse it -- the permission to recover must already be on disk when there is something to recover; then the record *before* the count, so the file never claims more than it holds. Killed between those two it holds one whole record nobody counted, which is recovered and announced; killed inside a record, the fragment is dropped and the log truncates it when it next opens the file, because an append landing on half a record puts **every later record 16 bytes out of phase** -- a save file that parses and is nonsense. **The relaxation is scoped by the spare bit and that is the point**: a file without `FLAG_APPEND` is held to precisely the check batch 29 shipped, longer than its count is corrupt and shorter is a truncation, so the compacted files every A/B in this table is measured on keep their guard -- and the bit is what batch 32's comment on `VERSION` said the next feature should take instead of a version. **`--edits` is not the other two flags spelled shorter**: those are a measurement pair, separately runnable so one binary can render `--demo-edits --save-edits f` against `--load-edits f`, and a missing file there is a typo that has to be an error; this is a *world*, a missing file is the ordinary first run, and **the sentence it prints is then the only guard left** -- "loaded your world" and "started a different one" look identical from inside the renderer. Sharing the path with either of the other two is refused rather than resolved by precedence. The append log hangs off `--edits` and not off `--save-edits`, so every A/B ever taken through those two still writes the same bytes. Measured at the fixture's own `edits` vantage on the 393-block hut: the log's file, handed to **`voxelcraft-pre33.exe`** -- a binary that has never heard of the flag -- is **0 pixels of 921,600** against the hut built live, and still 0 with 93 records deliberately left uncounted and 7 bytes of a half-written record behind them, same sha256. **The two rows that make those mean anything are 67,983 pixels** for the counted records alone -- what a reader trusting the count would have thrown away -- and **348,624** for no journal at all; pre33 handed the same doctored file refuses it and exits 1. The residual is named rather than fixed: **an edit is discrete and a position is continuous**, so appending fixes the deltas exactly and cannot fix the player's position, and the entry in [errors](errors.md) that said those were one defect with one fix is corrected. `--no-exit-save` is the machinery that made any of it observable, negative-tested in a pair. **No file under `src/render/` was touched, and no shader** | 14 vantages, **0 pixels**, identical hashes against `voxelcraft-pre33.exe`; `harness implies` unchanged at 21 claims; `validate` clean at the start of the batch; batch 29's round trip re-run, and the eight new tests negative-tested against two stubs that fail four and three respectively | [persistence](persistence.md) |
| 34 | **the bar in the file**: batch 32 persisted *which slot* was selected, which is a save file remembering the cursor and forgetting the hand. The ten slots' *contents* now go into a 24-byte block behind the player block, so `block::HOTBAR` is a **default** rather than the bar -- `Player::bar` holds the live one, `--bar` authors one for a capture, and middle-click fills a slot from the world. **The roadmap's scope fork was taken at its narrowest**: this is the file-format half of the inventory item and nothing runs out. `FLAG_BAR` is bit 2, a **second presence bit rather than a wider player block**, and the reason is specific rather than tidy -- a version-2 file has a player and no bar, so folding the bar in would make the player block's size depend on the version and reading it a branch on that number, which is the one thing the `flags` word exists to avoid. Batch 32's claim of *one reader, no branch on the number* is therefore now made about **three** versions, and this is the first batch where it was tested against a layout the reader had never seen. **24 bytes because the records behind it are read in place**: ten `u16` is 20, and all four combinations of optional blocks have to leave the record array 8-byte aligned -- so the assert is on the four sums and not on the struct, and the two spare `u16` are load-bearing rather than decoration. `player_bytes` became `extras_bytes`, a sum, with three readers that must not disagree about where the records start. **The half worth reading is which blocks a bar may not hold, and that every refusal keeps an argument some other file already makes**: `TALL_GRASS` most of all, because `SPEC_FOLIAGE` compiles the marcher *unable* to see a tuft in a run the generator gave none and the `const` assert beside `FLAG_FOLIAGE` that has guarded that since batch 14 can only ever speak for a compile-time array -- a placed tuft would be invisible, not wrong; `WATER` keeps `flags_from`'s claim that a run with no sea can never acquire a water pixel; `AIR` is a category error; anything past `BLOCK_COUNT` is a panic in `def` three call sites away. **Picking is the caller that makes the list earn its keep and has nothing to do with files** -- the world is full of both, and the crosshair can be pointed at either. `--bar` is refused *as a whole* where `--hotbar` clamps, because a slot number out of range has a nearest legal answer and a block id does not, and it is refused against a journal that already carries a bar rather than losing to it silently. **The bar restores without `--resume` and the camera still does not**: the boundary batch 32 drew is the mode's, no vantage names a bar, and a bar nobody set is `block::HOTBAR` -- so the existing capture corpus is untouched by arithmetic rather than by luck. End to end through `--hud`: a bar authored live and the same world reloaded are **0 pixels of 921,600 and the same sha256**, against **1,604 pixels, max 241** for the default bar at that camera -- the pair, because the first row alone is the vacuous claim this fixture has caught before. A 24-byte-shorter test helper turned the format change into a failing assert instead of a quieter number. **No file under `src/render/` was touched, and no shader** | 14 vantages, **0 pixels**, identical hashes against `voxelcraft-pre34.exe`; `harness implies` unchanged at 21 claims; `validate` clean at the start of the batch; six new tests, every positive claim in a pair with the negative that says it is not vacuous; `voxelcraft-pre34.exe` handed a version-3 file names the version rather than failing on a byte count, which is the bump batch 32 paid for and this is the batch that collects | [persistence](persistence.md) |
| 35 | **terrain casts, and the occluder is a heightfield rather than a march**: the last visible hole in the stated cinematic goal, open since batch 11 -- the deck shadowed the haze's sun lobe and a ridge did not, so a mountain with a low sun behind it carried an unshadowed patch of glow on its dark face. The honest fix is a `shadow_ray` per shaft sample, priced at **+10.57 ms** of `resolve` against a frame that costs 4, and its cheapest corner -- four steps looking 128 blocks -- **finds nothing at all**, because the ridge is 300 to 600 blocks away: *the budget that makes it affordable is the budget that makes it useless*. **What ships is not a cheaper march but a projection, and that rejection stands unchanged.** For a directional light the shadow of a heightfield is itself a heightfield -- one altitude per column below which the sun is blocked -- so `lit(p)` is `p.y > E(p.xz)`, a single tap, and the search along the third dimension has been done once for the whole world instead of once per sample. **That `E` is an altitude and not an angle is the entire reason it works**: horizon mapping is the obvious neighbour of this idea and is the version that cannot, because a maximum elevation *angle* is referenced to the surface and the angle to the ridge that set it needs a distance a horizon map has already thrown away. The field is a max-plus prefix scan -- Hillis-Steele in the `(max, +)` semiring -- so a 256-texel reach costs **eight taps per texel instead of 256**, and it **ping-pongs rather than running in place**, which is a determinism decision and not a correctness one: in place, a thread reading a neighbour another had advanced would take a *longer* reach, harmless for the answer and fatal for a project whose every measurement rests on `validate` rendering the same arguments twice and getting the same file. **Two properties of this engine are why the heightfield is cheap here and would not be in general**: `WorldGen::height` is a pure function of the column, so the envelope covers ground **no chunk has been streamed for** -- a structure built by projecting resident geometry loses the ridge exactly when a low sun wants it -- and the field is static per column, so only the *sweep* is per-frame and the window scrolls two 512-column strips per texel of walking. `shade_hit` keeps its real `shadow_ray`, and that asymmetry with `cloud_shade`'s two callers is deliberate: the ground shadow is paid per *visible pixel*, where exactness is affordable, and handing it the quantised envelope would trade a correct shadow for a coarse one to buy nothing. **The batch caught two things in itself and neither was found by a sweep.** The fill samples each texel's centre and `terrain_shade` read it back as though index `i` sat at the cell's edge -- a uniform two-block displacement of every shadow, smaller than the ramp it is blurred over, invisible in the A/B, in the control and in every metric the fixture has, and found by rereading the code. And `the_scan_is_the_envelope_it_claims_to_be` was written asserting agreement with a brute-force gather to `1e-3`, on a theorem that holds over an integer lattice and not over the bilinear taps a non-axis-aligned sun actually makes; it failed, the code was right, and **what it caught was the batch's own prose in three files**. Rebound to `shaft::SOFT` the same test says the real number: **0.6 blocks, a seventh of the ramp**. The fixture consequence is the one worth carrying forward: **`--cloud-shadow 0` stopped being a pre-batch-11 revert on its own**, because the deck stopped being the shaft's only occluder, and `harness implies` went from 21 claims to 22 as a *pair* -- the `Nothing` that now needs both controls, and the `Something` that says the first is a claim about composition rather than about a control nobody wired up. Four residuals, all in [errors](errors.md): overhangs cast solid columns, a cave mouth admits no shaft, tree canopies do not cast, and player edits do not either -- the canopy is close to a line and was deliberately left, because a canopy height is a look constant and this batch was the mechanism | 14 vantages, **0 pixels**, identical hashes against `voxelcraft-pre35.exe` under `--no-terrain-shafts` -- and the hashes are the batch-start `capture` manifest's, unchanged. The positive half is **10 of 14 vantages moving**, 258,498 pixels at `low-sun` and 202,276 at `sky` of 921,600; the four that do not are the existing bounds working rather than the feature failing -- `night` has no lobe, `lattice` looks away from the sun, and `cave` and `underwater` have no haze along the ray. `harness implies` **21 -> 22 claims**, every one as the control table says, including 393,912 pixels at `sky` for the new positive half. `validate` clean at the start. Cost **+0.17 to +0.30 ms of the frame** over four benched sessions at two vantages, always significant on `total` and **never separable on `resolve`**, which is what a fixed-cost pass and a small tap look like | [sky](sky.md) |
| 36 | **a hash per face, and the class belongs to the texture**: `face_uv` takes the position *inside* the voxel, so every block has sampled the identical 0..1 of its layer, in phase, forever -- on a grassy slope `GRASS_SIDE`'s fringe profile is a function of x alone, identical in every block, and the grass/dirt boundary lines up block after block. On record since batch 21, never measured on land. **The batch owed three research prompts before it owed any code**, and what came back had a shipped answer with a name: OptiFine's `natural.properties` binds each texture to a symmetry group and binds `grass_block_side` to mode **F, horizontal reflection only** -- rotation would swing the grass rim onto the side or the bottom of a face, mirroring alternates the profile while leaving the fringe *height* fixed. That is `perm::FLIP_U`, arrived at by somebody else first. **The one structural decision is that a class belongs to the layer and not to the block or the face**: `GRASS_SIDE` is four faces of one block, and binding the rule to the picture makes it impossible to give the same picture two different rules. **It is exact on this atlas and would not be in general** -- 16x16, power of two, `mag_filter: Nearest`, so a mirror or a transpose is a bijection of the texel grid with no resampling and no colour that was not already in the palette, and it commutes with `downsample`'s box filter, so every mip is the permutation of the mip rather than a blur of it. Three bits generate all eight of `D4` from one transpose and two mirrors, written with `select` rather than branches because `pclass` varies pixel to pixel wherever two materials meet. **The key touches nothing about a ray**: it is the integer world coordinate of the voxel's minimum corner, which `shade_hit` already had exactly in `box_min`, plus the face -- a key drawn from the float hit point would flip along a face seam as `taa` moved the sample, and the 3x3 neighbourhood clip would read a static world as a moving one. **`block_faces` words are packed now and that is the batch's real hazard**, the same shape as the stride of eight that bit batch 8 for six batches: a raw word used as a layer indexes the atlas far past its end, so `face_layer`/`face_perm` are the only ways in, `face_layer_at_every_call_site` holds all three readers to them, and the one local allowed to hold a raw word has to be *named* `*_word` to say so. That test was negative-tested by removing an accessor, which is what the fixture's own rule asks for. **What the batch declined is as deliberate as what it took**: `blob()` wraps with `rem_euclid(16)`, so `STONE`, `GRAVEL`, `COBBLE`, `GLOWSTONE`, `BEDROCK`, `SNOW` and half of `LICHEN` tile *seamlessly* across block edges today and a permutation breaks that join -- thirteen of twenty-one layers stay at `NONE`, and whether the variety is worth the seam is a look question this batch did not answer. It also declined the thing all three documents made their primary recommendation, **N procedural variants per material**, on grounds none of them address: the two fix the same defect, so shipping both means no measurement can say which paid, and the permutation is the free half. **And it corrected the reason this was rejected three batches ago.** [errors](errors.md) recorded that a hashed uv *offset* cannot work because the sampler is `ClampToEdge` -- but an address mode applies *within* an array layer, `Repeat` would wrap correctly, and `face_uv` already clamps `local` to [0, 1] so the switch would move no pixel. **The sampler was a setting, never a constraint.** What actually kills the offset is content: shifting a bounded tile moves `GRASS_SIDE`'s fringe across the middle of a face. The conclusion survived its reason being wrong, which is the kind of thing worth finding out. **The picture says one thing nothing had said before**: with the phase broken, the default slope still bands -- because a large part of what the roadmap called corduroy is the *terrain's staircase* and not the texture's phase. The defect was filed as wholly textural for several batches and it is not | 14 vantages, **0 pixels**, identical hashes against `voxelcraft-pre36.exe` under `--no-tex-variation`. The positive half is **297,118 pixels of 921,600** at `default` -- 32% of the frame -- MAE **1.3747** and max **37** on the `slope` crop. **The fixture cannot see the defect this batch fixed**: `speckle` went 4.0573 to 4.0533 across that same change, blind exactly as [harness](harness.md) warns a coherence complaint will be, so the ranking is a picture and 36b's first item is the missing metric. Cost **+0.041 to +0.053 ms of `resolve`** at `default`, t = -3.83 and -8.47 over 8 paired rounds, which is a hash and a handful of `select`s and nothing else; **the control is free by the fold and not by the stopwatch**, which is the honest order here. `shaderstats` in both directions puts `resolve` at **267,904 bytes with it and 265,856 without** -- 2,048 bytes of machine code leaving the module -- with the register count unchanged at 72 and `march` byte-identical, the same code-size shape batch 13 found and not an occupancy one. The two benches of the control against the revert read **-0.039 +/- 0.028 (t = -1.37)** and **-0.077 +/- 0.054 (t = -1.45)**, both formally indistinguishable from zero -- but neither had the resolution to separate 0.04 ms from 0, so they corroborate the fold rather than prove it, and saying so is the point. `harness implies` **22 -> 23 claims** -- a `Something` with no `Nothing` beside it, which is the opposite shape to every other pair in that table and is the whole claim a control with no composition to check can make: that it is wired up at all. `validate` clean. Three new tests, one negative-tested against a removed accessor; `clippy` silent. **One repair beyond the batch**: `shaderstats` had been dead since batch 35 added bind-group entry 19 against its hardcoded `0..19`, which nothing noticed until this batch needed it to check a fold -- it now counts from `BIND_GROUP_0_ENTRIES` | [research-review](research-review.md) |
| 36b | **a metric for a repeat, and the frame is the wrong place to take it**: batch 36 moved 32% of the default frame's pixels and `speckle` went 4.0573 to 4.0533, blind exactly as [harness](harness.md) warns a coherence complaint will be -- so every ranking in this area was a picture, and the two items behind it in the queue are both rankings. **The obvious instrument is blind and the reason is perspective, not tuning**: correlate a crop against itself over a lag annulus and both builds read 0.4 to 0.7 at every lag and differ in the *fourth decimal*, at high-pass windows of 3, 7, 15, 33 and 129 alike. A block spans about 20 pixels at the bottom of the `default` frame and about 3 at the crest, so the lattice the eye reads as one surface has no single period in the image -- every depth's peak lands at a different lag carrying a few per cent of the area, and the sum is a monotone decay with no peak in it. **Inside a 64-pixel tile there is one period and the peak comes back**, which is `coherence`: the mean over tiles of the peak normalised autocorrelation in a 6-to-32-pixel annulus, after `speckle`'s own 3x3, so the two metrics high-pass identically and differ only in what they do next. Every constant was swept and each has an optimum rather than a direction -- tile **1.18x at 48, 1.20x at 64, 1.19x at 96, 1.08x at 128**; band 1.20x at 3 falling monotonically to blind at 129; annulus **1.07x stopping at 20 against 1.21x reaching 24**, which locates the responding lattice at 21 to 24 pixels. **And it is worth 1.2053x and no more, which is the honest ceiling of an image-space reading**: the peak is a lattice, the lattice is the terrain's staircase and the texture's phase *together*, and the control can only move the fifth of it that is texture. Under 8% at every other crop and **exactly nothing at `lod`**, where nothing is a block wide. So the quantity is computed a second time where it has no estimator in it at all -- **`tests/repeat.rs`, on the atlas**, in the shape `tests/waves.rs` set: two blocks show one layer under two draws of one hash, and the mean correlation between what they show is a closed form in the texels and `PERM_CLASS`. That table is the batch's finding. The thirteen layers at `NONE` score **1.0000 by construction**; the seven isotropic ones under `D4` score **0.1395 against a 1/8 floor of 0.1250**, so the dihedral group is within sampling error of everything eight states can buy; and **`GRASS_SIDE`, the layer the whole of batch 36 was aimed at, scores 0.8365** -- mirroring u leaves every row where it was, the green band stays at the top and the soil at the bottom, and only the fringe between them alternates. **That one number explains the 1.20x**: the batch took seven layers to the floor and left its own subject at 0.84, and a frame full of grass slope is where you see it. **One error caught by an assertion that turned out to be a prediction**: the first score centred three channels on one global mean, so most of the variance was the *palette* -- dirt's red 50 code values above its blue, which no permutation touches -- and every `D4` layer read 0.88 to 0.94. `rg_std`'s defect exactly, two quantities charged at once. **A second prediction was also wrong and is recorded as such**: the mip test was written asserting the repeat climbs at range and it does not, because a box average of independent noise is independent noise -- what grows is the standard error, so the assertion is now the 1/N floor within the texels left | **no control, because there is no renderer change to control**: `src/render`, `block.rs`, `textures.rs`, `config.rs` and `scene.rs` are byte-identical to the parent commit -- a stronger statement than a sweep of matching captures, and the sweep was run anyway because the rule is the rule: `bitexact --exe-b voxelcraft-pre36b.exe` is **14 vantages, 0 pixels, identical hashes**. The metric's own known-positive is synthetic and in `tests/harness.rs` -- the same noise latticed at 20 pixels and unlatticed reads **1.0000 against 0.0785**, a 12.7x separation at the correct lag, where `speckle` cannot tell the pair apart. `CHECKS` **6 -> 7 rows**, the new one at `--no-tex-variation` -> `coherence` -> `default/slope`, `RatioAtLeast(1.10)` against a 1.2053x response -- a `RatioAtLeast` and not a delta because putting the phase back has to make the frame *more* regular and the per-tile readings behind that mean go both ways. `implies` unchanged at **23 claims over 67 rows**, whose own `--no-tex-variation` row moves 297,118 pixels at `default` and **597,986 at `terraces`** -- where `coherence` reads 0.9957x, which is the cleanest statement this fixture has that how much of a frame moved and how regular it is are different questions; `validate` clean; `clippy` silent. New guards: the synthetic lattice pair and the flat crop on the metric, and on the repeat score the layer table, the mirror-against-group gap, what minification does to it, the hash's uniformity and the independence of neighbours, and the shader's own three state bits read out of `render::shader_source()` | [harness](harness.md) |
| 37 | **the ground past the march, and the entry's premise was worth more than the feature**: `shade_hit` marched a real `shadow_ray` to `shadow_dist`, a literal **220 blocks**, and nothing shadowed anything past it -- while batch 35's light envelope already reached 1024 and was being filled and scanned every frame for the haze alone. So the batch is a second tap into an existing field. **The design is one offset and it turns a blend into a partition.** The obvious form, `lit * terrain_shade(hit)`, is wrong in the way a screenshot flatters: `E(hit.xz)` is a maximum over *all* occluders up-sun including every one the march had just answered exactly, so the envelope's 4-block quantisation gets a second vote on near geometry and paints soft false shadows over ground the marcher had resolved to the voxel. Sampling the envelope `d` blocks **up the sun ray** removes exactly those -- write out `E(q.xz) = D*tan + max over s >= D`, note `q.y = p.y + D*tan`, and the two `D*tan` cancel, leaving `q.y > E(q.xz)` equal to `p.y > max over s >= D`. **Sampling `d` blocks up-sun is the envelope with every occluder nearer than `d` struck out of the maximum**, so the march takes `[0, shadow_dist]` and the envelope the rest, with no gap and nothing counted twice -- and **no crossover distance and no blend width were authored, because the cancellation left nothing to tune**. The same algebra says the restricted maximum can never exceed the full one, which is what makes it safe to multiply into a march rather than mix with. **The entry's second sentence was never measured and is wrong by about two orders of magnitude.** It said "at a low sun the whole far field is flat, evenly lit terrain"; a diagnostic build with `shadow_dist` at 1500 moves **15,348 pixels of 921,600** at the best vantage in the fixture, 1,328 at a mid camera, and **759 / 616 / 0 / 602** across four seeds. The mechanism is a squeeze with no tuning in it: an occluder past 220 blocks must clear a ray that has already climbed `220 * sin(elev)`, needing elevation under about 20 degrees against this generator's relief, while the ground's own `ndl` **is** `sin(elev)`, so under about 5 degrees there is no direct term left to remove. Swept on the sun alone: **18,062 pixels at `--time 0.26`, 1,104 at 0.30, 0 at 0.33 and above**. **And the long march is not a ground truth past the chunk grid**, which is the other half worth carrying: `shadow_ray` returns false the moment the ray leaves it, `GRID_X` is 32 chunks, and at a 1-degree sun a shadow is kilometres long -- so the envelope shadows **2,805** pixels the march does not there and **1** at a 5-degree sun, which is the march running out of world rather than the envelope being wrong. Where both move a pixel they agree in direction **12,955 of 12,955**, and the envelope darkens one further than the march **zero** times. The 559 and 207 it misses the other way are unattributed between the canopy and the quantisation, and are why the canopy lift moved to the front of batch 35's leftovers: the envelope has two readers now, so one fill line improves both | 14 vantages, **0 pixels, identical file hashes** against `voxelcraft-pre37.exe` under `--no-distant-shadows`. The positive half is **7 of 14 vantages moving** -- 18,062 pixels at `lod`, 9,411 at `lattice`, 1,306 at `sky`, 698 at `low-sun`, and single and double digits at three water vantages; the seven that do not move are the squeeze above working rather than the feature failing, `night` having no sun and `overcast` at `--time 0.50` having a sun too high for anything past 220 blocks to reach. `harness implies` **23 -> 26 claims**, every one as the control table says, and the three new ones are a set rather than a row: that the ground's reader is wired up at all, and that it is independent of the haze's in **both** directions -- 9,411 pixels adding `--no-distant-shadows` to `--no-terrain-shafts`, and 258,528 adding `--no-terrain-shafts` to `--no-distant-shadows`. **`--no-terrain-shafts` stopped being a pre-batch-35 revert on its own**, exactly as `--cloud-shadow 0` stopped being a pre-batch-11 one at batch 35, and the pair is now both flags. Cost **+0.062 ms of `resolve` at `lattice`** (t = -9.65) and **+0.019 at `lod`** (t = -15.00), **indistinguishable from zero at the coastline** (+0.045 +/- 0.042, t = +1.07) -- separable from zero where the shaft's tap never was, because this one is per visible pixel and not four times a view ray under a ramp. **The control is free by the stopwatch as well as by the fold**: `--no-distant-shadows` against the revert at `lod` reads resolve **+0.000 +/- 0.000** and total +0.001 +/- 0.004. `capture` and `validate` clean at the start; `clippy` silent; one new test, `the_offset_is_the_envelope_with_the_near_occluders_struck_out`, which pins the identity to 1e-2 blocks rather than to `SOFT` and carries two assertions under it -- that the restricted maximum is never above the full one, and that the handoff removes an occluder at a quarter of the columns, without which it would pass on an offset of zero | [sky](sky.md) |
| 38 | **a canopy with holes in it, and the holes are geometry**: `BlockDef` had `leaves` and `pine leaves` at `opaque: true`, so a tree was a solid green box with a trunk under it, and every screenshot in this repository showed it. **The batch owed three research prompts before it owed any code**, and all three documents came back with the same answer and it was not the one the roadmap entry assumed: not a marcher that passes *through* a partly transparent face, but a **4^3 sub-voxel occupancy mask** -- a fourth tree tier, 64 cells in one `u64`, the same bitmask arithmetic `march_chunk` already compiles for level 0. **The argument that decided it is not performance**: a cutout in the *tree* discards no texel, so the atlas alpha stays the batch-12 tint mask for every block -- and `textures.rs` builds `LEAVES` with `pxt`, so leaves are alpha-255 *tintable* and batch 14's "for foliage, alpha means opacity" could never have extended to a block that needs both meanings at once. It also retires `Castano` renormalization and footprint-scaled thresholds before anyone builds them, because a cutout that is geometry has no alpha to mip. **The fit nobody in the research saw**: a 4^3 cell needs 2 bits an axis and the voxel field has had exactly six spare since the key was designed, one pair per byte -- so `make_key`'s layout did not move, and *the key is now full*. **The cost of this batch was not the feature, it was finding where an override may sit.** Every shape that put `if SPEC_LEAF_CUTOUT` on `hit_t`'s path left the *disabled* build one ULP from the binary it reproduces -- 201 pixels of 230,400 at max delta 1, terrain only, sky byte-identical -- and three shapes were tried before removing the branch entirely proved the parameter and the widened decode were never the problem. What ships has no branch there at all: `march_chunk` records the *entry* micro-cell on every solid hit, so the micro plane reduces to the cube's own face exactly, cell 0 at offset 0 and cell MICRO-1 at offset MICRO. The awareness flag was a second instance of the same thing, worth **5,120 bytes of `resolve`** written the way the foliage flag beside it is, down to 1,024 nested. **`march` folded byte-identical throughout; it is the deferred pass, reached through water's secondary rays, that is fussy.** Dappled shadows fall out for free and needed no ray-class plumbing -- a shadow ray meets the same holes the camera does, because they are the same geometry, which is why there is no `skip_cutout` beside `skip_water` and `skip_foliage`. **What it leaves**: `LEAF_FILL` ships at 0.72 unswept, and the LOD cliff the research ranked critical is real and bounded -- both at the front of the roadmap | 14 vantages, **0 pixels**, identical hashes against `voxelcraft-pre38.exe`. The positive half is **34,986 pixels of 921,600** at `default` (3.80%), MAE 0.1531, max 194; **2,508 of 921,600** at `lod`, max 16, all inside one bbox in the near field because the cutout is LOD 0 only. `shaderstats` folds `march` 21,504 -> 17,536 bytes and `resolve` 291,200 -> 269,440, registers 72 -> 58 and 80 -> 72. Seven new tests, one of which pins the no-branch finding so a later tidy cannot undo it; `clippy` silent | [foliage](foliage.md) · [research-review](research-review.md) |
| 39 | **the known-positive was the tree canopy, and nobody had suspected it**: `harness validate` failed on an untouched parent commit, which `CLAUDE.md` says is the batch and you fix it now, so batch 38b (glass) was put back on the queue with its design worked out and one of its two stated costs retired. `coherence`'s `--no-tex-variation` row had fallen from **1.2053x to 1.0755x**, under its 1.10 floor, **with no line of the metric changed and no constant moved** -- bisected to batch 38 in three runs against pre36b, pre37 and pre38, all three of which still read 1.2053x. The fixture could not say *why*, because `coherence` is a mean over 190 tiles and reports only the total, so the reading that tells a diluted mean from a deleted signal did not exist. It does now (`compare --tiles`), and it is decisive: **35 of the 190 tiles carried 79% of the response, those 35 are the tiles holding tree canopy, and every canopy-free tile is unchanged to four decimals**. So the in-phase lattice this control was mostly moving was the `LEAVES` layer, `D4`-permuted, on solid leaf cubes -- never `GRASS_SIDE`'s corduroy on the slope the crop is *named* for, which `metric.rs`, `harness.md`, `tests/repeat.rs` and the parked 36c entry all said or implied. **`tests/repeat.rs` had the causality backwards**: the swing a frame control produces is the distance from a layer's permuted score *to 1.0*, so `D4` at 0.14 swings six times as far as `FLIP_U` at 0.84, and the score times the layer's *area* is the prediction. **Batch 38 was therefore accidentally the larger share of a texture-repetition fix.** Three repairs were measured and declined rather than argued away: a robust summary (mean 1.0755x, median 1.0836, top-quarter 1.0683 -- they agree to the second decimal, so the tiles did not stop contributing, the thing they contributed stopped existing), a re-sweep of all four constants (none moves; `COH_LAG_MAX` 24 edges 32 by 1.0853 against 1.0755, a third decimal), and re-pointing the row (nowhere to go -- 1.0736x at `coastline/water` and under 1.02x everywhere else, the **wrong way** at `shore`, `terraces` and `underwater`). Propping it up with `--no-leaf-cutout` on both sides restores 1.2053x exactly and was **refused**, because the row would then certify the instrument against a build nobody ships. What shipped is the floor re-fitted to **1.037**, the whole finding at the row itself, and the fitness claim moved to where no frame can reach it -- the synthetic lattice pair in `tests/harness.rs` at 12.7x, which had been carrying it all along | **no control, because there is no renderer change to control**: the diff is `src/harness/`, `src/bin/harness.rs` and two test files, and **no shader, no `src/render`, no game source at all**. `bitexact --exe-b voxelcraft-pre39.exe` run anyway because the rule is the rule. `validate` **8 of 8 and 9 of 9 crop contents**, `implies` unchanged, `clippy` silent, suite green. **No test was added and two had their prose corrected**, which is the right shape here: the finding is that a *documented attribution* was wrong, and the guard against that is the synthetic lattice pair already in `tests/harness.rs`, now carrying the fitness claim explicitly | [harness](harness.md) · [errors](errors.md) |
| 38b | **glass stops being an opaque cube, and the tag bit was never needed**: `block::GLASS` had shipped since the first batch as an opaque cube with a pale blue texture on it, so a window was a wall you could see the bricks of. What ships is **the water model with the medium taken out** -- the primary ray stops at the first pane and writes an ordinary key, `resolve` spawns one transmitted ray, and Fresnel at glass's own `F0` mixes it against a sky reflection. **The transmitted ray does not bend, and the batch had to correct its own design to get there**: `refract()` copied from `shade_water` put a double image inside the glasshouse, because a slab refracts twice and `skip_glass` means the ray never finds the exit face to cancel against -- so the medium is the one term that does not generalize, and it was found by looking at a picture after every number had gone green. **A second defect the user found in the window, and the fixture structurally could not**: `trace_world` had hard-coded `skip_water = true` since batch 8, right for water's own three callers and wrong for the first one glass added, so **the sea was invisible through a pane** -- 21,441 pixels of 518,400 at max delta 134. Every vantage holding water holds no glass and the one holding glass holds no water, so both the fourteen bit-exact rows and the fifteenth's 168,456 were green and blind. A default that is right for every caller you have is still a default. **The fix is correct and costs real time**: `shade_water` joining `shade_glass`'s call tree took `resolve` to 615,424 bytes and 96 registers from 80, and the batch now taxes a water-filled `coastline` frame holding no glass by **+1.036 ms of 12.3**, which is roadmap entry 38e at the front of the queue. **The entry's design said one free bit would tag the hit and there was no free bit**, because batch 38 had spent the voxel field's last six on the micro-cell -- and the feature never needed one: `resolve` already calls `block_at` to tell water from everything else, so **the block id is the tag**, and the reusable half is that a primitive the *marcher* resolves differently needs key bits while a surface `resolve` *shades* differently does not. The cap at two interfaces is likewise an asymmetry rather than a counter -- `march_chunk` grew a `skip_glass` beside `skip_water`, the primary ray passes `false` and **every secondary ray passes `true`**, which is water's own rule since batch 8 and is what makes a double-pane greenhouse resolve to what is behind both walls. **The research called a separate opaque-only depth buffer *mandatory* and it is not owed**: Correction 2's mechanism is right and its consequence does not hold here because `trace_world` never reads `hiz` or `pairs`, priced at **0 pixels and identical hashes at all fifteen vantages under `--no-hiz`**. `schlick_glass` is a second function rather than a parameter on `schlick`, deliberately, because batch 38 measured what one folded-away ULP costs a control. **The light scope was the user's: glass is `opaque: false`, so the flood runs through a pane and a glasshouse needs no hole in its roof where the batch-29 hut does** -- a *world* change no pipeline override can revert, which makes `--no-glass` **the first control in this project that is not a pure revert** | **fourteen vantages, 0 pixels, identical hashes** against `voxelcraft-pre38b.exe`, and the fifteenth is the positive: **168,456 pixels of 921,600**, MAE 41.81 over the `pane` crop, max channel delta 129, against the same glasshouse replayed into the old binary through its journal. The control's impurity is **131,689 pixels at max delta 17** and **0 pixels** off the structure | [batch-38b-glass](shading.md) |
| 40 | **the sweep, and the cost was not where any of the three documents put it**: P1's research had come back with three families of water optimisation, all three ranking a sky-term fix first, and the entry it supported was none of them -- so the batch spent its session on **eight paired benches instead of a feature**, each against a build one constant away from `voxelcraft-pre40.exe`, twenty seconds a build. **The ranking inverted in both directions.** `CLOUD_OCTAVES` 4 -> 1, ranked first by all three documents and priced by one at "at least 3.0 ms", is worth **-0.594 ms** -- and **-0.619 with `--no-water` on both sides**, which is the row that settles it: the saving is the *same size with the water taken out of the frame*, so water's own `sky_color` calls cost nothing measurable and the entire cloud FBM at a sea-filling vantage is 0.6 ms of primary sky. A planar cloud buffer is a **sky** optimisation and moved to P2's candidates with its real ceiling attached. `WATER_REFLECT_DIST` 768 -> 384, the one family the research did rank second, came in at **-1.476 ms** against its own projection of ~0.45 -- 3.3x under. **What nobody proposed is the finding**: all three documents modelled a secondary ray as a traversal loop, and it is not one -- a refraction ray that *hits* calls `shade_hit`, which casts a **third** ray, and that ray is **-4.100 ms +/- 0.068 of a 13.080 ms coastal `resolve`, 31% of the pass**, the largest single number ever measured in this engine. Its curve was measured as well -- **0.702 / 1.707 / 2.303 / 4.100 ms saved at 32 / 16 / 8 / 0** -- and the shape is the half worth keeping: the first eight blocks cost three to five times per block what the last sixteen do, because most shadow rays terminate early, so the near blocks are paid by every ray and the far ones only by survivors. `shore` reads -2.096 ms for the same 48 -> 0, so the effect is not one vantage's. **Nothing shipped, deliberately**: both remaining candidates move a look, and the batch's own rule is that only `--reference` ranks one -- so it handed the next session a *look decision with its cost already known*, which is what batch 41 then spent | **no control, because there is no renderer change to control**: the diff is `PERF.md`, `CLAUDE.md`, `lessons.md`, `research-review.md` and `roadmap.md`, and no source file of any kind. `harness validate` clean before the sitting. Every diagnostic build was thrown away after its bench and the tree was left byte-identical to `2ba2659`; `voxelcraft-pre40.exe` is that commit, built as the sweep's fixed side A | [research-review](research-review.md) · [lessons](lessons.md) |
| 41 | **the shadow ray under the sea, cut from 48 blocks to 16, and the look cost belongs to batch 37**: batch 40 left a *look decision with its cost already known* -- truncate `WATER_SHADOW_DIST`, or shorten the traced reflection's reach -- and said to ship one. **Ranked, they are not close.** Seven one-constant diagnostic builds, twenty seconds each, swept against `voxelcraft-pre41.exe` at six vantages: the shadow budget at 32 / 24 / 16 / 8 / 0 moves **80 / 201 / 784 / 17,462 / 50,054** pixels of 921,600 at `coastline` and a **max channel delta of 1 / 1 / 1 / 2 / 3**, while the reflection reach at 384 moves **22,183 at max delta 109** at `lattice` and 13,947 at **162** at `open-sea` -- and the milder 512 is no better, 20,332 at 109. **17x the pixels at 100x the amplitude, for less of the saving**, since 48 -> 16 is worth 1.707 ms against the reach's 1.476. The reach is filed in [errors](errors.md) with its number and its retry condition. **Why the truncation is nearly invisible is batch 37 and not this batch**, which is the transferable half: `shade_hit` passes the *same* budget to `terrain_shade_beyond`, so the light envelope resumes exactly where the march stops and the two partition the ray -- a shorter budget moves the handoff rather than deleting a shadow. **And the fixture could say so rather than assume it**, by putting the envelope's own control on *both* sides of the sweep: with `--no-distant-shadows` everywhere, the 16-to-48 band still moves only **1,084 pixels at `shore`** while the 0-to-16 band moves **369,628**. So the near march is where the shadow is and the far march was doing almost nothing, with or without a backstop -- which is why the batch stopped at 16 and left the other 2.393 ms of the curve alone rather than taking the whole 4.1. **What 16 keeps is a margin for the occluders the envelope structurally cannot hold**: it is `WorldGen::height`, so it knows the terrain and knows nothing about a tree, a wall or anything the edit journal placed, and a 16-block exact march still catches a tree-height occluder at a mid sun. No vantage in the fixture holds a tree shadowing water, which is a reason to keep the margin and not evidence there is nothing to keep it for. **The control is the first here to carry a number rather than a switch**: `SPEC_WATER_SHADOW_DIST` is an `f32` override that *is* the distance, because a bool would have had to `select` between two literals on the path of the call -- batch 38's measured way to lose a control's bit-exactness -- and because a distance cannot be carried in one bit of `frame.flags`, which makes this the one flag in `SPEC_MASK` the shader never reads. **The saving is neither occupancy nor code size**, the two mechanisms `gpu.md` has had names for since batch 13: `shaderstats` reads **28,151 instructions, 615,424 bytes and 96 registers on both sides**, identical, because the change is one immediate operand and what goes away is loop iterations that do not run | **15 vantages, 0 pixels differing, identical file hashes** under `--no-water-shadow-cut` against `voxelcraft-pre41.exe` -- a pure revert, and the `f32` override folds to exactly the literal it replaced, which a third sweep pins directly: the shipping build is **byte-identical at all fifteen** to the one-constant diagnostic built with `48.0` edited to `16.0`. The positive half is **5 of 15 vantages**, worst **1,309 pixels of 921,600 at max delta 1** (`open-sea`), 247 at max delta 10 (`default`), and **0 pixels at the other ten**. **Ranked the way the briefing says a look change has to be ranked** -- two converged `--reference` renders over an 8x8 jitter grid -- the two builds differ by an MAE of **0.0013 at `default` against that reference's own half-split noise floor of 0.7472**, 575x under it, 90x under at `open-sea`'s water crop, and exactly **0.0000 on the dry `slope` crop**; `speckle`, `coherence`, `rg_std` and `rg_speckle` agree to four decimals on both frames. The project's own ground truth cannot resolve the change. Cost ****-1.672 ms +/- 0.007 of `resolve` at `coastline`** (t = -223.00, 0/8 rounds positive), **-0.531 at `default`** and -0.465 at `shore`, with every other pass flat to +/-0.012 -- a 10.5% faster frame at the coastline and 4.4% at the default camera. Batch 40 read the same step at -1.707 +/- 0.023 from the other direction, two independent pairings 2% apart**. `harness implies` **26 -> 28 claims**, and the first run of it was a false pass worth a line in [pitfalls](pitfalls.md) -- `harness-base.exe` carries the `IMPLIES` table it was *built* with, so a copy taken at the top of the session reported "26 claims, every one as the control table says" without having looked at either new claim. The two are a pair: that the control is wired up at all, which is the only thing a control with no composition to check can say, and that `--no-water-refract` clears it, because the refracted ray is the budget's only reader. `capture` and `validate` clean at both ends; `clippy` silent; three new tests, **all three negative-tested** against a broken copy of what each claims | [water](water.md) · [errors](errors.md) |
| 42 | **two candidates measured, both killed, and nothing shipped on purpose**: P2 said to measure first and choose from what the measurement finds, and what it found was that its own front candidate was worthless and that roadmap 38e's stated mechanism is wrong. **The first candidate was batch 41's finding pointed at the whole frame.** `frame.shadow_dist` is 220 blocks and batch 37's envelope already covers everything past it, so the same truncation should pay -- and on *every lit pixel* rather than only on sea. The march is real money: deleting it is **-1.168 ms +/- 0.073 of `resolve` at `default`**, 16% of the pass, and -0.643 at `low-sun`. Truncating its *reach* to 64 blocks is **-0.040 and -0.042**. **97% of the cost is inside the first 64 blocks**, because a shadow ray on land either hits terrain at once or escapes to open sky at once, where water's grinds for hundreds of blocks -- water is *transparent to a shadow ray* and that is the whole of why batch 41's curve was shaped the other way. **The second candidate was 38e's own first fix, and it worked exactly as specified and bought nothing.** `shade_glass` called `shade_water` itself and the driver inlined a second whole copy of that tree; the pane's transmitted leg now substitutes into the primary dispatch instead, so the module holds one call site. `resolve` went **615,424 -> 343,552 bytes with `SPEC_GLASS=1`, 271,872 bytes and 85% of everything glass adds** -- and the frame came back **+0.092 ms +/- 0.002 slower** at `coastline`, 8/8 rounds positive, **bit-exact at all fifteen vantages**. So the same picture, drawn by two thirds less machine code, half a per cent slower, and not shipped. **Code size has been a hypothesis about time here since batch 12 and this is the first measurement that separates them** -- batch 12's finding was that size is what *differs* when a folded control costs something, which is still true and was never evidence that shrinking the pass makes it faster. The mechanism for the +0.09 is in the source rather than in a counter: after the merge the primary `shade_hit` call takes variables a branch may have overwritten where it used to take values the compiler could see through, so **the common path pays for the rare one being folded into it**. What the hoist could *not* move is the register count -- **96 with glass compiled in against 80 without** -- which is now the only thing separating the two builds and is what 38e carries forward. **The session also answered the question the user actually asked alongside it**: a time-of-day x fog-density sweep at dawn, 54 renders over three cameras, judged from contact sheets and full-resolution crops rather than from metrics -- `--time 0.26` with `fog_density` at its shipped **5.0e-4** is the best-looking dawn in the set, which confirms [sky](sky.md)'s own tuning at a time of day it had never been judged at. The one thing the pictures argue with is the *default start time* of 0.30, which is a bright morning rather than a dawn; that is a preference and was left alone | **no control, because nothing shipped**: `src/` is byte-identical to `278e7e0` apart from two rewritten assertions in `tests/glass.rs`, and the renderer, the shaders and every constant are untouched. The hoist's own sweep is the measurement rather than a verification -- `bitexact --exe-b voxelcraft-pre42.exe` read **15 vantages, 0 pixels differing, identical file hashes** against the restructure before it was reverted, which is what makes "the same picture, less code, slower" a statement about time alone. `capture` and `validate` clean at the start; `clippy` silent; suite green | [gpu](gpu.md) · [errors](errors.md) |
| 43 | **the leaf fill, swept at last, and the crop had to be built before the constant could be judged**: batch 38 carved leaf blocks into a 4^3 mask and left `LEAF_FILL` at **0.72** -- the one *look* constant in that batch, authored against the research's 0.45 on an argument (our leaf blocks are a solid 5x5 stamp where the paper's canopies were already sparse) and never ranked. Roadmap 38d had said for five batches what the sweep needed -- "`--reference` is the tool and a canopy against open sky is the crop" -- and **the fixture had no such crop**, which is why the entry kept being deferred: every one of the fifteen vantages shows leaves against ground or against other leaves, where a carve is a speckle in a green mass rather than a broken outline. So the batch built the crop first: **the `canopy` vantage**, a wooded crest at eye level pitched just above the horizon, lit from the front at `--time 0.35`, now the sixteenth. **Seven one-constant builds, and the finding is that the two halves of the frame want opposite directions.** *Against sky* the silhouette wants the fill **low**: at 0.82 and 0.90 a crest is the same blocky edge a solid cube gives, so the whole of batch 38 is invisible exactly where a tree is most visible, and the breakup only becomes legible below about 0.65 -- which puts the shipped 0.72 at the edge of the range where the feature reads at all. *Against ground* a crown's interior wants it **high**: every carved cell that opens onto shadowed interior is a dark speckle from above, and 0.45 is visibly moth-eaten -- the "tree that has been shot at" the old comment predicted, now seen rather than argued. **0.62 is where they meet**, and the old number's reasoning was never wrong about the failure mode, only about where it starts, so the floor is real and near 0.5. **The control is batch 41's shape, which is now the second instance of it**: `SPEC_LEAF_FILL` is an `f32` override that *is* the fraction, because a fraction cannot be carried in one bit of `frame.flags` -- making `FLAG_LEAF_THIN` the second flag in `SPEC_MASK` the shader never reads -- and because a bool would have had to `select` between two literals on the path of `leaf_solid`, which is batch 38's own measured way to lose a control its bit-exactness. **Deliberately not nested under `FLAG_LEAF_CUTOUT`** even though a build with the cutout off never reads the fraction: nesting would make `--no-leaf-cutout` quietly a second control for this, which is the mistake `FLAG_DISTANT_SHADOWS` avoided against `FLAG_TERRAIN_SHAFTS` -- two readers of one field, one control each | **16 vantages, 0 pixels differing, identical file hashes** under `--no-leaf-thin` against `voxelcraft-pre43.exe`, and the override folds to exactly the literal it replaced: the shipping build is **byte-identical at all sixteen** to the one-constant diagnostic built with `0.72` edited to `0.62`. The positive half is **13 of 16 vantages**, worst **11,371 pixels of 921,600 at max delta 195** (`overcast`), 11,069 at 195 (`default`), 7,984 at 167 (`low-sun`), and **6,915 at 202 at the new `canopy`**; the three that do not move are `lattice`, `underwater` and `cave`, none of which has a tree on screen. **Free by construction and by the fold**: `shaderstats` reads `march` 21,504 bytes at 72 registers and `resolve` 615,424 at 96 in *both* directions, because the two builds differ in one immediate operand and in no code at all. `capture` and `validate` clean at both ends; `clippy` silent; two new tests | [foliage](foliage.md) · [harness](harness.md) |
| 44 | **the frame measured before anything was chosen, and every lever the choice turned up was dead**: P3 was deliberately unnamed -- *measure where the frame goes and choose from what it finds* -- so the first thing this session built was the measurement the entry asks for and this project had never had. **`--bench-frames` at all sixteen vantages in one sitting**, against a `PERF.md` that carried two cameras and no `shaft_scan` row at all. It says `resolve` is **66.4% of the set** with a spread of 45% to 83% behind that headline; that **`terraces` is the most expensive frame in the fixture at 12.66 ms and had never been benched**, where every cost table since batch 8 was taken at `default` or `coastline`; and that `march` is very nearly the larger pass at `underwater` (3.64 against 3.78, on **161,554 pairs**, the highest count in the set) and at `canopy`. **The decomposition of the vantage the map named** then put water's two secondary legs at **8.2 ms of a 10 ms `resolve`** -- `--no-water-reflect` **-4.178 +/- 0.034** and `--no-water-refract` **-4.031 +/- 0.010** -- with `--no-glass` recovering **0.520** of a regression 38e has now been measured at three vantages. **So the target chose itself: water's traced reflection, which has three distance constants, and all three were built and thrown away.** The **reach** (`WATER_REFLECT_DIST` 768) is real money -- -0.446 at 512, -0.746 at 384, -1.164 at 256, -1.646 at 128 of a coastal `resolve` -- and every one of those buys the same thing: the headland at the right edge of that frame stops being mirrored in the sea beneath it. **It is gone by 512**, at 5,442 pixels and max channel delta 86. The **fade** is worse: -0.553 at `coastline` for **68,661 pixels over the sixteen vantages at delta 146**, 13 of 16 moving, and **zero at `terraces`**. The **reflected hit's shadow budget** is the one that looked like batch 41's millisecond lying on the floor -- a refracted hit is handed 16 blocks and a reflected hit 220 -- and it is **free to look at and buys nothing**: 220 -> 16 is **-0.044 +/- 0.029** and *deleting the shadow outright* is **-0.019 +/- 0.056**, against a band holding 273 pixels of 14,745,600 at delta 8. **The medium is why, for the third time in three batches**: a refracted hit is under the sea and its shadow ray sets off through water, transparent to it, and grinds; a reflected hit is a hillside in air and its shadow ray stops at once, exactly as batch 42 found for the primary march on land. Batch 41's saving was never *a secondary leg's shadow ray is expensive*. **And batch 40's -1.476 ms for the reach was the reach and the fade in one build** -- measured apart they are -0.746 and -0.553 -- so `water.md`'s 22,183 pixels at delta 109, attributed there to halving the reach, is the fade's number at `lattice`, where the reach alone moves 7,615. What shipped is the measurements, at the constants' own definition sites, so the next session finds them instead of rebuilding them | **16 vantages, 0 pixels differing, identical file hashes** against `voxelcraft-pre44.exe`, which is what a comments-only change has to read. No new control and no new flag: nothing in `SPEC_MASK` moved, because a control has to buy something and the only lever here that was free to look at bought zero. The three rejections each carry their own A/B against a one-constant build rather than a flag -- six reach binaries, three shadow-budget binaries and one fade binary, all built from the untouched tree, benched paired and alternated at 8 rounds, and deleted. `capture` and `validate` clean at the start (8 checks, 11 crops, `coherence` 1.0594x against its 1.037 floor); `implies`, `bitexact`, `cargo test` and `clippy` clean at the end | [water](water.md) · [errors](errors.md) · [roadmap](roadmap.md) |
| 45 | **flat lighting for a hit nobody looks at directly, and the gate decided the sign**: batch 44 located water's cost -- at `terraces` the traced reflection is 4.178 ms, cutting its reach to 512 is bit-exact and saves 0.256, cutting its shadow budget to zero saves nothing -- so the money is the *shading* at the end of a short ray, and in `shade_hit` the shading that costs memory is the **nine-cell gather**. `resolve` calls `shade_hit` four times: the primary surface, water's refracted leg, water's reflected leg and glass's transmitted leg. The last three now take **one light lookup** at the air cell in front of the face, with no AO and no bilinear -- the cross-quad's own model, which a tuft takes because it has no face to gather across and these take because nobody can tell: a reflection is Fresnel-weighted at a grazing angle off a wave-perturbed normal, a refracted hit is multiplied by `exp(-extinction * depth)`, a transmitted one is dimmed by the pane's texel, and each transport destroys the contact cue the other eight lookups pay for. **-1.684 ms of `resolve` at `shore`, -1.584 at `terraces`, -1.186 at `coastline`, -0.726 at `lattice`, -0.682 at `glass`, -0.502 at `default`** -- every vantage benched faster, every round of every pair faster, and the largest saving since Hi-Z. **The finding is not the lighting.** The same feature was built twice and the two differ in one expression: gated the house way, `SPEC_FLAT_SECONDARY && (frame.flags & FLAG_FLAT_SECONDARY) != 0u`, it is **+1.000 at `terraces`, +1.420 at `default`, +1.465 at `lattice`** -- uniformly *slower*, a **2.58 ms swing** with the same arm taken and the same pixels out. `frame.flags` is a run-time value, so the driver cannot prove which arm `shade_hit` takes and keeps **both** in each of the three copies it inlines. **`default` is what identifies the mechanism**: a frame with water only at its right edge paid the largest regression of the six, and a cost that lands hardest where a feature does least work is code existing rather than work running -- batch 12's two mechanisms separated by one extra row of a bench already running. The fix is the override alone, letting the pipeline key be the only copy of the decision, which is what the two *numeric* overrides already do for a different reason; batch 45 is the first switch that has had to. `resolve` goes **615,424 -> 554,496 bytes**, and set beside batch 42's 271,872 bytes for +0.09 ms it says bytes removed predicts nothing in either direction. The look cost is real and is the batch's own trade: **230,613 pixels at `terraces`** and 193,680 at `shore` at max channel delta 39, 0 at `underwater`, `cave` and `night` -- shallow clear water looking down is where a submerged step visibly flattens, and a hazy or deep path is where the absorption had already taken the cue. The session also spent one free bench on batch 44's leftover question and closed it: **`--no-leaf-cutout` at `canopy` is -0.323 ms +/- 0.005 of a 3.138 ms `march`**, so the 4^3 micro-march is 10% of that pass and not the collapse it had been billed as | **16 vantages, 0 pixels differing, identical file hashes** under `--no-flat-secondary` against `voxelcraft-pre45.exe`, checked twice -- once for each gate spelling -- because the plumbing changed between them. The positive half of the pair is the shipping build against the same binary, which moves 230,613 pixels at `terraces`: a no-op claim and a not-wired-up claim are the same reading without it. Four new tests in `tests/secondary.rs`, and the load-bearing one asserts `secondary_smooth` does **not** contain `frame.flags` -- the wrong form is the form a tidy-up reaches for and nothing in any image shows it. The other three pin the primary call site's literal `true`, and that all four `shade_hit` callers go through the one gate. `capture` and `validate` clean at the start; `implies` 28 claims, `cargo test` 91 tests and `clippy` silent at the end. **A docs test failed and it was batch 44's fault**: that batch's rewrite of `PERF.md`'s frame tables dropped every `N fps` figure, which `the_readme_quotes_no_measurement_from_perf_md` scans for, and the suite had been run *before* the rewrite. Fixed here | [shading](shading.md) · [gpu](gpu.md) · [errors](errors.md) |
| 46 | **the other eight gates, folded and thrown away, and the screen that predicted it in two minutes**: batch 45 found that gating a hot inlined arm with `SPEC_X && (frame.flags & FLAG_X) != 0u` rather than the override alone was worth **2.58 ms** at `terraces`, and left the obvious generalisation -- eight more switch overrides written the same way -- as the front lead on the board. **It is not a lead.** The session screened all eight with `shaderstats` before benching anything, one fold at a time, offline and with no GPU: every one folds further, so none could be ruled out, but the total is **12,544 bytes against batch 45's 60,928** and `march` is byte-identical throughout. The benches then said what the ratio already had -- all eight together are **-0.119 ms +/- 0.029 at `terraces`** (t = -4.09, the only row of five that clears its own standard error) and -0.065, -0.060, -0.049 and -0.020 at `shore`, `default`, `coastline` and `low-sun`, every one inside its error. The two largest movers alone, 71% of the bytes, are **-0.058 at `terraces` and +0.001 at `open-sea`**, so no subset is individually significant either and there was no defensible partial ship. **The finding is the scope of batch 45 rather than a number**: the cost is the gate shape *times the arm behind it times how many copies the pass inlines*, and batch 45 was the one case with a large arm -- the nine-cell light gather -- in a function `resolve` inlines four times. These eight guard a uv permutation, a simplex tint, a shaft sample and four wave-field branches. **Not shipped**, because the `frame.flags` half is what keeps the flag word the thing that decides: a pipeline key that ever disagreed with it would still draw the picture the flag asked for, and a tenth of a millisecond at one vantage does not buy that property eight more times. Two corrections on the way past: there are **eight** such sites and not the nine the previous batch's grep reported, because two of those hits were comments -- `SPEC_FOLIAGE` was already folded, and batch 14 had found 384 bytes in doing it, which is the same effect priced in bytes and never connected to time | **Nothing shipped, so there is no control to check and that is the entry working.** The build under test was **bit-exact at all sixteen vantages with identical file hashes** against `voxelcraft-pre46.exe`, which a pure fold has to be and which is what made the whole question a pure cost measurement with no look side. `capture` and `validate` clean at the start (0 failures, `coherence` 1.0594x against its 1.037 floor); `cargo test` and `clippy` clean at the end. What landed is one comment block at the override declarations in `common.wgsl`, naming all eight sites, the numbers and the retry condition, so the next session finds the measurement instead of rebuilding it | [gpu](gpu.md) · [errors](errors.md) · [lessons](lessons.md) |
| 47 | **the smooth-lighting gather priced, a cache killed before it was built, and the first positive statement about what `resolve` is bound by**: batch 45 made secondary hits take one light lookup instead of the nine-cell gather and bought 1.5 ms, leaving the *primary* hit -- every visible pixel -- untouched. This batch asked what that one is worth and whether it could be had for free. **The ceiling, by pointing the primary at batch 45's existing flat model**: **-1.764 ms at `cave`, 72% of `resolve`**, -0.916 at `default`, -0.904 at `edits`, -0.899 at `canopy`, -0.687 at `lod`, -0.599 at `terraces` and **-0.065 at `coastline`**, where a frame of water and sky barely reaches it. `cave` is the clean reading -- no sky, no water, every pixel a near primary hit -- and it makes this the largest identifiable block of the pass on a land frame. **The free route looked excellent**: the nine samples depend on `(ci, v, normal_id)` and *not* on where in the face the pixel landed -- only the two bilinear weights are per-pixel -- so every pixel covering one block face recomputes them identically, and at the bottom of the `default` frame a block is about twenty pixels across. Four hundred pixels, one answer, cacheable per face in workgroup memory and potentially bit-exact. **One diagnostic retired it in twenty seconds.** Point all nine reads at the *same cell*: identical instructions (`resolve` moves 640 bytes), and the memory system sees one address instead of nine -- perfect sharing, with none of a cache's tag compares or barriers. It recovers **-0.191 ms at `cave`, -0.123 at `default`, -0.106 at `canopy`, -0.092 at `edits`: 10% to 13% of the ceiling**. **Collapse the memory traffic nine to one and 87-90% of the cost stays**, so the block is instruction-bound, a real cache would recover less than those rows, and it was not built. **The finding outlives the dead end**: `gpu.md` has listed *what `resolve` is bound by* as unknown since batch 13, and every result until now ruled something out -- occupancy twice, code size once. This is the first that rules something *in*, and it is consistent with all of them, because occupancy buys latency hiding and there is little latency here to hide. The technique is the reusable half: **to ask whether a block is memory-bound, build the variant that changes only its memory behaviour and holds its instructions fixed.** Where the instructions are, for whoever returns: eighteen `light_curve` evaluations -- three multiplies and an add each, so a 16-entry table for the 4-bit levels buys nothing -- four corner reciprocals, the Minecraft AO rule and three bilinear mixes, with no hotspot among them. Making it cheaper means doing less of the arithmetic, which means changing the look, which is where batch 45 deliberately stopped | **Nothing shipped, so there is no control to check.** Both diagnostics were one-constant builds from the untouched tree, benched paired and alternated at 8 rounds against `voxelcraft-pre47.exe`, and deleted. `capture` and `validate` clean at the start; `cargo test` and `clippy` clean at the end, and the working tree is byte-identical to its parent apart from one comment block. What landed is that comment, at the gather itself, carrying the ceiling, the sharing number and the reason a cache is not the answer -- so the next session to look at the largest block in `resolve` finds the measurement instead of the idea | [gpu](gpu.md) · [errors](errors.md) · [lessons](lessons.md) |
| 48 | **the first batch aimed at `march`, and the roadmap's own design for it was wrong in a way the constant's comment already said**: P4 asked to clamp `frame.far` when the camera is submerged -- "a uniform change, no shader edit" -- because *absorption kills the image within tens of blocks*. `WATER_EXT` is `(0.280, 0.055, 0.020)` per block and its comment reads **red is gone within a few blocks and blue survives tens**; "tens of blocks" is red's number, and blue still transmits **7.7% at 128 blocks**. **The deeper error is that a ray's water path is not its length.** `water_path` is analytic against the sea plane, so a ray leaves the medium at `(sea_level - cam.y) / rd.y` and is unattenuated every block after -- at the fixture's own submerged eye, three blocks down and pitched -14 degrees with a 70-degree field of view, the frame reaches **+21 degrees**, where a ray is out of the water within **nine blocks** and can see a headland two kilometres off at full strength. A uniform clamp deletes that, for the top third of the frame. **What shipped is per tile and tests the water path**: `tile_select` takes the most upward of the four corner rays it already builds, and if even that one is still submerged at `WATER_FAR_DIST` the whole tile stops there instead of at 2600 blocks. `frame.far` is untouched, which also leaves `depth_key`'s quantisation and the Hi-Z scale alone -- the uniform version would have shifted both at the instant the camera crossed the surface, with the *previous* frame's Hi-Z written in the old scale. **-0.291 ms of an 8.4 ms frame at `underwater`, bit-exact at all sixteen vantages**, and the uniform ceiling it replaces is -1.371 for one pixel at delta 8. **The flag is the first control bit deliberately *outside* `SPEC_MASK`**, and for a structural reason rather than a judgement: the feature is ANDed with `FLAG_UNDERWATER`, which is a per-frame fact about where the player is standing and can never fold at pipeline-build time -- and `SpecPipes` holds `march` and `resolve` and nothing else, so `tile_select` could not have taken an override anyway. `const _: () = assert!(SPEC_MASK & FLAG_WATER_FAR == 0)` is the inverse of every assert around it. **The finding worth more than the milliseconds is that the sweep meant to author the constant was measuring nothing.** Every reach from 32 blocks up is bit-exact at `underwater`, and so is every extinction down to `--water-absorb 0.05`, where blue still transmits **88%** at 128 blocks -- water barely absorbing at all and not one pixel moves. The probe that explains it is a reach of **32**, which moves **one pixel at max delta 4**: the vantage's submerged content ends within 32 blocks, so there is nothing out there to cut at any reach above it. **The zero-pixel readings are the fixture being blind, not the constant having margin**, which is `harness.md`'s own warning about a known-positive read off the wrong frame. So **256 ships and the four-times-faster 128 does not** -- -1.135 against -0.291 -- authored on blue's extinction putting transmittance under 1% rather than on a capture, because the argument that would justify 128 (the sky-light flood loses a level per block, so anything deep is dark already) **fails for the case this is aimed at**: a horizontal ray at three blocks' depth stays near the surface the whole way, where the flood has darkened nothing. That is batch 38d's position exactly, and it wants 38d's answer -- a crop, worth **0.844 ms** | **16 vantages, 0 pixels differing, identical file hashes** under `--no-water-far` against `voxelcraft-pre48.exe`, and **the shipping build reads the same 16 zeros** -- which is a no-op reading, so the positive half is the bench rather than a picture: **-0.291 ms total and -0.214 of `resolve` at `underwater`, 8/8 rounds, t = +16.58 and +28.33**. A feature that never fired cannot do that, and without the pair the two claims read identically. The control is free in both directions (`tile_select` 0.150 against 0.160). Five new tests in `tests/submerged.rs`; the two load-bearing ones assert the gate tests **both** flags -- `FLAG_WATER_FAR` alone would clamp a *dry* camera to 256 blocks, and nothing at pipeline-build time could catch it -- and that the cull reads the per-tile reach, the failure that leaves every other check passing while the feature does nothing. A third pins the floor under the constant so 128 cannot be taken without the crop. `capture` and `validate` clean at the start; `implies`, `cargo test` and `clippy` clean at the end. **One number is unexplained and parked rather than chased**: `resolve` moved **-0.755 ms at a 128-block reach** on a build that touches only `tile_select`'s cull, with the frame bit-identical and the same file hash. `frame.far`'s other readers, pixels changing and `MAX_PAIRS` truncation are each ruled out by measurement; a warmer visibility buffer and a GPU power-state effect are not. Roadmap P3b, at the front of the queue | [water](water.md) · [errors](errors.md) · [gpu](gpu.md) |
| 49 | **the ghost march: `march` is billed for the work it does and `resolve` for the writes it issues**: batch 48 changed one expression in `tile_select` and `resolve` came back **-0.755 ms at `underwater` on a bit-identical frame with the same file hash**, decoding the same visibility buffer and shading the same pixels. It was parked at the front of the queue rather than chased, and the reason it was worth a whole session is the branch it left open: **if `march` doing less work raised the clock `resolve` runs at, the per-pass split of every paired bench in this project would be soft** -- batch 45's 2.58 ms gate swing and batch 47's whole gather table are arguments about *which pass* moved. **It is neither candidate the roadmap named.** Batch 48's change moves two quantities at once -- the pairs `march` is handed and the `atomicMax` writes it issues -- so the discriminator is a third build that moves one: **the ghost build emits the culled pair *tagged* in bit 31 of the pair word and skips only the write**, marching pre-48's set with batch 48's write traffic. At `underwater` with hi-z off, where the pair counts confirm each row varies one thing: **suppressing 150,372 pairs' writes on the same 372,335 pairs is `march` -0.077 and `resolve` -0.770**; **cutting the work from 372,335 to 221,963 pairs with the writes already gone is `march` -2.380 and `resolve` -0.002** (t = -0.39, 2/8 rounds). It closes on every pass in both hi-z blocks -- hi-z on, `march` +0.312 - 0.653 = -0.341 against -0.328 measured, `resolve` -0.756 + 0.009 = -0.747 against -0.740, total -1.128 against -1.109. **`march` doing 2.380 ms less work moves `resolve` by two microseconds**, so the split is sound for work and instructions and batch 45's and 47's results stand as written. **What it does not survive is fire-and-forget writes: 91% of what suppressing them saves is billed to `resolve` and 9% to the pass that issued them.** Two candidates ruled out by measurement rather than argument. **A power or clock state**, twice: the work row above, and -- cheaper, and the one worth copying -- in the writes row `resolve` moves **-20.5%** while `select` reads 0.155 against 0.155 and `taa` 0.502 against 0.501, because **a clock change is global and the short passes are already in every bench table**. **A warmer visibility buffer**, which was the roadmap's own lead: with hi-z on, `build_hiz` streams the entire 16.6 MB buffer between the two passes in 0.110 ms identical on both sides -- **151 GB/s, a read from memory and not from cache** -- and `resolve` still moves -0.756, so nothing `march` left resident survives to be read. **The decisive reading is a same-binary A/B and that is correct here**, against `errors.md`'s standing rejection of one: that rule is about pricing a feature by its absence from a binary, and here both sides are one executable running one shader over the same 372,335 pairs, differing in the run-time value of one bit -- a revert build would have added a confound rather than removed one. **Two cautions the next build of this shape needs**, both found rather than assumed: hi-z on is *not* one-variable for a write change, because suppressing a write changes what `build_hiz` finds and therefore how the next frame routes pairs (the ghost build marches 183,808 where its own control marches 161,554); and the gate has to be checked against the driver sinking `march_chunk` into it, which the `march` column does for free at -0.077 of 7.436. **What it leaves is roadmap P3c** -- `march` issues `atomicMax` on every hit and discards the result, so a chunk behind a nearer one pays a read-modify-write to change nothing, and the general form of the ghost gate is the test every depth-buffered renderer has: `if key > atomicLoad(&vis[pix])` before the real atomic, correct without a re-check because a racing write can only raise the value. **-0.850 ms at `underwater` hi-z off is what the ghost build already measured for it**, bit-exact by construction | **Nothing shipped in `src/`, so there is no control to check and the working tree is byte-identical to its parent.** `bitexact` against `voxelcraft-pre49.exe` is 16 vantages, 0 pixels, identical hashes, which is what a documentation-only batch has to be. The evidence is the three diagnostic builds, each deleted at the end, and **the ghost build's validity is a pair rather than a single reading** because a no-op claim cannot be told from a not-wired-up one: the negative is 0 pixels differing and identical file hashes against the cut build at `underwater`, `coastline`, `terraces`, `shore` and `default` -- which alone proves nothing, since the suppressed writes are all writes that lose -- and the positive is the pair counts, 372,335 marched against the cut build's 221,963, taken at the bench's own 140 frames because the fixture yaws 1.4 radians over a run and a 25-frame snapshot reads one camera. `capture` and `validate` clean at the start (19 rows, `coherence` 1.0594x against its 1.037 floor); `implies`, `cargo test` and `clippy` clean at the end | [gpu](gpu.md) · [errors](errors.md) |
| 50 | **read before you write, and Hi-Z had already taken it**: batch 49 filed P3c as the best-shaped entry on the board -- `march` issues `atomicMax(&vis[pix], key)` on every hit and discards the result, so a chunk behind a nearer one pays a read-modify-write to change nothing, and batch 49 had measured 0.850 ms of frame riding on such writes with **91% of it billed to `resolve`**. The skip is one `atomicLoad` and a compare, and it is **bit-exact by an argument rather than by a capture**: `atomicMax` only ever raises the word, so a key that loses the load would have lost the max, including against a write racing in between, which can only raise it further. **It buys nothing.** Against `voxelcraft-pre50.exe` -- a real revert, not a flag -- `resolve` is **-0.029 +/- 0.021 at `underwater`** (t = -1.35, the vantage the screen said should suit it best at 4.93 chunk tests per pixel) and **exactly +0.000 at `default`** on six consecutive rounds. **A null needs a mechanism, and a *count* got one where a timing could not.** A diagnostic build tallied how many of `march`'s hits carry a key that cannot win: hi-z off against hi-z on, **58.0% -> 12.8% at `underwater`, 72.5% -> 21.5% at `default`, 69.8% -> 0.0% at `cave`** -- **Hi-Z removing 94.7%, 94.0% and 100% of the losers**. An occlusion cull removes precisely the chunks whose writes would lose, so the pretest pays a load on **every** hit to skip an atomic on one in five to one in eight, and at `cave` on none at all. `cave` is the structural case and the cleanest number in the batch: **2,073,600 hits for a 1920x1080 frame, exactly one per pixel**, because every tile marches only the camera's own chunk -- the one `tile_select` exempts from the box test. **The fact worth keeping is about Hi-Z rather than about the pretest**: it is a *write* cull as well as a march cull, and since batch 49 priced such a write at 91% to `resolve`, part of what Hi-Z has bought since it shipped was never `march`'s time at all. **Where the previous batch was wrong, stated rather than quietly dropped**: batch 49's ghost suppressed a *superset* -- writes that lose, and writes that **win** at a pixel nothing nearer reached, which is why it rerouted the next frame's pairs -- and its entry noted the superset and then also argued the figure was "pessimistic" for a shipping version. True of the Hi-Z feedback, false overall, and the superset is much the larger effect. **A measurement of a superset is an upper bound on the subset and nothing more.** The gate spelling was screened offline before anything was benched, batch 46's way: the house `SPEC_X && (frame.flags & FLAG_X) != 0u` form costs **256 bytes** against the override alone (21,888 vs 21,632) with registers unchanged at 72 and both folding to an identical 21,504 when off, so the house form was kept on a measurement rather than by default | **Not shipped**, so there is no control to check, and the working tree is byte-identical to its parent apart from one comment block -- `bitexact` against `voxelcraft-pre50.exe` is 16 vantages, 0 pixels, identical hashes. While the feature was in the tree it read **the same 16 zeros in *both* directions**, which is the point rather than an aside: a capture cannot tell a correct pretest from an inverted one from one never reached, because all three draw the identical picture, so the five guards in `tests/pretest.rs` were source-level and the positive half was the bench alone. Those tests went with the feature. **The bound is looser than this project's benches usually give, and the batch says so**: the standard error is **0.021 against the 0.004-0.008 the same vantage gave two hours earlier**, so what is established is *no effect larger than about 0.06 ms*. The cause was heat soak -- `resolve`'s A side climbed **3.51 -> 4.18 over seven rounds, +19%**, ten times the fixture's documented 2% ramp, with the user's monitor reading **CPU 94C, GPU 80C** -- caught from outside the output, because nothing in a paired bench announces it. Pairing and alternation still cancel it; what is lost is resolution. New row in [`pitfalls.md`](pitfalls.md), and the `default` rows are the trustworthy ones because equilibrium is a level rather than a ramp. `capture` and `validate` clean at the start; `cargo test` and `clippy` clean at the end | [gpu](gpu.md) · [errors](errors.md) · [pitfalls](pitfalls.md) |
| 51 | **the gather's early-out, and the condition that decides it**: roadmap P5 ranked itself the best-shaped entry on the board -- the nine-cell gather is 72% of `resolve` at `cave`, the `cave` frame is very nearly black, and a face lit at level 1 pays exactly what a face in full sun pays. The premise held up exactly: the gather's whole output reaches the pixel through `(ambient_rgb + sun_rgb) * ao`, every term is proportional to the light just gathered, and `ao` multiplies that product and nothing else because the `frame.ambient` floor sits outside it -- so **on an unlit face the nine lookups are already multiplied by zero** and removing them needs no look constant authored. **What could not be found is a cheap test for which faces those are, and the two candidates fail in opposite directions.** Thresholding the light at the face's own air cell is **-1.579 ms of a 2.284 ms `resolve` at `cave`, 8/8 rounds, t = +272** -- 89% of batch 47's ceiling, the whole frame 3.364 -> 1.787 ms -- and moves **10,150 pixels at max delta 66 at `overcast`**, which is the same build the millisecond was read off. **No threshold rescues it, and that is a measurement rather than a judgement**: 0.0117 and 0.00139, two levels apart on the flood's own scale, produced *identical file hashes at all sixteen vantages*, so not one of those pixels was a face at a middling light level. The mechanism is exact -- `light.rs` decrements one per step over the six face neighbours, so it bounds every cell it can *reach* and says nothing about a diagonal whose two in-plane connectors are solid, where the light arrived from in front of the face. **That is an inside corner, voxel terrain is made of them, and it is the corner AO shuts hardest** -- the cut damages exactly what roadmap A7 was declined for losing. Testing the *chunk* instead -- `light_flags` bit 0 plus a span test, which together say the nine cells **are** one byte -- is exact and **-0.017 ms over six rounds, sign mixed**, because a chunk is 64 blocks tall so the one holding a cave holds the lit surface above it. A chunk-boundary guard was suspected of the pixels first and priced at **132 of them** (10,150 -> 10,018), which is how `light::flood` being chunk-local -- on record in one doc comment and in no document, test or vantage -- became roadmap P5b | **Not shipped**, so there is no control to check, and `src/` is byte-identical to its parent. While the feature was in the tree `--no-dim-cut` was a bit in `SPEC_MASK` and read **16 vantages, 0 pixels, identical hashes** against `voxelcraft-pre51.exe` in *both* builds -- and the exact build read the same 16 zeros with the feature *on*, which is the point rather than an aside: **bit-exact at sixteen vantages was the feature doing nothing**, and only the bench could tell a correct early-out from one that never fires. `lessons.md`'s *a no-op claim cannot be told apart from a not-wired-up claim*, met in the wild. Seven source-level guards went with the feature. Neither the register count (**96 throughout**) nor the size predicted anything -- the *fast* build was 10,624 bytes **larger** than the parent | [gpu](gpu.md) · [errors](errors.md) · [roadmap P5](roadmap.md) |
| 52 | **the crop that could rank the reach, and the first distance cut bought at a visible price**: roadmap P4's third sub-lead had been sitting since batch 48 in batch 38d's exact shape -- a constant nothing in the fixture could see. `WATER_FAR_DIST` was authored at **256** off blue's extinction, because `underwater`'s submerged content ends within 32 blocks and every reach above that read zero pixels at every extinction down to `--water-absorb 0.05`. **Those zeros were the camera.** This batch built the one P4 specified: `sea-horizon`, the seventeenth vantage, a submerged eye along the horizontal at a chain of wooded islands out to 264 blocks -- default seed, `--time 0.35 --cam-submerge 3 --cam-yaw 215 --cam-pitch 0`, every argument predating the batch so it is a `bitexact` row from day one. **`--cam-pitch 0` is the load-bearing argument and the reason the obvious camera fails**: the clamp fires only for a tile whose most *upward* corner ray is still submerged at the reach, which three blocks down is `atan(3/256)` = 0.67 degrees, so the clamped region is everything at or below the horizon and nothing above it -- above y = 0.43 of this frame the reach moves **zero** pixels at any value, and a vantage at the default -14 degree pitch is `underwater` again. The constant became rankable in one command, against an **unlimited** reach: **0 pixels at 264 and up, 316 at 256, 1,640 at 224, 24,179 at 192, 67,621 at 128, 175,890 at 64**, with the milliseconds paired 8 rounds against the 256 build at **-0.168, -0.604 and -1.930**. **The two crops are the finding and neither is the count**: of 128's 67,621 pixels, 62,589 are open water in `deep` moving by at most **2**, and the damage is the other 5,032 in `horizon` at max delta **23** -- a pale seam where a distant shoreline's *underwater* silhouette is deleted and the brighter haze behind it shows through. **Blue's extinction was right and batch 48 was right to trust it**: the frame goes exact at 264 against a derived `ln(255)/0.020` = 277, 5% apart, so the authored 256 sat 8 blocks under exact. **128 ships anyway, decided by the user against the picture**, because 1.930 ms is 18% of a 10.6 ms frame -- **the first truncation in this engine bought at a visible price rather than out of an empty band**, against batch 41's canonical 1.672 ms for 1,309 pixels at max delta 1. **The rule the blind sweep had inverted is worth more than the milliseconds**: `harness.md` wrote the crop up as the thing that would *stop* the aggressive value, and it licensed it -- **a blind vantage is not a conservative one**, it is one that cannot argue in either direction, and the crop bought the right to decide rather than caution. Two numbers moved that nobody was looking at: **the control's own cost tripled when the constant did** -- `--no-water-far` is -2.776 ms at `sea-horizon` and -1.125 at `underwater` at the shipped 128, against -0.835 and -0.291 at 256 -- and **batch 49's split held on a fresh vantage**, -1.880 of that 2.776 landing in `resolve` against -0.859 in `march`. `march` is also outright the larger pass here for the first time in the set, 4.15 against 3.74 | **17 vantages, 0 pixels differing, identical file hashes with the renderer untouched** -- run before the constant moved, which is what proves the new vantage is inert on both binaries and a valid `bitexact` row rather than one that needed batch 38b's `--load-edits` substitution. Then **16 of 17 against `voxelcraft-pre52.exe` with 128 in**, `sea-horizon` alone at **67,305 pixels, max delta 23** -- the 316 difference from the 67,621 above is exactly what 256 was itself throwing away, and the two numbers answer different questions. A **`CHECKS` row is the durable half**: `--no-water-far` must move at least 150 pixels at `sea-horizon`, and **it is the only row in that table that polices a vantage rather than a metric**, because every other kind of check this fixture has would have passed throughout batch 48. Its floor is fitted to the **weakest defensible reach** (316 at 256) and deliberately not to the shipped one (67,621 at 128), which would have made it an assertion about `WATER_FAR_DIST` that fails the day anybody legitimately puts the reach back up. `tests/submerged.rs` gains a second guard for the same seam -- the floor is now founded on a *vantage*, so the vantage's submergence and pitch are asserted, since deleting or re-aiming it would leave a number with no argument under it, still passing. The floor itself went **200 to 128**, a loosening, and it is on the record as one: 200 was never measured, and the capture disagrees with it in both directions at once -- exact at 264, *above* it, and shipped at 128, *below* -- so it was replaced rather than relaxed. `capture`, `validate`, `implies`, `cargo test` and `clippy` clean. **One thing was not measured and is on file as such**: the seam sits at a fixed distance from the *camera*, so it slides across the water as the player swims rather than staying with the shoreline, and no still frame can price that -- if it reads badly in play, 224 costs 1,640 pixels at max delta 3 and 192 keeps a third of the money | [water](water.md) · [harness](harness.md) · [errors](errors.md) |
| 53 | **the whole of P4: a mask the marcher should have been walking, a skip one level up, and the reach the user asked for** -- the entry's three sub-leads in one batch, on the user's instruction, plus the instrument that made all three rankable. **That instrument is the half to read first.** `march` accumulates its loop counter into `dbg` under `FLAG_HEATMAP` and has since the heatmap existed; nothing on the CPU had ever read it. `--march-stats` adds the reader and `harness march` the sweep, and what they produce is **an integer** -- so two runs an hour apart under any thermal state agree to the unit, which `PERF.md`'s three caveats say of nothing else here. Three candidates were priced as *deliberately incorrect* ceilings in four minutes with no GPU timing at all: a uniform-water leaf skipping 16 instead of 4 is **-68% of `deep-water`'s steps and -57% of `sea-horizon`'s**, an empty root cell skipping 64 instead of 16 is **-62% of `default`'s**, and the coalesced sub-leaf test skipping 4 instead of 2 is **-10%**. The third was dropped on that number alone. **Then the exact halves of the first two.** The root mask is a `vec2<u32>` indexed by the same `cell_bit` a leaf is, so the coalesced 2x2x2 test the leaf level has run since the tree was written **lifts to the root verbatim** -- four lines, an empty 32^3 in one step instead of two to eight, **-4.90% of DDA steps over the eighteen vantages**, -15.58% at `low-sun`, -12.15% at `default`, and zero at both water cameras. **The two together take the set from 196,470,005 steps to 176,826,842, -10.0%.** **In milliseconds, on an idle machine against a null pair reading +0.002 +/- 0.007: -1.903 ms of a 9.67 ms frame at `deep-water` for the whole batch, -0.540 at `sea-horizon`, -0.497 at `coastline` and -0.243 at `default`.** The `coastline` row is the one nobody predicted -- a camera 24 blocks *above* the sea, where `march` gains nothing and is 0.012 ms slower, and `resolve` is half a millisecond faster, because **`resolve` calls `march_chunk` itself** through three legs that all ignore water. A traversal change here has two customers and only one of them is the pass it lives in. The larger one is `dry_mask`: **water is in the occupancy tree**, which `water.md` calls the load-bearing choice and which makes an ocean interior read as solid at every level to the submerged primary ray and to **every** secondary ray in the frame. One 64-bit mask per chunk -- `root_mask` with the water-only 16^3 cells cleared, built in `build_local`'s existing loop, `GpuChunk` 80 bytes to 96 -- and those rays traverse the world they can actually hit: **-25.17% at `deep-water` and -16.64% at `sea-horizon`** -- the root skip is worth 0.00% and -0.04% at those two, so both figures are the mask's alone -- with the pair counts identical, and `resolve` picking up the same code for its three water-ignoring legs. Both are **bit-exact, and the pairing is the point**: a pure traversal change is exactly what `bitexact` cannot tell from a feature that never fires, so every zero here is quoted beside a step count that moved. The third sub-lead is the reach, and it is the user's own report -- *"the only time it looks odd from inside is when I go too deep"*. `WATER_DARK_DEPTH` is **derived rather than authored**: `light.rs` takes one sky level off per block of water and `flood` decrements one per step in every direction, so a cell 15 blocks down holds sky level exactly 0 however the light got there. A tile under that depth for the whole of `[0, WATER_FAR_DIST]` has darkness and absorption covering the interval with no gap between them, and stops at `WATER_DARK_DIST` = **64, which is the LOD 0 chunk edge and where the ladder stops moving** -- 48, 32, 24 and 16 render one image with one sha256, because the cull is per chunk AABB. **19,576 pixels of 921,600 at max delta 3** at the new `deep-water`, against batch 52's 67,621 at 23. **The first cut of it tested one end of the interval and the fixture caught it**: a descending ray is shallowest at the eye and a rising one at the far end, and the far-end-only version culled lit water for **98,323 pixels at max delta 9 at `sea-horizon`** -- a camera three blocks down where nothing is dark. Anchored at the eye instead, seventeen of the eighteen vantages are bit-identical to the parent and batch 52's decision is untouched. **And P4's third item was mis-stated in two documents.** `MAX_PAIRS` truncation is not "reachable at 8K": measured at `default` it is **1,529,818 pairs against 1,048,576 at 5120x2880**, 46% over and truncating, and **80.5% of the cap at plain 3840x2160** -- one `--scale 2` from losing geometry on a 4K capture. The list is screen-sized, so it is now sized from the screen like `vis`, `hiz` and `dbg`, with `MAX_PAIRS` kept as the **floor** so 1280x720 and 1920x1080 allocate exactly what every number in this ledger was taken at. Behind it sat a narrower limit nobody had guarded in thirty-one batches: the pair word spends 19 bits on the tile index, 7680x4320 is **98.9%** of it, and past that a tile marches another tile's chunks and draws a frame that is lit, shaded, stable and wrong. `warn_if_oversized` is the guard | `--no-water-dark`, and `harness march --exe-b` for the two traversal halves, which have no picture to revert | [water](water.md), [gpu](gpu.md), [harness](harness.md) |
| 54 | **the surface from below: Snell's window, total internal reflection, and a ramp the user authored** -- the first batch in this project whose subject came from **playing the game**. Thirty-two blocks down, a mountain still drawn above the horizon: *"i dont think it should be"*. They were right, and the first thing worth recording is what the answer was **not**. No reach constant could have removed it: a `tile_select` clamp fires only when every ray in a tile is still submerged at the clamp distance, and a ray aimed at a peak that breaks the surface leaves the water -- the escape boundary is `atan(depth / WATER_FAR_DIST)`, **14.0 degrees at that depth**, above which clamping would delete the **sky**. Batch 53 made deep water cheaper and could not make it look right. **What was missing was the boundary itself.** `FLAG_UNDERWATER` makes water non-blocking for the primary march, so a submerged ray crossed the surface as though it were not there -- a miss went to `sky_color`, a hit above the waterline was shaded in full, and the only mention of Snell's window in the tree was `water.md` recording that there was not one. `surface_from_below` puts it back: the wave normal at the crossing, Schlick against the **transmitted** angle, and a traced mirror into the water. **Writing Fresnel against the transmitted angle is what makes one expression do both jobs** -- `sin2_t >= 1` *is* the total-internal-reflection test and the same quantity gives the curve inside the window -- so there is **no angle constant anywhere**, and a test refuses `48.6`, `41.4` and their cosines by name because `refract` already reads `WATER_IOR` and two copies could drift. **Then the user judged the build and returned two corrections, and they turned out to be one ramp.** *"The first 3 blocks of water should not have a mirror"* and *"midway to the bottom you dont really see the surface light that much if at all"* are the two ends of `smoothstep(SNELL_MIN_DEPTH, WATER_DARK_DEPTH, depth)` over the **eye's** depth: zero in the top three blocks, where the frame is **bit-identical** rather than merely close, and one where the sky flood has already reached zero. The far end is *derived* -- it is batch 53's `light::MAX_LEVEL`, so the two halves of the engine agree about where deep water starts -- and the near end is *authored*, with the user's sentence at the constant. **The fade stops at `SNELL_DIM_MAX` = 0.9 and the reason is a limitation elsewhere**: fading fully to `underwater_body()` makes a frame 24 blocks down uniformly near-black, because that colour bottoms out at `frame.ambient` once the flood is zero -- **the engine's deep water is far darker than clear water is, and that is the 4-bit light nibble showing through**. If roadmap L1 ever gives deep water real in-scattered light, the cap is the first thing that should go back up. **The first build traced a ray into the water for the mirror and cost 6.49 ms of an 8.75 ms frame** -- `resolve` at `deep-water` went 3.54 to 10.03, more than three times what batch 53 had *bought* there. Split with three diagnostic builds: the wave normal **1.12 ms**, the trace **2.50**, shading what it found **2.87**. Then the measurement that settled it -- against a mirror that is simply `underwater_body()`, the traced one differs by a max channel delta of **3, 3 and 7** at three pitches. **It is invisible, and the reason is the reason it should not have been built**: a mirrored ray at these angles travels a long way through water and `water_medium` converges it to the body colour on the way back, so the free answer *is the limit the expensive one was walking towards*. Dropping it took the feature to **+0.262 ms**, a 28x cut, with the picture unchanged to a delta of 7 -- and took three invariant tests that had been re-argued for it (`trace_world`'s caller census, `shade_hit`'s light-model census, `SPEC_WATER_SHADOW_DIST`'s uniqueness) straight back to their original claims. What is left undone is the *compression* inside the window, and it is architectural rather than a scope cut -- the transmitted ray should bend, and `march` has already traced a straight line into the visibility buffer, so `resolve` cannot change which geometry the primary ray found. `resolve` grew **15,232 bytes and not one register**, 96 before and after -- and those bytes cost nothing measurable at the fifteen dry cameras: `coastline` +0.044 +/- 0.087, `terraces` +0.042 +/- 0.057, both inside their own error | `--no-snell`, a pure revert: **17 of 18 vantages bit-exact**, and that is a fact about the cameras -- fifteen are dry and the two submerged ones sit exactly at the ramp's zero | [water](water.md) |
The image-quality figures for batches 1-4 live in their own file and nowhere else;
everything from batch 5 on is in `PERF.md` under its own heading.
| 55 | **the probe tap: roadmap L1's read side priced before L1 has a format, and an entry of its own replaced rather than reordered** -- the second batch here whose subject came from the user, and the first whose main output is a *decision about a document*. L1's step one was *"colour the block-light channel, torches tint"*, written into the roadmap and into `CLAUDE.md`; **there are no torches.** `block::def(..).light > 0` is true of exactly one block in the table, `GLOWSTONE` at 15, so an RGB block channel would have shipped against empty content -- batch 53's lesson inverted and worse, because `--no-water-dark` was bit-exact at seventeen of eighteen vantages for want of a *camera*, which one command fixed, and this would have been bit-exact for want of a *world*, which no camera can fix. **The framing that replaced it is that `light.rs` is already a transfer function**: the sky nibble stores how much sky a cell sees and `frame.daylight` multiplies at read time, which is PRT with a one-coefficient direction-free basis. That matters because of a fact nobody had checked: **the sun moves** -- `day_length_seconds` 600, `freeze_time` false -- and `relight` fires only on an edit, so anything baking a *lit result* is invalid in the game and would look correct in every capture in the fixture, all of which pin `--time`. **The batch itself is one measurement and no feature.** `--probe-tap` takes one hardware trilinear tap into a 128^3 RGBA16F field per shaded surface and multiplies it into `amb`; the field holds 1.0, `x * 1.0` is bit-exact for every finite `x`, and the whole output is a millisecond. **It ships *off*, and that is a requirement rather than a default** -- a diagnostic left on would charge every later measurement the time it was built to measure, which is `--no-foliage`'s +1.24 to +2.07 ms with the excuse removed. **The finding is not the cost but its shape.** `resolve` moved between **-0.055 and +0.017 ms** across five vantages, mixed sign, every figure inside 0.06 of a 3.3-9.9 ms pass -- and eight more rounds would not have separated that from zero, so the batch **tripled the feature instead of the rounds**: `PROBE_TAPS` 1 to 3 is one literal, and `cave` read **-0.040 +/- 0.000 at both**, every round identical, at the vantage where batch 47 measured the gather at 72% of the pass. **A cost that does not move when the work triples is not the work**; `shaderstats` located it exactly -- 582,400 / 583,680 / 584,960 bytes at 0, 1 and 3 taps, and **96 registers throughout** -- and `sky`, with almost no shaded surfaces, still read -0.020. **So the answer L1 needed is better than the one asked for**: not *one tap is cheap* but *the cost does not scale with the coefficient count*, which is what decides whether an SH L1 or 6-axis ambient cube is affordable, since those are three taps and three read for what one reads. A per-texel **noise** fill read the same (+0.019 against +0.017 at `terraces`, identical at `cave`), closing the one hole a bit-exact measurement leaves -- a uniform texture is exactly what memory compression is best at. **Three readings, because bit-exact is the one result that cannot speak for itself**: the frame *must* move at `--probe-fill 0.5`, `shaderstats` *must* show the arm compiled in, and only then does 0 pixels mean free rather than absent. **The batch also stopped guessing at two capabilities.** `--bin probe` gained four feature rows: `EXPERIMENTAL_RAY_QUERY`, `RAY_HIT_VERTEX_RETURN`, `RAY_TRACING_PIPELINES` and `COOPERATIVE_MATRIX` are **all true** on this adapter. The last is the correction worth keeping -- **this session had argued the tensor cores were probably shut because wgpu likely did not expose cooperative matrix, and the user asked for the `probe` line rather than the belief.** The belief was wrong, and it is the same failure `CLAUDE.md` already recorded about ray query standing unchallenged for four batches, produced a second time by the file that recorded it. What is confirmed is that the *adapter* reports them; whether naga can express the operations is a further question nobody has asked | `--probe-tap` is the inverse of a control -- the feature ships off and the flag switches it on. **18 vantages, 0 pixels, identical hashes** both ways: shipping build against `voxelcraft-pre55.exe`, and tap on against tap off in one binary | [shading](shading.md), [roadmap L1](roadmap.md) |
| 56 | **the removal priced, and L1's read side settled as net negative cost** -- the companion to 55 and the half that turns a number into a decision. That batch measured what a probe tap **adds** and found it does not scale with the coefficient count; an architecture is only worth building if what it **removes** is larger, and nothing had measured that -- `lessons.md` has it as batch 40's *price the removal before designing the bake*, and L1's own text admitted the gap. **`--probe-ambient` is that build and it is deliberately incorrect**: the field *replaces* `shade_hit`'s ambient rather than multiplying into it, the field holds a constant, the frame is wrong and is thrown away. **What leaves** is what a pre-composed directional field makes redundant -- nine `blk_n` `light_curve` evaluations, four per-corner `bk/(sk+bk)` divisions with their `mix(SKY_TINT, BLOCK_TINT, ..)`, the `amb` bilinear, **`face_shade`** and the **ambient floor**. **What stays** is what a coarse probe could not carry: the nine gather lookups, `sky_n`, the AO rule and `sky_sm`. **That split is a claim about spatial frequency and not about cost** -- AO is a contact cue and `sky_sm` gates a hard shadow, and a probe field is for the low-frequency half. The flag **implies `--probe-tap`**, enforced at the parser rather than at `flags_from` so no consumer can build the broken pair, because the removal without the tap shades from nothing and the fastest build is always the one that computes nothing. **Net saving: +0.130 ms at `cave`, +0.091 at `default`, +0.030 at `sky`**, three of five vantages real and all three positive, two inside their own error, `resolve` **582,400 bytes shipping to 575,360** at **96 registers either way**. **The result that matters is the ordering, not the magnitude**: the saving is largest at `cave` -- the most surface-dense frame, where batch 47 measured the gather at 72% of the pass -- and smallest at `sky`, which has almost no shaded surfaces. Batch 55's *cost* did not grow with surface density and was therefore code; this *saving* does and is therefore work, and the two readings together are what make the pair a decision rather than two small numbers. **So a directional coloured field is not a tax on `resolve` to be funded out of the appearance budget -- it buys frame time while replacing two day-one constants with something derived**, and neither the format nor the cost now constrains the bake. `terraces` is the single negative row at -0.044 +/- 0.047 and is this session's noisy vantage, reading +0.017 +/- 0.002 early in batch 55 and +0.048 +/- 0.048 ninety minutes later; nothing is built on it in either batch. **What neither batch prices is now all of the remaining risk**: the field held a constant rather than irradiance, so the read cost transfers and the assumption that *one* tap carries sky-indirect, block light and bounce together does not; the bake is entirely unpriced and is L1's actual work; and nothing ranks the picture, because both builds render deliberately wrong frames | **not a control** -- the second flag here whose build is disposable. The shipping build is **18 vantages, 0 pixels, identical hashes** against `voxelcraft-pre56.exe`, and the frame *must* move under the flag | [shading](shading.md), [roadmap L1](roadmap.md) |

| 57 | **the ambient cube: roadmap R1's first rung, and the first time a surface's *position* decides its ambient rather than a table** -- the batch batches 55 and 56 were pricing. `face_shade`'s 1.00/0.80/0.62/0.50 per normal was the whole of directional occlusion since day one: a wall at the foot of a cliff lit exactly like the same wall in an open field. `src/probe.rs` bakes a **six-axis sky-occlusion cube every 8 blocks** in `stream::build`, on the sixteen rayon threads `CLAUDE.md`'s table measures as idle, and `shade_hit` multiplies the table by one hardware trilinear tap into it. **Six axes is exact rather than an approximation here** -- every cube face in this world has an axis-aligned normal, so the shader *selects an entry* instead of reconstructing an SH basis, and selecting it is a switch on `normal_id` which is what `face_shade` already was. That is also why the read is **one** tap and not three: the six entries are six slabs of one 3D texture and a face samples only its own. **Three decisions made this small, and two of them were retreats the measurement forced.** *(i)* **The bake marches `WorldGen::height` and never the voxels**, which makes it a pure function of world position -- so two chunks either side of a boundary write values of the *same* function into adjacent texels of one world-space lattice and there is **no seam at all**: roadmap P5b answered by construction, with no apron, no residency dependency and no re-bake when a neighbour lands, and a probe occluded by terrain 190 blocks away rather than one chunk away. What it pays is trees, caves, overhangs and edits, none of which the height field carries -- the flood still does, so what is lost is their contribution to *directionality* and not to level. *(ii)* **The field stores occlusion, not shading.** The first build baked the product `face_shade * occlusion` and the sweep caught it: `cave` moved **367,222 pixels at max delta 1**, because 0.62 and 0.80 do not survive a round trip through `f16` and 1.0 does. Storing the occlusion alone makes every absence of the feature exact instead of close -- an unbaked texel, a coarse LOD and a pinned `--probe-fill 1.0` all reduce to `face_shade(id) * 1.0` -- which is what lets the control be a pure revert **by construction**. *(iii)* **The cube normalises against an open field rather than storing absolute visibility**, and this is the retreat worth keeping. The absolute version is the better physics -- in an open field the sky is azimuthally symmetric, so the X/Z 0.62-against-0.80 split is a readability convention and two side faces genuinely receive the same ambient -- and it made the picture worse: `default` came back **brighter and flatter**, that frame being mostly X-facing steps going 0.62 to 0.75, with the occlusion the batch exists to add buried under an exposure shift nobody asked for. **The instrument decided it**: the user ranks this rung by a before/after screenshot pair, and a pair cannot separate two changes. **The tap costs what batch 55 said it would**: **0.02 to 0.04 ms** of `resolve` at `cave`, the most surface-dense frame, against that batch's -0.040 for one tap. **Quoted as a range on purpose** -- the same paired A/B read -0.042 +/- 0.002 with the machine hot and -0.020 +/- 0.009 an hour later with it cool, the vantage's absolute `resolve` moving 2.73 to 2.36 between them, which is the power-state caveat `PERF.md` opens with and not the shader. Indistinguishable from zero at `default` (-0.050 +/- 0.054) and `terraces` (+0.046 +/- 0.029); `resolve` 582,400 to **584,832 bytes at 96 registers either way**. **It banks none of batch 56's saving and that is structural rather than a shortfall** -- that figure is what a *pre-composed* field lets `resolve` delete, and sky occlusion alone cannot retire the nine `light_curve` evaluations, the per-corner divisions, the `amb` bilinear or `floor_rgb`; R2 is the rung that collects it. **The real price is off-frame and that is the whole point of the stage**: **1912 us per chunk** of bake beside the flood's 1344, paid out of the resource nothing is using. Two smaller findings: the tap is taken at **half a probe along the normal** because a surface sits on the boundary its own occluder defines, so an unbiased `hit` reads a probe as likely to be inside the terrain as outside it; and `SPEC_PROBE_CUBE`'s declared WGSL default shipped `false` for an hour, which changes no frame -- `make_spec` sets every override explicitly -- but had `shaderstats` reporting the **control's** code size as the shipping build's, every row internally consistent | **`--no-probe-cube` is 0 pixels and identical hashes at all 18 vantages** against `voxelcraft-pre57.exe`, and by construction rather than by luck. The feature moves **18 of 18** at max delta 13-62; `cave` is 30,300 at max delta **1**, which is the no-sky rule holding rather than the feature reaching. Two new `implies` claims say the difference comes from the *field* and not the code path: pinning it to 1.0 already switches the cube off (0 pixels at four vantages), and pinning it at all moves the frame (602,853 at `default`, 873,403 at `terraces`). **A third thing the batch produced is a pitfall rather than a number**: `cargo run --bin harness` does not rebuild `voxelcraft.exe`, so a sweep taken straight after a renderer edit measures the previous binary and reports a count identical to the last one -- which read as *the change did nothing* until a real build showed `cave` at 30,300 against 369,260 | [shading](shading.md), [roadmap R1/R2](roadmap.md), [src/probe.rs](../src/probe.rs) |
| 58 | **the ground bounce: roadmap R2's first rung, and the constant it retires names itself** -- `floor_rgb`'s own comment has read *"it is what stands in for the bounce light the two-source model never simulates"* since batch 3, and this batch simulates one. `probe::bake` gathers the cosine-weighted mean albedo of the ground each axis can see, off the horizon march batch 57 already pays for, and stores a per-channel multiplier on that constant in the **`.rgb` of the texel whose `.a` is batch 57's occlusion** -- so a directional *coloured* field costs exactly what the directional scalar one cost, which is the finding batch 55 went looking for when it measured three taps at the price of one. **18 of 18 vantages move**, max delta 1 to 46; `--no-probe-bounce` is a **pure revert at 18 of 18, 0 pixels, identical file hashes**, and by construction rather than by luck, because the field holds a multiplier whose identity is 1.0 and the lattice is created holding 1.0. **The frame pays nothing measurable**: `resolve` is -0.001 +/- 0.005 at `cave` and +0.008 +/- 0.032 at `default`, both inside their own error at a detection threshold of about 3 se, at **+128 bytes and 96 registers either way** -- and what makes that believable rather than merely unresolved is that it is the *same* `textureSampleLevel`, which `one_tap_serves_both_fields` is what stops a later edit splitting in two. **The bake is 1868 -> 2549 us per chunk, +681 and 1.36x**, against the roadmap's expectation of several times: the gather rides the march that already exists and adds one biome lookup on every other radius. **Two identities, and only one is per channel, which a draft of this batch's own prose got wrong**: an unbaked texel returns 1.0 on every channel and that is what makes the control exact, where *reference ground* returns 1.0 in luminance alone -- over grass the multiplier is `(0.416, 1.259, 0.156)` and the floor turns green at exactly the brightness it had, which is what stops the feature being buried under an exposure shift the way batch 57's first build was. **The apron is still unpaid and this rung is why it did not have to be**: the roadmap read R2 as the batch that buys it, on the assumption a bounce needs a voxel DDA, and the dominant outdoor term is sunlight off ground the height field already describes, at sky level 15 by construction -- so the bake stays a pure function of world position, with no seam, no residency dependency and no re-bake. What it cannot bounce off is a tree, a cave wall, an overhang or an edit, which is where R3 and R4 both point. **And the sweep's own headline is the number that looks like proof and is not**: a global tint would also move 18 of 18, so three tests read the field off a real `WorldGen` at a chunk *searched* for two surface types -- that it varies with position, that it varies between axes, and that the **brighter ground is on the brighter axis**, the last recomputed by a different route (a plain box mean, not the lobe gather) so a mirrored lobe table cannot agree with it. **Each was checked against a build that breaks it**: flattening the lobes fails the second and third and passes the first, and *mirroring* them fails only the third -- a world where every wall takes its colour from the ground behind it, which no picture reads as wrong. **One claim of this batch's own failed and was removed rather than relaxed**: batch 57's `implies` row saying a pinned field already switches its feature off does *not* have a working twin here -- 0 pixels at three vantages and **2 at `terraces` at max delta 1, reproducibly** -- because the cube's tap multiplies `shade`, a scalar factor at the end of `shade_hit`'s expression, where the bounce's multiplies `floor_rgb`, an addend inside the parenthesised sum, and reassociating a sum is where a ULP goes. It is `hit_t`'s invariant in a new place; it says nothing against the control, which is exact against a real revert; and **it is not the non-reproducing `implies` reading batch 57 filed**, which fired in the same sweep and still does not reproduce. [`errors.md`](errors.md). **Most visible at `night`** -- 757,159 pixels at max delta 46 against `cave`'s 27,415 at 1 -- because `floor_rgb` does not scale with `frame.daylight`, which predates this batch and which it did not change. [`shading`](shading.md), [`src/probe.rs`](../src/probe.rs) |
| 59 | **coloured block light: roadmap R4, and the first rung of the main sequence whose cost lands on the frame.** The constant it retires is `BLOCK_TINT = (1.0, 0.65, 0.32)`, which was **the colour of every emitter in the world** -- and batch 55 had already found the sharper half of that: `def(..).light > 0` was true of **exactly one block**, so a coloured channel shipped against this world would have been bit-exact everywhere for want of *content*, which no camera can fix. So the batch is content as much as code. Three lamps -- amber `[15, 9, 3]`, azure `[3, 9, 15]`, verdant `[4, 15, 6]` -- and the light cell widened from one byte to two, `sky:4 \| r:4 \| g:4 \| b:4`. **The channel had to widen rather than the block table, and the flood is why**: a scalar level with a per-block tint applied at read time cannot mix, because the cell between an amber lamp and an azure one holds one number and no memory of which block put it there. `light::flood` is called three times on three arrays rather than taught about colour, which is what keeps `max(r, g, b)` equal to the level the single pre-59 flood held at every cell it reached -- the identity the control rests on, asserted rather than sampled. **`BLOCK_TINT` is the one retired constant in this ladder that did **not** survive as a calibration**, where `face_shade` and `floor_rgb` both did, and the reason is the gamut: keeping it as a multiplier on a normalised chroma is bit-exact for a white emitter and caps blue at 0.32 of what red can reach, so a blue lamp is three times dimmer than a white one at the same level. It moved into glowstone's row instead, as the levels whose `light_curve` values come nearest it -- 15, 13, 9 reaching **(1.0000, 0.7111, 0.3024)** against (1.0, 0.65, 0.32), with **green 9.4% high and 12 9.4% low**, a tie a test asserts is the closest four bits can get. **Two builds were measured and thrown away, and both are findings.** The ungated one cost **+0.835 ms of a 2.655 ms `resolve` at `cave`** -- twenty-seven `light_curve` evaluations where there had been nine, at a vantage holding no emitter, where all twenty-seven return zero. The gate is an OR of the nine gathered cells' block nibbles, **exact rather than approximate** (all-zero reduces the block term to `SKY_TINT * sk` to the bit), and it is tested on the *cells* and not on the chunk because the gather crosses chunk boundaries where the flood does not -- a chunk bit would drop a lamp just across the seam, and batch 51's rejected chunk gate is that lesson from the other side. Then **caching the nine curves in an `array<vec3<f32>, 9>` cost 0.147 ms**, and `shaderstats` said why in one row: shared memory **15,872 to 16,896 bytes**. Evaluating the twelve corner curves inline -- **thirteen per pixel rather than nine** -- put it back and was faster, so **on this pass occupancy is worth more than four `light_curve` calls**, which is batch 47's finding from the other end. **What shipped costs +0.321 ms at `cave`, +0.340 at `default`, +0.150 at `sky`**, and the split is the interesting part: **+0.110 is the widened cell**, paid by both arms because no override can shrink a brick, and **+0.202 is the coloured read at a vantage where the gate skips every evaluation** -- the shipping arm losing to an arm that executes strictly more instructions, at 586,624 bytes against 588,928, 96 registers and 15,872 bytes of shared memory either way. **That has no mechanism and is at the front of the roadmap queue rather than investigated here.** Off-frame the batch is free: the flood is **1348 us against 1402**, because three arrays allocated only when a chunk has an emitter beat one allocated always; memory `attributes + light` **4.2 to 5.5 MB** over 96 chunks. Nineteenth vantage `lamps`, and **it cannot be a `bitexact` row against any earlier build** -- its chamber holds ids the parent does not have and `block::def` clamps rather than refusing, so the journal substitution would replay it as *meadow* | `--no-light-rgb`, and **the first control here whose picture reverts exactly and whose time cannot**: **18 of 18 vantages that predate the batch, 0 pixels, identical hashes** against `voxelcraft-pre59.exe`, but it recovers only **0.202 ms of the 0.321** because the cell it reads is two bytes whichever arm compiles. **Not a pure revert at a world that does not predate the batch**, which is `--no-glass`'s shape: with it *off*, `edits` moves **313,302 pixels at max delta 3** and `glass` **86,481 at 2**, which is glowstone's four-bit re-authoring and not the feature. The positive is `lamps`: **919,746 of 921,600 pixels**, MAE **13.10** over the `mixing-wall` crop, and **the user judged that pair and judged it a clear win** -- *"i love the lamps ... it looks a lot better as in quite a bit"*, on the `lamps` capture and on no other, which is the only vantage where the feature does anything visible. **That is the whole of what justifies the frame cost**, and it is recorded here because nothing else in this row does: `edits` and `glass` move by max delta 3 and 2 in daylight and neither of us could see it. Reproduce the pair with `--demo-lamps --time 0.30 --cam-height -20 --cam-yaw 0 --cam-pitch 0`, with and without `--no-light-rgb`. Plus two `implies` rows that separate *exact* from *never wired up* -- `Nothing` at `default` and `cave`, which hold no emitter, and `Something` at `lamps` and `edits`. 36 `implies` claims and 28 `validate` checks, both clean. New `tests/light.rs` asserts the four things no sweep can: that `max(r, g, b)` is the level the pre-59 flood held at every cell it reached, that two lamps of different colours mix at the midpoint, that `common.wgsl` still decodes a cell as two bytes and holds no byte-wide spelling anywhere, and that no 4-bit level lands nearer `BLOCK_TINT` than glowstone's row does. Every emitter in the table is checked against `bar_slot_refusal` by census rather than by name. Clippy silent | [shading](shading.md), [roadmap R4](roadmap.md) |
| 60 | **the ground only bounces the light that reaches it, and the ambient floor stops being a flat constant** -- roadmap R3's first rung, in two halves that ship together because neither is right alone. **The transport half**: batch 58's bake gathers the albedo of the ground a probe can see and weights it by a form factor, and never asks whether that ground is itself lit, so a valley floor and an open field of the same grass bounce identically. `ray_exposure` fixes that from the samples the horizon march **already takes** -- the step loop is split in two so the whole height profile of a ray exists before any step on it is weighted, and the sky a ground sample sees is a maximum of slopes over that profile rather than a new march. **Zero new height samples; the chunk's 20,480 stay 20,480.** It sees one azimuth and its reverse, so a ravine running *along* a ray reads as open -- biased toward batch 58's answer, which is the one it has to reduce to. **The energy half**: `floor_rgb` was `(0.85, 0.90, 1.00) * frame.ambient` with `ambient` at 0.08, which [ADR 0001](adr/0001-no-elevation-form-factor.md) measured at about 7% of the light on a lit surface and used to reject the elevation form factor -- *no redistribution of it can be worth more than that*. **This batch measured that ceiling a second time rather than arguing with it**: the transport half alone is **MAE 0.372 at `default`**, eight times under the 2.87 that ADR already rejected by eye. The ADR names the only way out -- *a build where the ambient floor carries materially more energy* -- and the added term is it: `probe.rgb * PROBE_SUN_GAIN * sky_sm`, added beside the authored floor rather than replacing it, so a cave keeps its sole light. **The gate is the batch's one real defect and it was caught by the fixture rather than by reasoning.** The first build gated on `probe.a * frame.daylight`, on the argument that a cave sees no sky; but the field stores **occlusion normalised to 1 in the open** so that every absence of it is exact, and the bake reads `WorldGen::height` and never the voxels -- so a buried probe returns **1.0, which is *unoccluded*, not *dark***. It measured **921,600 pixels at `cave` and at `lamps`**: full sunlight bouncing around a sealed chamber. `sky_sm` was already in scope two lines up gating `direct`, is the flood's own per-voxel answer, is zero in a cave and in a sealed room, and already carries `frame.daylight` -- so the corrected term needs neither factor the draft multiplied in. **`--probe-sun-high` is a sweep rung and not a control**, the only `SPEC_MASK` bit in this project whose default is *off*: two constants selected by an override rather than a uniform, because `GpuFrame` has no pads left and a value with two settings does not need one. **The user ranked the three arms by playing them and chose the shipping gain of 0.10 over 0.25**, and closed the line: the floor is as far as this goes | **19 vantages, 0 pixels differing, identical file hashes** under `--no-probe-shadow --no-probe-sun` against `voxelcraft-pre60.exe` -- a real revert binary and not a flag, and the pair stays exact after `FLAG_PROBE_SUN_HIGH` was added to `SPEC_MASK`. The positive is all 19 moving: `default` 760,295 at max delta 27 and MAE 2.2248, `terraces` 910,194 at 21 and MAE 4.8510, `canopy` 457,247 at 26. **`cave` 216 pixels at max delta 1 and `lamps` 404 at 1** -- the no-sky rule holding, which is batch 57's and 58's own signature and the thing the first build broke. **`cave` is 0 pixels between the two gains**, so no gain can leak underground, which is the `sky_sm` gate proving itself independently of the bug. **Frame cost is nothing measurable against a real revert**: `resolve` +0.001 +/- 0.027 at `default`, -0.018 +/- 0.014 at `cave`, +0.025 +/- 0.015 at `sky`, all inside their own error, so what is claimed is below about 3 se. The price is off-frame: the bake goes **2743 to 3414 us per chunk, +671 and 1.24x**. One new `IMPLIES` claim -- `--no-probe-sun` implies `--probe-sun-high` changes nothing -- **exact at 4 vantages with identical hashes across two different pipeline keys** -- and it is deliberately *not* filed as the counter-example to batch 58's ULP, because it is not the same shape. There the two arms differ in **live** code at the same expression site (`floor_rgb`, an addend inside the parenthesised sum, which is where a reassociation goes); here the differing override feeds only `probe_sun_gain`, inside a block `SPEC_PROBE_SUN` has already dead-stripped in both arms, so the driver compiles identical code and the fold cannot move. What it does establish is narrower and still worth having: **a `SPEC_MASK` bit whose only consumer is dead costs a neighbouring control nothing**, which is the question any new sweep rung has to answer before it is added beside an exact control. Two new tests in `tests/probe.rs`: that the shadowed field is nowhere brighter than the open one *and* somewhere darker (a bake ignoring the flag passes the first alone), and that the occlusion cube is untouched. `capture` and `validate` clean at the start; `implies`, 29 test suites and `clippy` clean at the end | [shading](shading.md), [roadmap R3](roadmap.md), [ADR 0001](adr/0001-no-elevation-form-factor.md) |
| 62 | **a mountain's shadow stopped popping when its LOD changed, and the user found it by playing** -- roadmap D2, opened and closed the same session. `grid` holds **one** chunk index per LOD-0 cell and serves every secondary ray: `shadow_ray`'s march and the nine-cell gather's cross-chunk `world_sample`. Across a cross-fade it resolved to the **fine** half unconditionally, and `RenderItem::fade` is *a pure function of distance* -- so the fine half enters `visible` the instant a chunk crosses the outer edge of the band, when its share is still ~0. **The surface was therefore ~100% coarse, crisp and stable, while its shadow had already switched to the fine silhouette in one frame.** Measured at `lod`, the two representations differ by **31,530 pixels of 921,600 at max delta 34** -- 66x the whole of batch 61's rejected soft sun -- so what snapped was large. **The fix is the sort key and nothing else**: order the grid writes by `RenderItem::share()` so the half carrying most of the rays wins the cell, which puts the discontinuity at the *middle* of the fade, the one moment the frame is already in flux and the temporal pass is already blending two representations. Outside a transition every share is 1.0 and the tie-break is the old rule exactly -- coarser first, finer overwrites -- which is what makes every non-fading vantage exact. **The batch's real instrument is neither a still nor a bench**, and batch 61 is why: that batch rejected a feature on stills and the user then found this defect by playing the rejected build. A pop is a *frame-to-frame* discontinuity, so it was measured as one -- sweep `--streaming-factor` in 0.025 steps with `--no-taa`, which walks the LOD ring across a fixed scene exactly as camera motion does, and diff **consecutive** captures. **Two of four ring crossings lose their pop outright: 45,174 px -> 4,723 and 35,753 -> 4,882**, and the total motion over that range falls **90,331 -> 21,478**, so it is removed rather than displaced onto a neighbouring step. **Two crossings are unchanged and that is the honest half** -- at those the *surface* representation changes too, which no shadow-side ordering can touch. **A first instrument failed and is worth recording**: `--cam-dolly` against a static capture reads 1.1176 MAE on *both* arms, because the residual of a moving capture is dominated by disocclusion and the pop is buried in it. **And it is faster, not merely free** -- resolving a fade to the coarse half marches a cheaper tree | **19 vantages, 0 pixels differing, identical file hashes** under `--no-shadow-share` against `voxelcraft-pre62.exe`. The control is free **by construction rather than by specialization**: no shader reads it, so it is neither a `frame.flags` bit nor a `SPEC_MASK` entry, and **not because there was no bit left** -- a draft of this row said batch 61 had spent bit 31 and it had not, since that batch was reverted; bit 31 is free and `FLAG_PROBE_SUN_HIGH` at bit 30 is still the highest. It is a constructor parameter rather than a setter so the compiler names all three `Renderer::new` call sites -- a setter one headless path forgot would be a control that silently does nothing there. The positive is **10 of 19 vantages moving**: `open-sea` 13,853 px at max 132, `lattice` 9,547 at 67, `lod` 4,208 at 38, `sky` 481, `default` 431, `overcast` 425, `low-sun` 213, `night` 187, and 1 pixel each at `edits` and `glass` -- which is inside D1's measured 1-in-15 single-pixel non-determinism and is not evidence of anything. **`open-sea` leading rather than `lod` is the finding under the finding**: that grid is read by the nine-cell gather's cross-chunk lookups as well as by shadow rays, so this is not a shadow-only change and the entry that called it one was wrong. Cost **-0.084 ms +/- 0.032 of `resolve` at `lod`** (t = +2.65, 7/8 rounds) -- a saving -- and **indistinguishable from zero at `open-sea`** (+0.044 +/- 0.031, t = 1.43). `capture` and `validate` clean at the start; `clippy` silent; suite green | [lod](lod.md) |
| 63 | **the ambient's sky colour is the sky the renderer actually draws** -- roadmap R7, the sixth rung of the main sequence and the cheapest of them. `SKY_TINT`'s `vec3(0.6038, 0.7084, 1.0000)` multiplied `amb` on **every shaded pixel in the world**, scaled only by `frame.daylight`, and nothing coupled it to `sky_base`, which `amb` never read at all. **Three things collapsed the batch to a handful of ALU.** The gradient is two lerps of four constants against `frame.daylight`, which `resolve` already has, so there is **no uniform, no CPU integral and no `GpuFrame` seam** -- an earlier draft of the entry budgeted all three, and a CPU integral would additionally have meant *writing* a second copy of the sky model, since `sky_base` is WGSL only. The gradient is linear in its endpoints, so its cosine-weighted hemisphere mean is `mix(horizon, zenith, W)` with `W` the mean of the *parameter* alone. And **`n.y` is exactly +1, 0 or -1 for every normal state this renderer has** -- cube faces are axis-aligned and `normal_of` returns `(0.707, 0, +/-0.707)` for both cross-quad planes -- so two weights cover the world: **0.78431373** up, the closed form `2 / (2 + SKY_GRAD_EXP)`, and **0.58578900** side, by quadrature. **The closed form is what says the quadrature is right**, and `tests/sky_tint.rs` asserts it as such. **What ships is a departure from `SKY_TINT` rather than a replacement of it.** The real sky is far darker than the constant -- day zenith `(0.0637, 0.2140, 0.8276)` -- so substituting radiance naively would darken every shadow in the world, into the standing judgement that it already reads too dark and into batch 57's ambiguity between an exposure shift and a feature arriving. Two exact identities disarm it, both holding at every strength: at the reference condition -- up face, full daylight, no halo, reachable with `--fog-scatter 0` -- the function returns `SKY_TINT` **to the bit**, and everywhere else the result carries `SKY_TINT`'s **luminance** by construction, so **only the hue moves**. The four gradient endpoints were hoisted out of `sky_base` into named constants because this batch gave them a second reader, which is batch 26's lesson applied before it could fire. **The sunset half is included and the entry's headline about it was wrong**: at low sun `frame.daylight` falls and *both* endpoints lerp toward the night constants, so the gradient alone is dark blue and carries no orange whatever -- the warmth is `scatter_lobe`, added as **energy rather than sampled as a direction** (`SKY_HALO_ENERGY` = 4, derived from `hg_gain`'s own normalisation; a single directional sample reads the wrong side of a cliff because the lobe is ~30 degrees wide and the hemisphere is not). **And the halo is worth most at midday and least at the two low-sun cameras, the opposite of what was promised**: the sky ambient is `sk * frame.daylight`, so at a sunset there is almost no ambient left to re-colour | **19 vantages, 0 pixels differing, identical file hashes** under `--no-sky-tint` against `voxelcraft-pre63.exe` -- a real revert binary and not a flag, checked at two strengths. The revert is exact **by construction**: the cleared arm returns `SKY_TINT` itself rather than a formula evaluating to it, which would be a claim about how naga reassociates and is what batch 58 lost two pixels at `terraces` to. The positive is **15 of 19 moving**, max delta 14 to 42: `terraces` 910,104 of 921,600, `edits` 895,445, `glass` 889,047, `default` 759,958, `low-sun` 589,337, `canopy` 412,430 at the highest delta. **The four that do not move are the four with no sky in them** -- `cave`, `lamps`, `night`, `deep-water` -- each by mechanism: the first two are sealed so `sk` is zero, `night` has `daylight` at 0.05, and `deep-water` sits 24 blocks down past `WATER_DARK_DEPTH`. **R7 named `cave` as the vantage that must not move and it reads 0**, which is batch 60's failure not repeating. **The batch's most useful finding is a negative one and the user found it, not the fixture**: at the physical strength the whole feature is **MAE 1.2185 at `default`**, under [ADR 0001](adr/0001-no-elevation-form-factor.md)'s 2.87 rejected-by-eye line, and they reported *"0 difference in the pictures"* from an image slider before any number on file disagreed. **759,140 pixels differing and MAE 1.22 are the same frame described two ways, and only one says whether anybody can see it** -- a count answers *did the feature fire*, not *is it worth having*, and this batch read the first as the second throughout. `SKY_TINT_SAT`, an exponent on the per-channel ratio, is the one authored number in the batch; the ladder is 1.0 -> MAE 1.2185, 2.5 -> 3.0379, 4.0 -> 4.8419 at `default`, and **the user played it and chose 2.5**, beside `--probe-sun`'s shipping 2.2248 and well under `--probe-sun-high`'s rejected 5.7074. **The two halves rank apart through `--fog-scatter 0` and not a second flag bit**, which matters because `FLAG_SKY_TINT` is **bit 31 and `frame.flags` has no more**: gradient alone is `terraces` 579,125 and `default` 630,439. **Frame cost is 0.02 to 0.045 ms of `resolve` at `cave`, where 0 pixels move**, and is quoted as a range rather than a number because the two sweeps bracketing it were an hour apart on a machine that heat-soaked between them -- -0.045 +/- 0.014 (t = -3.21) cool against -0.024 +/- 0.017 warm, with the same vantage's absolute `resolve` moving 2.804 to 2.839 and `default`'s 6.140 to 6.520, which is `PERF.md`'s power-state caveat and not the shader. Nothing distinguishable at `default` or `sky`. **What survives both readings is that the cost tracks surface density**, batch 56's own discriminator for work against code. `resolve` **587,264 to 592,512 bytes at 96 registers either way**, and **shared memory 15,872 to 14,848, -1,024, which nothing aimed at and for which no mechanism is claimed**. Six new tests in `tests/sky_tint.rs`, every one recomputing a shipped literal from the endpoints beside it -- the one failure mode no picture and no sweep could catch, because a drifted literal renders a plausible frame and the control reverts the whole function rather than the constant inside it. `capture` and `validate` clean (28 rows, 0 fail); `implies` 37 claims clean; 30 test suites and `clippy` clean | [shading](shading.md), [roadmap R7](roadmap.md) |
| 64 | **a chunk outside the view frustum casts a shadow again** -- roadmap D3, and the third defect in this project found by *playing* rather than by a metric. `Renderer::build_chunk_list` frustum-culled `render_set` into `visible`, and `visible` was the only thing that filled `grid` -- which is what every shadow ray walks and what the nine-cell gather reaches through when its neighbourhood crosses a chunk boundary. **So a chunk the camera was not pointing at occluded nothing**: turn until a mountain leaves the frustum and its shadow leaves ground that is still on screen. The user's words were *"the shadow should not depend on me the player but it does depending on my position and angle ... the large mountain shadow becomes shorter or larger like a weird pop in pop out"*, and the mechanism was found in one grep the same minute. **The fix is a partition, not a wider frustum.** `partition_for_grid` returns the frustum-culled entries first and the entries kept for their shadows alone after them, and the two lists are **concatenated** into one upload: `frame.chunk_count` carries the length of the *first*, and `tile_select` -- the only reader of that field anywhere in the tree -- loops `0 .. chunk_count`, so the tail is unreachable from the pass whose cost scales with the list and reachable only through `grid`, which is indexed and carries no count. **That is why the pass whose cost was the thing to watch pays +0.000 to +0.001 ms.** **The tail is bounded by the lattice rather than by a distance of its own, so the batch adds no constant**: a chunk covering none of the 32x8x32 grid can never be named by a grid cell, so uploading it would be uploading something nothing can read. At `default` that is **157 chunks drawn and 346 in the grid**, against `MAX_CHUNKS` = 8191 -- 23x headroom on the 13-bit id. **It is not a shadow-only fix and could not have been**: a cross-chunk light lookup into an off-screen neighbour used to return `NO_CHUNK`, which `world_sample` reads as *open sky and not solid*, so the ambient at the frame's own edge was computed against a world that stopped at the frustum. **The band the defect lives in is narrower than it sounds and knowing both halves is what made a vantage findable**: an occluder has to be more than **52 degrees** off the view axis -- the frustum's horizontal half-angle at `--fov 70` -- before it is culled at all, *and* within `shadow_dist`'s **220 blocks** of the ground it darkens, past which batch 37's light envelope carries the shadow off a 512x512 height window no frustum touches. That second condition is why a low sun reads exactly **0** | **20 vantages, 0 pixels differing, identical file hashes** under `--no-offscreen-shadows` against `voxelcraft-pre64.exe`, a real revert binary and not a flag. The revert is exact **by construction and `tests/shadow_grid.rs` says why**: with the control clear the off-screen list is empty, so `chunk_scratch` holds the same entries in the same order and the fill runs over the same indices -- a structural claim a sweep of twenty cameras cannot make. The positive is **4 of 20 moving**: `offscreen-shadow` **18,468 pixels of 921,600 at max delta 31**, `terraces` 7,843 at 11, `lattice` 32 at 15, `open-sea` **1 at 1**. **That single pixel is roadmap D1's exact signature and is not D1**, which cost eleven commands to establish: A-against-A at that vantage is clean **8 of 8** and the A/B reproduces the pixel **3 of 3**. **Sixteen zeros are not margin**, they are the band above. **The twentieth vantage is the batch's own and its two numbers disagree about which camera is better**: `--time 0.28` moves 32,937 pixels for a crop MAE of 0.22 at max delta 9, and the shipped 0.32 moves 18,468 for **2.6165 at max delta 27** -- a count says the feature fired and cannot say whether anybody can see it, which is batch 63's lesson arriving one batch later as a choice of camera. Its `CHECKS` row is therefore an MAE over the crop and not a pixel count over the frame. **And the defect itself is still not something one frame can hold**: at that camera's position the control moves 29,240 pixels at yaw 265 and **0 at yaw 275**, ten degrees apart with the same slope in both, so the claim lives in `tests/shadow_grid.rs` as an equality -- the grid-eligible set is identical at eight yaws from one position, while the drawn set is not, which is the frustum cull still doing its job. That file's first draft asserted the equality on the raw union and **failed at 337 keys against 340**: the drawn list is not lattice-filtered and has no reason to be, so what must be yaw-independent is what a secondary ray can *reach*. Cost is **0.100 ms of `resolve` at `terraces`** (+/- 0.020, t = -4.98, and -0.105 +/- 0.023 in the Hi-Z-off arm), and nothing distinguishable at `default` (-0.050 +/- 0.042) or at `offscreen-shadow` itself (-0.014 +/- 0.047) -- **the cost does not track the pixels that moved and should not**, since what is paid is `march_chunk` on cells that used to read `NO_CHUNK`, where a ray that now hits terminates early and one that still misses has paid for a traversal. Four new tests, 31 suites and `clippy` clean. **Two fixture rows did not come back clean and neither is this batch's**, both established rather than assumed. `validate` fails `--water-absorb 0` / `rg_speckle` / `terraces` at ratio 1.2468 against a 0.8 floor -- **`voxelcraft-pre64.exe` fails it at 1.2722 and `pre63` passes at 0.4993**, so batch 63 flipped it and this batch moved it 0.025 back toward passing; filed as roadmap D4 rather than chased, with the four-binary table and the one command that would settle its mechanism. `implies` reported **3 disagreements of 1 pixel each, all at `lattice`**, which is roadmap D1's exact signature: none reproduced -- `implies --only lattice` is **37 of 37 clean on this build and 37 of 37 on `pre64`**, and D1's own A-against-A test is **0 of 20** here, so there is no evidence the rate moved | [lod](lod.md), [harness](harness.md) |

## On the external architecture report

`Voxel Ray Tracing Architecture.pdf` was written against these `.md` files without ever
seeing the code, and it is **not in this repository**. Its frame structure, key layout and
Dolonius indexing describe this engine correctly, and its insight that analytic motion
vectors are nearly free here was worth acting on. Its four per-feature cost estimates have
all now been scored and all four missed, in four different directions — `PERF.md`'s
"Projected vs measured" keeps the table and the one durable conclusion, which is that a
per-feature millisecond is the wrong *shape* of estimate when the cost depends on where the
camera points. **Do not re-plan finished work from that document.**
| 65 | **every opaque surface reflects the sky** -- roadmap R6, the last rung of the main sequence, and the first term in this renderer that is *added outside `albedo`*. Everything that is not water or glass has been perfectly diffuse since batch 1; a Schlick weight at `GROUND_F0` now carries `sky_base` in the mirror direction, gated by `sky_sm`. **The ceiling question was asked before building rather than after, which is what R6's entry demanded** -- and it is the first rung of the sequence to pass it. R3's rung modulated a floor `Config::ambient` pins at ~7% of a lit surface and R7's carried `SKY_TINT`'s luminance by construction so only hue could move; both found their cap only after being built. Specular is bounded by Fresnel instead, and Fresnel is not a share of anything: 4% at normal incidence but **20% at cos 0.3 and 61% at cos 0.1**, on a ground plane that is at grazing incidence over most of its extent. Measured MAE **5.98** at `default` against batch 63's shipped 3.04 and [ADR 0001](adr/0001-no-elevation-form-factor.md)'s 2.87 by-eye line. **The first build was wrong and the way it was wrong is the batch's most transferable finding**: it had no roughness term, Schlick reaches 1.0 at grazing for *every* F0, and the frame came back a blue-white haze at MAE 15.65 and max delta 202. What is missing from a bare Fresnel is the geometric shadowing-masking term -- on a rough surface the microfacets that would mirror a grazing ray are occluded by their neighbours, at exactly the angle Schlick says reflectance is highest. `GROUND_ROUGHNESS` caps the climb at `1 - roughness` = **0.10** instead of 1.0. **Turning the gain down would not have fixed it**, because the fault was the curve's *shape* and not its scale: a roughness ladder from 0.80 to 0.97 moved MAE only 11.50 to 10.35, which is what proved the grazing term was never the bulk of it. **The gate is `sky_sm` and batch 60 is why it is not `probe.a`** -- that batch gated a bounce on the baked occlusion and lit a sealed chamber with 921,600 pixels, because the field stores occlusion *normalised to 1 in the open* so every absence of it returns 1.0, which is unoccluded and not dark. Third time that gate has been the right one. **`sky_base` and not `sky_color`**, per `CLAUDE.md`'s invariant that one takes a ray origin and the other must not; it also already mixes toward `horizon * 0.26` below the horizon, so a ceiling, an overhang and an underside reflect dark ground with no special case. **The control needed a bit `frame.flags` did not have**, batch 63 having spent 31, and the exit is the batch's second finding: every `SPEC_MASK` bit since `FLAG_PROBE_TAP` is one *no shader reads*, existing only so `ensure_spec` can tell two pipelines apart, so what had to widen was the **pipeline key and not the uniform**. `FrameParams::spec_hi` is CPU-side, never uploaded; `GpuFrame` does not grow, keeps its `mat4x4` alignment, invents no pad and invalidates none of its five `offset_of!` asserts. Widening `flags` to `u64` would have touched seventeen call sites and every `FLAG_` constant to buy the same thing. **Two things declined and recorded rather than hidden**: there is no roughness *lookup* -- the sky is sampled as a mirror, tolerable only because the sky is low-frequency, which is the same approximation R6's entry says a probe field would make -- and `sky_base`'s sun disc is unshadowed, so a slope the shadow ray rejected can still catch a glint. `GROUND_F0` 0.04 is one constant for the whole world because **nothing here is a metal**; a metal's F0 is its albedo and would be per block, which is batch 59's *content* kind of rung and would widen a per-voxel store. `SKY_SPEC_GAIN` **shipped at 0.5 over 0.25 and 1.0, chosen by the user from images**, 1.0 being the unscaled physical answer -- `LEAF_FILL`'s shape at batch 43 and `SKY_TINT_SAT`'s at 63 | **20 vantages, 0 pixels differing, identical file hashes** under `--no-sky-specular` against `voxelcraft-pre65.exe`, a real revert binary and not a flag. **16 of 20 move with it on**, max delta 10 to 62: `terraces` 910,219, `edits` 895,834, `glass` 895,701, `underwater` 913,271, `default` 760,651, `overcast` 760,631, `low-sun` 594,065. **The four reading 0 are `cave`, `night`, `lamps` and `deep-water`, each by mechanism rather than by luck** -- `sky_sm` is exactly zero underground, zero in a sealed chamber, carries `frame.daylight` so is zero at night, and is exhausted 24 blocks under water. They are **the same four batch 63 found**, which is two independent gates agreeing about which cameras have no sky, and `cave` reading 0 is batch 60's failure not repeating. **Cost +0.295 ms of `resolve` at `cave` where 0 pixels move, +0.516 at `default`, +0.271 at `sky`**, all against the real revert at t = -28.0, -10.6 and -14.6. **The mechanism is register pressure and not arithmetic, which `shaderstats` settles in three builds**: `resolve` is **96 registers without the term and 128 with it**, and removing `sky_base` from the term leaves it at 128, so it is not the sky lookup. 592,512 bytes to 592,256 -- the code got *smaller*. That is why a vantage with zero changed pixels pays nearly as much as one with 760,000. **A `sky_sm > 0.0` early-out was built and rejected**: it saved nothing at `cave` (0.295 to 0.282, inside its own error) and made `default` and `sky` **worse** (0.516 to 0.594, 0.271 to 0.305) -- roadmap P11's lesson repeating, ask what a gate costs before crediting it with what it skips. `validate` fails only D4's known `rg_speckle` row, which this batch moved 1.2468 to 1.2763 by the mechanism D4 already names; `implies` **37 claims, every one as the control table says**; suite green, `clippy` silent. Six new tests in `tests/sky_specular.rs`, one negative-tested by driving `GROUND_ROUGHNESS` to 0.0 | [shading](shading.md), [gpu](gpu.md) |
| 66 | **`resolve`'s cost is its register count, and which feature owns the registers does not matter** -- roadmap P13 closed and P8's standing question answered, taken as one batch because they are the same experiment with opposite signs. P8 had asked since batch 42 whether glass's millisecond is size or occupancy; batch 65 supplied an independent second sample by adding 32 registers while making the module *smaller*. **The test is `cave`, where glass draws nothing and batch 65's specular changes zero pixels, so both features are pure dead weight**, and all four register counts are selectable from one binary because both are specialization bits. Against the 80-register build: glass's 16 registers cost **+0.156 ms +/- 0.005**, the specular's 16 cost **+0.151 +/- 0.018**, and both together, 32, cost **+0.441 +/- 0.014**. **The same number twice from two unrelated features** -- a transmissive material with a traced reflection and a Fresnel sky lobe -- which is what settles it. **32 registers costs 1.41x two lots of 16**, so there is an occupancy step between 96 and 128 and a register budget must not be priced by interpolation. **Size is now ruled out three times**: batch 42 removed 271,872 bytes of glass and the frame got slower, batch 65's term shrank the module while costing 0.3 to 0.5 ms, and this batch stubbed glass's `shade_water` dispatch to remove **200,000 bytes** with the register count not moving at all. **The fix that closed P13 is the shape to reach for first: `shade_hit` is inlined four times**, so a term in its body exists in four copies and the allocator budgets for all of them. Passing a literal `false` at the three secondary sites folds the specular arm away there and takes `resolve` from **128 back to 96**. **`do_spec` is a new parameter rather than a reuse of `smooth_light`, deliberately** -- that argument is `secondary_smooth()` at those sites, folds identically today, and reusing it would quietly make `--no-flat-secondary` a second control for the specular, which is the coupling this roadmap warns about every time it appears. **What it costs is the picture on transmissive surfaces**: a floor seen through water or a pane no longer takes a sky sheen, which is batch 45's own rule arriving one rung later -- the three transports destroy the contact cue, so the cheap model is right there. `terraces` **MAE 0.8388** at max delta 16 and `shore` **0.4665** at 17, both far under [ADR 0001](adr/0001-no-elevation-form-factor.md)'s 2.87 by-eye line. **P8 is not closed and two of its candidates are struck**: glass's own `shade_hit` inline reads 96 when stubbed, and so does the `shade_water` dispatch that the roadmap's `SPEC_GLASS_OVER_WATER` suggestion was aimed at -- so that suggestion buys code size and nothing else. What is left is `shade_glass`'s own body. **And glass's 16 registers are currently free in the shipping build**, because the specular's single remaining copy sets the same 96-register peak glass does, so the two cost 16 rather than 32 and have to be priced as one problem | **20 vantages, 0 pixels differing, identical file hashes** under `--no-sky-specular` against `voxelcraft-pre65.exe`, unchanged from batch 65 -- the fix moves what the shipping build draws, not what the control reverts to. **Batch 65's bill against that same real revert binary falls from +0.295 / +0.516 / +0.271 ms to +0.051 / +0.065 / +0.044** at `cave` / `default` / `sky`, an 83-87% recovery, with `default` now indistinguishable from zero (t = -1.60, 3/8 rounds). `resolve` **128 registers to 96**; 593,920 bytes against batch 65's 592,256, so the fix costs 1,664 bytes of code to save 32 registers, which is the batch's own trade stated in one line. 16 of 20 vantages still move and the same four still read 0. `validate` **clean, exit 0** -- the first clean run since batch 62, D4 having been fixed in this session. `implies` **37 claims**, suite green, `clippy` silent. One new test, and **one existing guard caught the change and was right to**: `tests/secondary.rs`'s `the_primary_hit_asks_for_smooth_light_by_literal` pins the primary call site's text, which grew a second literal; its claim was unchanged and only the assertion was updated | [shading](shading.md), [gpu](gpu.md) |
| 67 | **`resolve` is 80 registers, and P8's millisecond is closed** -- the entry had been open since batch 38e and had survived batches 42, 66 and one wrong strike. **Both ceilings had to fall in one batch and the order was forced**, which is the finding: the shipping build is a `max()`, so glass and the specular each independently held 96 and fixing either alone would have paid *nothing*. **Step 1, the specular.** Batch 67 stubbed its three operations one at a time and every simplification read 80 -- dropping `sky_base`, calling it on `n` rather than the reflected direction, replacing Schlick with a constant -- so it was none of them individually. Folding the weight to one scalar first changed nothing, because the compiler already schedules that. **What mattered was *where*.** Written next to the return, `n` and `rd` must stay live across the nine-cell gather, the probe tap and the shadow march, and `sky_base`'s own demand lands on that peak; hoisted to just after `n` is finalised they die where they always died and a single `vec3` crosses instead. 96 to **80** with glass compiled out. **Step 2, glass -- and it only became tractable because Step 1 went first.** Batch 66 had found a *dual* ceiling: stubbing either of `shade_glass`'s two secondary arms read 96, both together read 80. After the specular hoist that collapsed to a **single** ceiling, because the hoist lowered `shade_hit`'s demand and `shade_hit` is one of the two arms -- so stubbing `shade_water` alone now reads 80. **`SPEC_GLASS_OVER_WATER` was therefore the whole remaining fix**, which is the suggestion batch 66 wrongly struck and the previous session un-struck. What ships is not a specialization but a replacement: water through a pane is now **analytic** -- the body colour, water's own Fresnel and a sky reflection off the face normal -- rather than a second complete inline of `shade_water` with its two `trace_world` marches and two `shade_hit` calls. **Batch 54 is the precedent and an exact one**: it built Snell's mirror by tracing a real ray, measured 4 ms, and found it differed from `underwater_body()`'s analytic colour by a max channel delta of **3 to 7**, because absorption converges a ray to that colour anyway. Batch 45's rule is the second half -- a transport destroys the detail a secondary ray buys, and this is water behind *one more* transport. **What it costs is the wave normal**: the analytic arm uses the flat face normal, so the sea through a pane has no wave detail. That is the one fidelity loss in the batch and it is ranked rather than asserted, at MAE **0.5990**. **A hoist of glass's own reflection above the dispatch was tried and rejected** -- it traded one live set for another and read 96, which is also the batch's clearest statement that NVIDIA allocates in blocks: shedding five registers does not cross a sixteen-wide step | **`resolve` 96 registers to 80**, 595,712 bytes to 396,544. **The win: +0.103 ms of `resolve` at `cave` and +0.399 at `coastline`** against `voxelcraft-pre67.exe`, a real revert binary, t = +4.30 and +4.96, 8/8 rounds both. Nothing distinguishable at `default` (-0.029 +/- 0.037) or at `glass` itself (-0.052 +/- 0.042) -- **the two vantages that gain have no pane in them and the one with a glasshouse gains nothing**, which is occupancy behaving exactly as batch 66 said it does and not a per-pixel cost. **19 of the 20 old vantages bit-exact**; `sky` moves **1 pixel at max delta 1**, which is D1's signature and is not D1 -- the A/B reproduces it **3 times of 3**, and the mechanism is the float reassociation the hoist introduces. `--no-sky-specular` is still a pure revert against `voxelcraft-pre65.exe`. **The twenty-first vantage, `glass-sea`, was added because the twenty before it were blind**: the analytic water arm moved **0 pixels at every existing vantage including `glass`**, whose own crop comment says a glasshouse "that ever landed on a shoreline would fail" its `Land` tag -- so the fixture had deliberately excluded the only configuration that could see this. Batch 52's lesson a second time, and the new camera reads **123,079 pixels at max delta 37, MAE 0.5990**, under [ADR 0001](adr/0001-no-elevation-form-factor.md)'s 2.87. Three neighbouring cameras read 0 before one worked. `validate` **clean, exit 0**, 21 vantages; `implies` 37 claims; suite green; `clippy` silent. **One guard fired and was right to**: `tests/glass.rs`'s `water_seen_through_a_pane_is_shaded_as_water` pinned the literal `shade_water` call; its *claim* survives -- the sea through a pane must still be water and never `shade_hit` -- so the assertion now pins `WATER_BODY`, `schlick` and the absence of `shade_hit` in that arm | [shading](shading.md), [water](water.md), [harness](harness.md) |
| 68 | **the roadmap drained, and an open entry recovered from a commit that deleted it** -- a documentation batch, no code touched. The file's own rule since batch 47 is *every entry here is open; a shipped entry leaves this file entirely*, and it had not been run for ten batches: **1,119 lines, of which ~320 were retrospectives on R1-R7, P8 and P13, all closed.** **The defect that made it a batch rather than a tidy-up is that the accretion had cost an open entry.** `P11`, the gather's data-dependent gate -- **0.351 ms of `resolve` at `cave`, measured in three builds, and the cheapest open decision in the project** -- was deleted by batch 67's commit while the header link to it survived. **Both in-file anchor links were dead, 2 of 2**, and four documents (`CLAUDE.md` twice, `shading.md`, `PERF.md`, ledger row 65) still said *roadmap P11 has the table and the open decision* about a section in no file. Recovered verbatim from `a1940dc^` and restored, plus two notes: batch 65's `SPEC_HI_MASK` makes its third option cheaper, and its closing paragraph addressed R3, which has shipped. **Two more drifts found by the same sweep**: the rung table still listed R6 as `open question` while two other places in the same file said it shipped at batch 65, and the header's sixth paragraph said *P13 is the register ceiling and P8's millisecond still taxes every reading* fifteen lines below the two paragraphs saying batches 66 and 67 closed both. **What made the drain mostly deletion rather than a move is the finding**: R2's, R3's, R4's, R5's and R7's retrospectives were **already in `lessons.md` verbatim**, so the roadmap was carrying a second copy -- and batches 66 and 67 were in the roadmap *only*, with `lessons.md`, the file `CLAUDE.md` routes to *when you plan a batch*, having zero mentions of either. **Three sections added there and nothing invented**: the register-count law (*ask how many times a function is inlined*, *ask where a value dies*, *a negative stub means not the sole cause*, *NVIDIA allocates in blocks*), R6's ceiling question and *reach for the curve's shape before its scale*, and the **MAE calibration ladder** -- seven reference points from 0.372 invisible to 5.98 shipped, which lived only inside R7's retrospective and is what ranks every future look batch. **The ladder is not monotone in what ships and that is the information in it**: 5.98 shipped and 5.7074 did not. **A new table replaces the 320 lines**, *Where a closed designation went*, one row per closed R/P/D letter pointing at its ledger row, ADR or `errors.md` entry -- because `CLAUDE.md` deliberately does **not** rewrite a designation in six documents when an entry ships, so the references had to resolve somewhere and now resolve in one hop. **A draft of the `CLAUDE.md` row transcribed that table's size as *sixteen rows* and it was caught by this project's own lesson** -- *a census that transcribes a table's size goes stale the day it grows*, batch 59 | **Docs only: `git status` is three `.md` files and no Rust, so there is nothing to bench and no control to check.** Suite **green, 6 of 6 in `tests/docs.rs`** including `every_relative_markdown_link_resolves` and `no_document_transcribes_the_test_count`. **Three verifications written for this batch and each caught something or proved something**: the anchor scan went **2 dead of 2 to 0 of 1**; a reference sweep over every `roadmap <designation>` in the tree went **6 unresolved to 0**, which is what added the `P2`, `P3b` and `P4` rows nobody had asked for; and a verbatim check confirmed all **six kept ranges byte-identical** to `HEAD` and P11 byte-identical to `a1940dc^`, so the drain moved no sentence it did not mean to. Roadmap **1,119 to 845 lines**, `lessons.md` **819 to 903**. `CLAUDE.md` CRLF/LF counts **603/10, identical to `HEAD`** -- the first edit silently turned one CRLF into an LF, which [`pitfalls.md`](pitfalls.md) is the standing warning about | [lessons](lessons.md), [roadmap](roadmap.md) |
| 69 | **`f16` packs on this driver, and the two capabilities that reach the register wall had never been asked for** -- roadmap **P14** opened, no renderer code touched. Batches 65 to 67 left `resolve` bound by its register count rather than by its arithmetic, and `CLAUDE.md`'s scarcity table answers that with rows that move *work* off the pass. **`f16` is the only idle capability that shrinks what the pass already holds**, and nothing here had ever asked whether it exists. `probe` gains four feature rows -- `SHADER_F16`, `SUBGROUP`, `SUBGROUP_BARRIER`, `SUBGROUP_VERTEX`, all **true** -- plus subgroup size (**fixed 32**) and `max_compute_workgroup_storage_size` (**49152**), and **it compiles a shader for the two that matter rather than printing a bit**, because *reports the feature* and *naga can lower it* are two questions and this file records the second going unasked for cooperative matrix. `shaderstats` gains `shaderFloat16` on its device, without which an `f16` module cannot reach a pipeline at all and the result would read as *f16 does not work* when it means *it was never enabled*. **The measurement is synthetic pressure at a real live range** -- `vec4` chains born beside `spec_pre` and consumed beside `spec_rgb`, crossing the nine-cell gather, the probe tap and the shadow march, which is the span batch 67 identified -- with the arms differing only in type. **At 32 live scalars `f32` reads 128 registers and `f16` reads 96**, two occupancy steps, which batch 66 priced at 0.44 ms at `cave`. **Two arms measured nothing and both are recorded in P14 rather than discarded**: a `sin()`-chained arm read 128 on *both* sides, because a transcendental widens to `f32` and the lever is therefore storage rather than arithmetic; and a pure multiply-add arm read **80 at every width to 48 scalars**, because the allocator rematerialised it past the gather instead of holding it -- an inert pressure test reads exactly like a clean negative. **What is open is the survey and it is the whole remaining job**: nothing here shows `resolve` *has* a step's worth of `f16`-safe live range, and `spec_pre` is 3 scalars. | **No renderer code changed, so no frame can move and no control applies** -- `git status` is two `src/bin/` diagnostics and three `.md` files, and `voxelcraft.exe` is in neither binary's build graph. **The SPIR-V was checked rather than assumed**: the `f16` arm carries `OpTypeFloat 16` and `OpCapability Float16` with 2 `OpFConvert`, the `f32` arm carries neither, so the saving is real 16-bit code and not a promoted no-op. The 32-scalar row reproduces exactly on re-run, compilation being deterministic. Suite green, `clippy` silent. **One claim in an earlier draft was withdrawn on measurement**: `resolve` declares no `var<workgroup>` and was called free scratch, but the driver reports **15,872 bytes** on it and the figure swung 2,560 to 14,848 across these arms with no relation to the shader -- it is driver-derived and P14 says so. `CLAUDE.md` CRLF/LF **603/10 to 616/10**, the ten bare LFs untouched | [roadmap](roadmap.md), [CLAUDE](../CLAUDE.md) |
| 69b | **both "untouched" rows on the scarcity table taken as far as reachability goes** -- a continuation of batch 69 in the same session, at the user's direction and against `CLAUDE.md`'s one-batch rule, which is recorded here rather than elided. **`CLAUDE.md` had asked one question explicitly and left it open for fourteen batches**: the adapter has reported `EXPERIMENTAL_RAY_QUERY` and `EXPERIMENTAL_COOPERATIVE_MATRIX` true since batch 55, and that file says in its own words that *whether naga's WGSL front end can express the operations is still the unasked question*. **It is answered and the answer is yes, both.** `probe` now compiles a ray query shader (`acceleration_structure`, `RayDesc`, `rayQueryInitialize` / `Proceed` / `GetCommittedIntersection`) and a cooperative matrix shader (`coop_mat16x16<f16, A>`, `coopLoad`, `coopMultiplyAdd`, `coopStore`, a 16x16 `f16` multiply into an `f32` accumulator). **And it goes one step past compiling, because a shader that names `acceleration_structure` proves naga can write the binding and not that the driver will build a structure behind it**: wgpu 30 built a real BLAS from **AABBs** -- three voxel-sized boxes -- with a TLAS instance over it, and the submission succeeded. **AABBs are the whole point and not an implementation detail**: this engine's first stated invariant is that there is no mesher and no triangle geometry anywhere, so `BlasGeometrySizeDescriptors::AABBs` is the only door an RT core has into it, and that door is open. **Three toolchain traps found on the way and filed in [pitfalls](pitfalls.md) rather than here**, because each fires in any project against these APIs: an `EXPERIMENTAL_` feature needs `experimental_features: unsafe { ExperimentalFeatures::enabled() }` or the device request is refused with a message that reads like the adapter lacking it; ray query and cooperative matrix each need their own `enable` directive; and `coopStore` takes the matrix first and the pointer second, reversed from what the name suggests, reporting an error about the *target* type. **What this does not do is make a case.** The argument in `CLAUDE.md` is unchanged and was never about reachability: an RT core is the right tool for a *bake* rather than the frame, since `tile_select` already broad-phases the frame's coherent rays for 0.13-0.17 ms; and every cooperative-matrix candidate named so far runs per frame, aiming at the one resource that is full. Both rows stay **nothing** -- now for a reason about value rather than about the toolchain | **No renderer code changed and no frame can move** -- `git status` is `src/bin/probe.rs` and three `.md` files, and `voxelcraft.exe` is not in that binary's build graph. `probe` exits 0 with all six of its compile tests passing and the acceleration-structure build reporting success through a popped validation error scope rather than through absence of a panic. Suite **green, 32 of 32**, `clippy` silent. `CLAUDE.md` CRLF/LF **616/10, unchanged by this commit** -- both edits were within existing table rows, so no newline was written at all | [roadmap](roadmap.md), [pitfalls](pitfalls.md) |
| 70 | **`idle_repaint`: a still frame owes nothing, so the window stops paying for it** -- the fork's batch, number taken after the merge because the main tree had already spent 67 to 69b, and this is the row that keeps that straight. It originates in the shore row of `PERF.md` rather than in a roadmap entry: 9.54 ms GPU against a 10.36 ms wall is 97 fps, 144 fps wants 6.94, and the FIFO clock is absolute -- a frame holding anywhere between 6.94 and 13.9 ms presents at 72, so any win that does not clear the whole band shows on no counter at all. **The observation the batch is built on is that the standing-still frame is the one the display has to show fastest and also the only frame a renderer can show for free**: `src/idle.rs` holds a token naming everything a fresh render can read that is allowed to have changed -- camera, `World::version`, the probe queue `sync_world` is about to drain, the app's relight queue, the pipeline words, the temporal mode -- and when it has not changed, `App::frame` asks `Renderer::repaint` for the present pass alone. **Bit-identical to re-presenting the frame it came from, by construction rather than by sweep**: the blit re-reads the texture slot the last render presented (`last_blit` is recorded in the present block), and no pass runs between the two presents, so the claim needs no vantage and `tests/idle.rs` is its verification. **The sun is the one authored motion that never stops and the tick is derived rather than chosen**: the batch-37 envelope's longest shadow run is ~600 blocks, `SUN_TICK_DEG` = 0.024 deg moves its edge 0.25 blocks -- sub-pixel at any camera that can see a run that long -- and a test pins the bound to the constant. **The sea and the deck are live by default and the token does not pretend otherwise**: with either speed non-zero the cadence caps at one repaint per render and any token change -- a step, a turned head, one placed block -- exits it that same vblank. **What was priced first and declined is the batch's honest half, and each refusal is arithmetic rather than taste**: a Fresnel-gated reflection skip (`F >= WATER_F0` everywhere and the shore is where F is largest), skipping the refracted seabed on blue extinction (0.020/block over a ~20-block shore path is transmittance 0.67, not the 1e-3 needed), a sky-ceiling early-out in `trace_world` (dead: the 16-iteration grid cap means a grazing miss never walks long), shadow-budget truncation (batch 44 already measured -0.019 +/- 0.056), and skipping renders *while moving* (48 Hz input latency in a fps counter's clothes). **What it measures is what the request named** -- the HUD's number while standing at the shore -- and the moving shore stays 97, said plainly because the ledger is where this project keeps the difference between the number asked for and the number bought | **Evidence class stated, not borrowed**: built in a sandbox with no GPU and no Rust toolchain, so no line here is a measurement. Bit-exactness is structural -- `frame_index`, the jitter phase, the history parity and the timer ring all stand still during a repaint, and the HUD is rebuilt each one so the counters stay live; the stats line annotates *idle repaint* and the GPU line *(last full render)*, because this engine's own definition of a confusing artifact is a 144 fps line beside a 10 ms GPU line. The fixture is untouched by a second construction: `classify` is reachable only from `App::frame`, which no capture, bench or journal path calls, so every vantage keeps rendering all seven passes per frame. No WGSL moved. The premise the token's bit-exact compare rests on is *tested against the real physics* in `tests/idle.rs`, dry and wading one deep, rather than asserted: ground velocity is written from the wish direction and the floor snap is a constant, so a settled frame reproduces itself to the bit. Predictions, in the spirit `PERF.md` keeps projected and measured in one table: a still frame drops from 9.54 to ~0.3 ms, pinning the HUD at the display cap; a presented frame after any motion renders as it always did. Acceptance on hardware: launch windowed at 1080p, walk to the shore, stop; expected reading is the line at the cap with the annotation on, and `--no-idle-repaint` returning the identical picture at the old cadence | [gpu](gpu.md), [harness](harness.md) |
| 71 | **roadmap D1 closed: the flickering pixel at `lattice` was the probe lattice's own toroidal alias meeting the unload grace, and the fix is an upload window derived from the alias distance, not a sort** -- the second fork batch, also renumbered on the merge. The entry's own discriminator had already cornered the channel (probe readers off: 0 of 40 A/A failures; on: 4 of 40), and the batch is the last step the entry named, done by reading rather than by experiment. **Three findings fell out of the code in order, and the third is why nobody had found it.** First, the bake is provably pure: per key it reads the generator, the chunk's own heights and one flag, single-threaded with fixed accumulation order -- and `heights_agree_with_the_generator` was already the guard -- so the per-chunk content never varied. Second, the collision the hash-order theory needs cannot happen in the steady state: only LOD-0 chunks bake, their origins are multiples of 64, the plan's LOD-0 reach is 160 blocks, and the lattice aliases at 512, so two chunks that own one texel block can never be wanted by one camera at once. **Third, the hole is residency, not the plan**: a chunk unloaded at a previous position survives 240 frames of grace in `World`, and a teleport -- or a capture vantage far from spawn, which is what `lattice` is -- holds two clusters 512 apart while the old one's builds are still landing, the drains over the settle pick a winner in rayon completion order, and one pixel on a rounding boundary flips by +1 on all three channels. **The fix is a window, and the arithmetic is what made it a window instead of a sort**: batches uploading within `UPLOAD_REACH` = 224 of one camera differ pairwise by at most 448, under the 512 alias distance, so their writes are provably disjoint and no order -- hash, timing or sorted -- can change the field; a sort would only have made the *wrong* winner deterministic. A bake outside the window is **deferred, not dropped** -- re-queued, uploaded when the camera returns, because dropping it would silently leave its region on the field's 1.0 identity fill forever, which is a different defect with no number. **The one interaction found late, with batch 70 in the same tree**: a deferred entry *sits in* `probe_dirty`, and the idle token's `probes_pending` reading it unfiltered would pin the repaint off forever at a spot where nothing will ever upload -- so the token counts only entries the window would upload now, which flips true on exactly the frame a deferred chunk comes back into reach, owing a render exactly then. `sync_world` gained its first argument: `cam` | **The claim's evidence class is construction again, stated as such**: built against the entry's entry-list rather than the machine, in the sandbox entry without toolchain or GPU, so the numbers are expectations written down beforehand. `tests/probe.rs` carries the invariants as they were derived: the window's own arithmetic (2R under 512-64 with the plan's 160 inside), a pairwise sweep asserting no two in-window chunks share a texel block, a guard-the-guard that the 512-apart collision *is* reachable outside the window, and the defer-and-survive doorway replayed without a renderer. Expected on hardware: D1's own A/A loop at `lattice`, 0 of 40 -- and the entry's own lesson applies to that too, a zero sampled once is not a zero, so 40 it is. Expected capture effect: none anywhere that did not already flicker; the merge keeps `validate`, `implies` and every vantage byte-stable by construction because in-window writes are unchanged when no collision exists, which is every camera the fixture can reach. **One methodology sentence the entry already wrote and the batch confirmed**: a single clean run of an intermittent process is worth almost nothing, and the entry had asserted one for two batches | [roadmap](roadmap.md), [shading](shading.md) |
| 72 | **roadmap P10 closed: a mixed root-FULL chunk marches as if its tree existed, and the index that was "impossible" turns out to have been in the tree all along** -- the third fork batch, on a defect whose reading was finished by batch 53 and whose fix waited for someone to notice that `leaf_attr` *solves it every frame*. The defect, restated: a 64^3 region wholly under the sea surface and holding the floor marks every 16^3 cell full, `make_root` collapses the run to `Inner::full(0)`, and the marcher's fast path hit the chunk's **face** for every ray -- including `resolve`'s refracted, reflected and shadow legs, which ignore water by definition. Batch 53 measured the footprint rather than guessing: **31,815 pixels at `coastline`, max delta 95**, 2 at `sea-horizon`, and it is exactly the mixed case -- a uniform full chunk is caught three exits earlier. Batch 53 also recorded why it stopped: "the loop cannot produce" the first dry voxel, because a full node's `run_ptr` is a sentinel. **What batch 71's kind of reading finds here is that the data was never gone.** Occupancy under a full root is all ones at every level, so no tree lookup is needed for *presence*; the attribute run went in dense -- 64 entries per 16^3 cell, `l1b * 64` -- which is the parameter `leaf_attr` and `world.rs`'s `l1_node` have *both* used since the tree was written, and `tests/full_march.rs` now pins the Rust and WGSL halves on one literal. The fix is therefore 20 lines: the fast path becomes `root_full && !SPEC_FULL_MARCH` for the old return, the loop's L1 load takes a synthesized `vec4<u32>(MAX, MAX, FULL_BIT, l1b * 64u)` under `root_full`, and the water, glass, foliage and cutout arms -- code the full path had *skipped* for twenty-one batches, including shadow-ray grass on packed jungle floor -- answer for it exactly as they do for a real full inner. A ray that was right before stops in its entry cell as before, because a full chunk is solid from its face to it. **The control is the second specialization word, second bit**: `FLAG_HI_FULL_MARCH`, keying the pipeline exactly as `FLAG_HI_SKY_SPECULAR` does, `--no-full-march` on the command line. **One leak on the way in, caught by writing the tests**: the march arm and `leaf_attr` have to spell the synthesized node identically -- `l1b * 64u` here and there -- or the marcher reads a neighbouring leaf's attr and the water test is wrong by four voxels with nothing in the frame to say so | **Evidence class stated, same as the fork's other two batches**: built in the sandbox without toolchain or GPU, so nothing here is a measurement and `cargo test` has not run -- every check the batch claims is a static one. `tests/full_march.rs` carries four: the state machine (water over stone collapses to a full root with a 32-of-64 dry mask), the two-file formula pin, the override reaching its return in batch 38's measured shape, and the flag parsing. Two `const _: () = assert!`s pin both second-word bits into `SPEC_HI_MASK`. **Where the acceptance protocol has to read the frame itself, written down beforehand**: `implies` under default against `--no-full-march` -- predicted **bit-exact at every vantage under the control and exactly only the mixed-full footprint different under default** -- batch 53's table plus, by the same mechanism, wherever upstream's batch-67 `glass-sea` vantage reaches a mixed full chunk through its water-skipping legs, a camera that did not exist when the table was measured; `validate` against `--reference` at `coastline` -- predicted **MAE rises, not falls, and that is the fix reading correctly**: the reference converges the *old* geometry at those pixels, so the fix differs from it by design, and the acceptance is that the moved pixels are the footprint batch 53 measured and not a scatter. `resolve`'s cost row on hardware, where a full-chunk march replaces a face hit only for the legs that were wrong -- predicted under +0.1 ms at `coastline`, zero where no full chunk is in a secondary ray. One disclosure, because this engine counts its iterations in a debug view: the fix path's hits carry `h.iters` counted through the loop where the face hit carried zero, so the `--march-stats` heatmap *moves* on mixed full chunks -- the frame channels are untouched | [roadmap](roadmap.md), [water](water.md), [tests/full_march.rs](../tests/full_march.rs) |

| 90 | **the look-roadmap's first four entries built at once, each behind its own flag and each default off** -- A1 (canopy lift), A4 (glass's traced reflection), A9 (caustics) and A10's curve arm, on the user's standing directive that every open look entry get built dark for one later verdict pass rather than gated one-at-a-time on eye readings. **A1 is pure data**: `ShaftField::update` adds the authored lift inside exactly the columns the tree placer gates admit (biome tree scale, above the beach, under the tree line), one expression, no shader, and at 0.0 the field is the pre-arm one bit for bit -- pinned as such in `tests/batch90.rs` including scrolled fills, because a half-applied arm leaks through carried texels first. **A4 is one march**: `shade_glass`'s sky-only `refl` swaps for `water_reflect_leg` -- the function keeps its by-now-generic-but-misnamed name deliberately, and its alpha-is-zero miss case falls back to the sky the arm already computed so no hole appears at a high window's fallback row; legal there by the recursion note at the call site. **A9 is pure ALU**: sun sheets on the flooded floor, woven from the wave field's *own* first two deep-water octaves read at the sun-projected waterline point -- same anchors, same slopes, same phase -- so the caustic is what the surface above shades, refocused, and a flat sea or `--anim-rate 0` folds the multiply to identity rather than freezing a ripple mid-air. Depth window, the `sky_sm`-carrying `direct`, and the bit nested under `sea_level > 0` beside the batch-75/81 trio. Two octaves rather than the schedule because the pattern is woven interference by physics and the third train is below the filter. **A10 is a uniform, not a pipeline**: the blit's filmic shoulder becomes a one-word selector in `BlitUniform` -- the KNEE hyperbola every constant was tuned against, against Narkowicz's 2015 ACES fit per-channel. Not AgX (the per-channel-matrix prescription is the follow-up this arm's reading decides) and not a retuning (that batch stays open under the entry's own name); an unrecognised spelling falls to `knee` rather than running anything silently. **The batch's worth-keeping convention is the bit ledger formalised**: look arms are FLAG_HI/SPEC pairs in the second word (64 and 128, freshly claimed), a data-only arm takes a config value, and an off-pipeline arm takes a runtime uniform | **Bit-exactness with all four flags off is construction, stated as such**: each SPEC arm folds the arm's code out of the compiled pipeline entirely, the zero lift folds to the legacy expression, and the blit word sits at its tuned value -- batch 53/73's pattern, applied four times to four shapes of arm. On top of that, what *did* run: the full suite green except the known pre-existing docs-link red, `tests/batch90.rs`'s pins (defaults-off table, parser spellings, bit allocations, zero-lift legacy field, lift-exactly-on-placer-columns), plus lavapipe renders at 1600x900 -- shaders Arms A/B read **MAE 0.005 over 1.2% of pixels** for A1+A4+A9 alone (bounded, where the entries said it would be) and **MAE 10.58 full-frame** with the ACES arm on, which is what a curve arm must do. Frame evidence ended there for the sandbox half, and **the hardware round then landed it**: control `--no-water-sec` bit-identical to `46f88cf` at all 21 vantages, build unpatched, suite 275/3 with the only new red a census this batch was handed rather than owned (converted to property assertions in batch 90b, `tests/spec_hi.rs`). **A4 is the round's win** -- MAE 5.47 at `glass` (148,496 px, max 146) against the 5.98 shipped-rung ceiling, for +0.043 +/- 0.020 ms of `resolve`. **A10 moves the whole frame for nothing** (921,600 px at `sky`, max 29; blit uniform) and is a pure eye verdict, weighted by the standing "too dark" judgement since the shoulder pulls down. **A1 fires below the invisible rung** (MAE 0.054 at `canopy` at lift 8, 21,682 px at max 24) and wants its lift sweep. **A9 measures zero by form, not plumbing** -- 115 px at max delta 1 at `coastline` where the window opens, 0 where it does not, *worse* under `--anim-time 3.0` -- and is filed **diagnosed-not-built** under batch 90b's new acceptance rule (an arm moves pixels at a named vantage past max delta 1 or it is not built); the mechanism (quartic carrier in `wave_amp`, lambda-32 trains the wrong scale at any form) is in [water.md](water.md), and `CAUSTIC_GAIN` tuning is proscribed there as P-A's failure shape. That makes two zero-measuring flagged arms in the tree (P-A, A9) and the rule is what stops a third | [roadmap](roadmap.md), [sky](sky.md), [water](water.md), [shading](shading.md), [tests/batch90.rs](../tests/batch90.rs) |

| 91 | **roadmap A5 and A2c, both armed: the canopy's LOD cliff thinned generator-side, and Snell's window filled from the marcher** -- same commit, two arms, both default-off under batch 90's standing law. **A5 (`--tree-blue-noise`)** is the research's cheaper-than-free fix: -- `--tree-blue-noise`, default off, batch-90's standing law held. The defect is a *permeability* cliff rather than a shape one: LOD 0 carves a leaf block to `LEAF_FILL` of its 4^3 (porous canopy), and `coarse_trees` was painting the proxy solid over the same canopy one level out, so a cross-fade *partitioned* rays across a halving of how much sky the tree lets through. Batch 38 gave the still-frame bound (2,508 px of 921,600 at `lod`, max 16, one bbox) and named what it could not cover -- motion through the band, which no fixture camera makes. **The arm is the research's priced pick, not the design I would have guessed**: no floats, no new sampler, nothing entering the march -- a 3D R3 rank field (generalized golden ratio, `phi_3` = 1.4656) evaluated on the coarse voxel lattice with a seed phase, keeping a cell when `rank < COARSE_CANOPY_KEEP`, which is *defined* as `render::LEAF_FILL` and compile-time pinned to it, because the entry's own claim (mean transmittance matches LOD 0) is a statement about that one constant being shared. Blue-noise rather than hash for the reason the entry says: white noise thins to the same 62 in **clumps**, and a clumped hole is the cliff again a level finer. Trunks are explicitly not thinned (LOD 0's are not porous); the off world is the legacy world exactly; and the priced alternative it rules out is the fractional-opacity scalar at +180% to +250% per coarse ray. The price tag itself -- **-40% per coarse ray**, a thinned mask skips rather than marches -- is now a bench the hardware round can read | **Evidence class stated, the batch's second-consecutive sandbox constraint**: written against the tree, parsed by rustfmt, but **not compiled here** -- the sandbox box currently offers ~0.75 GB free RAM and `rustc` is SIGKILLed compiling `ash`, so the green run belongs to the user's machine alongside the acceptance sweep. `tests/worldgen.rs` carries the arm's behavioural contract: flag-off proxies are solid, armed thins *only inside* cells the legacy proxy would have stamped (the A/B discriminant that requires no harness), trunks identical, survivor share near `LEAF_FILL` on a wide binomial window, generation deterministic, and a clustering reading that cannot pass with white noise in place of the R3 field. **Batch-90b's acceptance rule binds before any tuning**: the arm is not built until it moves pixels at `lod` -- and at a camera *crossing* the band -- past max delta 1, both numbers the entry owed footnotes to already. Censuses this round were already clean: no flag bit to take (data-only arm), no help-text row missed. **A2c (`--snell-bend`)** is the other arm in the same row, and the one the user flagged *measure hardest*, because it is the first batch-90-family bit the **marcher** reads. The arm: for a submerged primary ray that would cross the sea plane, split the path analytically at `t_surf` and re-march the transmitted half refracted by the flat boundary -- which builds the compressed-hemisphere filling batch 54 left as architectural, while **leaving `resolve` untouched by construction**: its cone, Fresnel split and mirror read `rd` and `dist`, both preserved by the bend as `t_surf + t2`; a TIR ray or a bent miss keeps exactly the pre-arm `h`, so the arm can only ever *replace geometry inside the window*, matching the entry's own ranking (a distortion, never a presence). The one worn approximation is labelled in the shader and `water.md`: flat bend facet against the waved Fresnel normal, a subset of the mismatch already shipping everywhere -- with the wave-field migration to `common.wgsl` as the named successor. `SURFACE_EPS` moved to `common.wgsl` as its first consequence (one definition, both files, pinned in tests). `tests/batch91.rs` pins what a source suite can: the flag, the bit (=256), the dark override default, the zero-footprint-in-`resolve` purity, and the single epsilon; the word's own census grew its **one** sanctioned line in `tests/spec_hi.rs`. **Hardware protocol, written down as the user asked for it**: control 21/21 off by construction; acceptance rule (named vantage, past max delta 1 · predicted inside-window pixels at `deep-water` and `underwater`, with `--reference` MAE allowed to *rise* by design since the reference converges straight geometry); `march`-row bench submerged vantages spec on/off; and the **register read batch 54 said the march-side version would owe** -- `march`'s registers against the spec, since a second inlined `march_chunk` is a shape the allocator has lost on before | [roadmap](roadmap.md), [lod](lod.md), [water](water.md), [tests/worldgen.rs](../tests/worldgen.rs), [tests/batch91.rs](../tests/batch91.rs) |
| 92 | **roadmap A8's six pieces triaged: three armed dark behind their own flags, three answered without code** -- the batch's shape being the entry's shape: A8 was a list, not a mechanism, and the user-approved batch B plan said build the three flag-arms, annotate the three that are not arms. **Piece 1 is `shaft::TEXEL` unlatched** (`--shaft-texel N`, clamp 1..=64): batch 35 pre-designed the arm -- the scan, the sampler and the upload read `frame.shaft_texel` from the field's first day, so the knob costs one CPU struct field, three construction sites, three frame writers and a clamp, and **never touches the shader or the second word** (data-only uniform class, same as A1). `DIM` stays 512, so a finer texel is a sharper edge AND a shorter window, and the tests pin exactly that the fill's centres and `envelope_reference`'s climb follow the knob rather than the constant -- because the reference is what the GPU scan is validated against, and a stale climb sweeps the *wrong function* green. **Piece 3 is the palette arm** (`--tint-balance`, FLAG_HI_TINT_BALANCE 512, the word's first constant *swap* rather than new code): the complaint read backwards off the table -- desert and tundra take almost no tint because their surface layers carry alpha 0, plains takes none by construction -- so the arm is two halves under one bit: `TINT_PLAINS_BALANCED` selected through `SPEC_TINT_BALANCE` inside the existing `SPEC_TINT` gate (off = naga folds the select = bit-identical module; on = same instructions, other immediates, *no register story* in the pass whose batch-67 entry prices registers), plus atlas masks on sand and lichen at `build_atlas` time -- the masks being *content* no pipeline key could switch. The accepted spill is written into the arm's comment: a mask is not biome-aware, so every biome's beach sand takes its local tint; whether that reads as detail or staining is the by-eye verdict the entry owes. **Piece 4 is the meadow's middle loose end** (`--meadow-side`): **one id and one atlas slice**, exactly as scoped by the entry and not one sibling more -- `MEADOW_SIDE` is appended per the ids rule (23, so `def`'s clamp lands grassy in either table generation), its side tile is the grass-side under the same `MEADOW_SHADE` scalar (fringe drawn with the top's recipe and mask -- *amended in row 94: the mottle is per-layer,
 nothing is byte-equal across slices* -- dirt untinted), the stamp is one ternary inside `coarse_meadow` with gates, cover curve and coin shared, and a four-chunk sweep pins that an armed world differs from the legacy one in *which* cube lands and never in *whether one does*. **Pieces 2, 5 and 6 are annotated, not built**, each with its reason: edits-don't-cast needs journal deltas joined into the fill **and** a capture built to see it -- a fixture dependency, not a flag; coarse ground cover is an accepted limit (what exists at stride 2 to paint is a colour question, the standing meadow answer); and the heightfield envelope is a *redesign* -- batch 35 rejected the exact march at +10.57 ms on a 4 ms frame, and an arm re-legislating that finding would be P-A's failure shape. | **Second-consecutive sandbox evidence class, stated as such**: written against the tree, parsed by rustfmt (`tests/batch92.rs` is rustfmt-canonical), **not compiled here** -- the green run belongs to the hardware round with the batch-91 arms. `tests/spec_hi.rs` grew its one sanctioned line; the three flags landed help rows; the bit ledger stands at 512 spent, next free 1024. **Acceptance, pre-written for the hardware round**: all three arms default off so the control is construction; batch-90b's rule binds before tuning -- piece 1 moves pixels at a low-sun ridge vantage across its texel ladder, piece 3 at desert and tundra ground, piece 4 at a steep coarse slope in the `lod` class, each past max delta 1 or it is not built | [roadmap](roadmap.md), [sky](sky.md), [shading](shading.md), [terrain](terrain.md), [tests/batch92.rs](../tests/batch92.rs) |
| 93 | **no code -- the chat queue, banked.** The user closed batch B's stewardship loop by naming what had drifted: the sibling sessions had been managing a queue with designations that existed only in chat -- "P-E held pending L5", "P-B unbanked", "P-A at #3, phases A-D" -- and **none of those letters existed in `docs/roadmap.md`, although the work two of them named is in this tree's git history.** The batch is the filing, and it is the smallest batch this season by intention, because the user's ask is one sentence: **a designation must not be used in a status report before it is an entry in the roadmap** -- now written into `CLAUDE.md`'s docs table as the converse of the rule already there (*a decision filed under a batch number is a decision nobody can find*; a chat log is worse, it is not even grep). **P-E is now an entry**, placed on L5's flank because its whole existence is L5's: the probe bake measured by the sibling fork at **13.9 ms of an 18.5 ms chunk build**, single-threaded on the main thread, parallelizable per slab across the idle rayon workers with the *edit rebake* as its first named consumer -- and **held, on the queue's own agreement, because it is the expensive answer to a question with a one-constant answer**: `SPACING` 8 costs **3,256 us** and there is no latency problem to parallelize away, while spacing 4's picture price is measured on the user's hardware at **MAE 0.61 at `canopy`, under the by-eye bar at every camera**. If L5's eye verdict kills the lattice, P-E closes unbuilt with it. **P14's open question is answered inside P14**: the survey the entry demanded ran as P-A (commits `f620c7e`..`4e48d71`, built and un-shipped) -- **11 `f16`-tolerant scalars, 80 registers both ways, bit-exact at 6 of 6 vantages, bench ±0.007** -- and the close is *measured zero*, with the mechanism question it owed written down where the next park will find it. **The closed-designation table takes three new rows**: P-A (closed, never was an entry), P-D (the probe-bake cache, spent at `d4701a3` on the pure-function argument), and P-B plus every further chat letter, **burnt** -- a letter with two histories in chat resolves to neither, so do not reuse it; a claim re-presented with content writes a new entry instead. **What this does not settle**: the claims in recent reports that have no home beyond grep -- offered by the user as the follow-up | **Docs-only batch: `git status` is four `.md` files**, no shader source, no test source; nothing in the frame can have moved, and no control applies. `CLAUDE.md`'s line endings were re-counted in full: the upstream "616/10, ten bare LFs untouched" invariant did not survive the fork -- the file is **0 CR, 712 LF** -- so the row stops counting an invariant that no longer exists and says so once, here | [roadmap](roadmap.md), [CLAUDE](../CLAUDE.md) |
| 94 | **the fork's stop condition: four compile errors, twelve red tests and one 865,982-pixel regression repaired before another entry is taken** -- the hardware round at `309ab8a` ran batches 90b-93 and found what the sandbox's evidence class said it could not: batch 91's tree did not compile (`an id and its table row are one list kept in two places`, now in CLAUDE.md's invariants with const asserts pinning the tail row and the lamps' light channels above it), and three batches of tests had been filed green from a machine where `rustc` is SIGKILLed before the crates graph links. **The lamps regression is reverted**: `MEADOW_SIDE`'s *id* was appended at 23 as the rule says but its `BLOCKS` row had been inserted at position 20, re-naming every batch-59 emitter one id down (`lamps` read it at 865,982 px before a screenshot caught it); the row moved to the tail and five const asserts now make the id/row pair unbuildable to drift. **The four compile fixes are the user's own, applied verbatim from the session handoff** (`as f32` on `canopy_rank3`, `shaft_texel` hoisted the way `taa` is, `format!` for a loop variable, `lod: u8` at `ChunkKey`) -- the sandbox can carry text, it cannot carry a build. **A8 and A5 are not filed as built, because their own tests said so**: A8's premise test pattern-guessed WGSL spellings that exist nowhere (re-written to the real `span * frame.shaft_texel` and `/ frame.shaft_texel - 0.5`), its discriminant trusted four hand-picked columns that were all fluke-flat down-sun in seed 41 (now a 8..504 grid sweep, step 11 by 13, ranked labeled-"break" first), and its texture test demanded the side tile byte-equal the top one, which the engine's per-layer hash seed makes false by design (now a structural pin: fringe band tint-masked and in the `MEADOW_SHADE`-scaled grass range, dirt below in its own, and `MEADOW_SIDE`'s perm class is `FLIP_U` -- `GRASS_SIDE`'s class for the same banded reason -- with the repeat census counting the *property* `none == COUNT - 9, flip == 2`). A5's pair counter ran *vertically* (`mz` passed as `y`) and measured the axis whose margin was 0.008; the batch measured all three axes of the generator's own field (x 0.392, z 0.279, y 0.637 against the coin's 0.449) and pins z < 0.32 with the other two axes held as *properties* -- y re-correlating above the coin is pinned loud, since it coexists with the transmittance mean the arm actually keeps. The pane census learned its two-class shape instead of a count: the fourth `march_chunk` site is batch 91 A2c's bent second segment, which is camera-ray class and *must* stop at a pane, so the guard now reads two primary, two secondary, and the boolean is the claim. **The hardware numbers are filed at the entries they belong to**: P12's verdict is **GO** (water-crop MAE 0.75/0.25/0.09 against a converged 256-sample reference, worst 0.6784 MAE / delta-33 at `terraces` against the 2.87 bar; -4.603/-4.317/-4.647/-3.863 ms at the four water vantages, ~90% of the fork's advantage; the entry's 8.2 ms ceiling superseded by the measured 5.0-6.8, recovery band 2.5-5.1 -- registers unmoved: `resolve` 80, `water_sec` 72, `march` 72), and L5's cost is now one table (13,915 vs 3,256 us bake, 18,514 vs 7,824 us build, 96 vs 12 MiB, RSS 501 vs ~217 MB; keep case is motion stability only, eye verdict still pending, and a revert pays 10.7 ms/chunk and 84 MiB while dissolving P-D and P-E with it). The batch-90 arms on hardware: A4 is the win (MAE 5.47 under the 5.98 rung for +0.043 ms at `glass`), A10 free, A1 fires but is near-invisible (MAE 0.054 against its 0.372 rung; sweep the lift), **A9 is a no-op machine** (0 px at `terraces`, 115 px max-delta-1 at `coastline`; it owes a mechanism for why it draws nothing before any tuning). The five asks are ruled on in CLAUDE.md's new session-rules subsection (evidence class, the mtime trap -- synced trees arrive with 1980 timestamps and `cargo` does no work, so `touch` the tree before a round's first build -- one runnable commit per WGSL/binding/layout/table change with its four-step gate, built-means-pixels, property-not-count censuses, cumulative state at hardware rounds). Two pre-existing failures are *not* this batch's to fix and are recorded, not touched: `every_relative_markdown_link_resolves`, `transitions_are_symmetric_and_never_uncover`. Evidence class of this row's repairs, stated: written against the tree, each edit anchor-verified, **not compiled in the sandbox** -- the gate described in CLAUDE.md is the user's to run, and batch 95 opens only on its green. | [CLAUDE.md](../CLAUDE.md), [roadmap](roadmap.md), [terrain](terrain.md), [tests/batch92.rs](../tests/batch92.rs), [tests/worldgen.rs](../tests/worldgen.rs), [tests/glass.rs](../tests/glass.rs) |
| 95 | **the goal pivots -- "i want my game to lean with that look" -- and the first face is sky & grade, built dark behind five doors** -- the user restated the goal 2026-09-16 against two modded-client frames (filed at `docs/look-reference-river.png` and `look-reference-bank.png`), the day batch 94 closed the perf queue head (P12's GO, the large-water complaint unreproduced at 120 fps in play), with the contract answered the same day: **look first, performance reclaimed after** -- no fps floor while arms land, every arm still measured so the reclaim pass knows what each cast cost -- and textures may be *richer procedural* (more atlas slices, species variation; still generated in `textures.rs`, CLAUDE.md's asset rule stands). **G1's five arms**: `--grade none|warm` (a luma-preserving cast on the blit selector's spent padding word -- the A10 one-uniform rule; brightness is not what it moves, so no lighting constant re-tunes) and the **sixth four-word frame append** -- `--cloud-patch` (cumulus arrive in *banks*: one coarse tap at `CLOUD_PATCH_SPAN`, 6x the deck's span, swings the coverage threshold by up to the full noise range and rides the deck's own drift so geography stays one field), `--cloud-relief` (sun-side cap-vs-belly shading on the flat deck; **shading only** -- `dens` is final before the block runs and the batch-95 test pins the ordering, because a shading term that adds density grows clouds the control cannot see), `--haze-warm` (the warm band over the treeline, added in `sky_base` so a far ridge picks it up through the air genuinely in front of it -- the batch-10 argument one level down), `--zenith-deep` (multiply-only, weighted by the gradient's own exponent). Every arm defaults to 0 and is a *guarded branch*, never a multiply-by-zero -- batch 14 priced code compiled into the pass whether it runs -- so the control frame is the pre-95 frame bit for bit, which is the property the by-eye pass actually needs. The append takes 272..288, the matrix 288, the struct 352, offsets pinned like the five histories above it. **Evidence class, stated as the batch-94 rule now requires**: written against the tree, every WGSL guard string verified as written (grep, not guess -- the a8_1 lesson), not compiled in the sandbox; the gate is the user's per the 95a/95b commit messages, and each knob owes the batch-90 law its pixel-move proof past max delta 1 at a named vantage before it counts as built. The goal's queue is the G-series in the roadmap, each with its pre-written reason to wait: G2 (ground-cover density) **must first say what A5 got wrong, not build past it** -- A5's own tests said at batch 94 it is not built -- and it is the batch the richer-texture sanction and the block-table rule both bind; G3 takes colour-and-content for water (depth-graded absorption, the shore band) now that the trace is cheap, with A9's zero staying out of it; G4 sweeps A1's invisible lift and A8's texel knob together, and pins the references' *season* first. | [roadmap](roadmap.md), [sky](sky.md), [CLAUDE.md](../CLAUDE.md), [tests/batch95.rs](../tests/batch95.rs) |
| 96 | **G1's first gate: built and rendering, the control repaired to bit-exact by construction, and three test files told to state properties** -- the hardware round at the 95c tree found naga quiet and no NaN field (neither of the two sandbox-flagged typos bit), 303 passed, and a red set worth reading one failure at a time. **The control miss was real and is closed**: uniform-guard branches moved 7 of 21 vantages 1-4 pixels at max delta 1 -- reassociation in the arithmetic *around* a branch the compiler cannot remove, batch 14's "code compiled in is billed" law one level down: it also rearranges what surrounds it. The four sky guards now fold through one `SPEC_SKY_LOOK` override (`FLAG_HI_SKY_LOOK`, bit 1024, -- the tint arm's class; knobs stay uniforms past the fold so the 0.3/0.5/0.7 ladders cost no rebuilds), the spec_hi enumeration grew its one sanctioned line, and the batch-95 property tests were re-worded to pin the *fold*, not the guard shape that failed. **The arms measured at deliberate top end, all four plus the grade at once**: MAE 18.41 / 11.11 / 8.88 / 8.09 / 3.51 at `sky` / `canopy` / `coastline` / `low-sun` / `default`, max delta 100-141 over ~921k px -- two to three times the 5.98 shipped rung, which is what a sweep's top end is for; the frames read closest to the references at `coastline` and `canopy`, and the knobs doing the vista work are `haze-warm` and `cloud-patch`. **The pane census stopped counting**: its integer was true in neither tree (the P12 fork marches its own secondaries, a fifth site), while its property held in both -- camera-class sites (`cam_pos`, and A2c's `h2` segment) stop at panes, every helper passes through; the test now asserts exactly that and no total. **`lod_stream` earned "flaky once in two runs of one binary"**, and 95d made the next flake self-describing (first-wakeup frame index plus pending) rather than trusting or distrusting the file wholesale. **A5's two tests remain red even after batch 94's accounting** -- transposition and per-axis analytics did not cure them, so A5 stays un-built by its own tests' verdict and the failure text is owed by this column; G2's first question is thereby sharper, not softer. The round's honest note filed at the entry: the references' other half is geometry and texture density -- G2's sentence, not a sky problem. | [roadmap](roadmap.md), [sky](sky.md), [tests/spec_hi.rs](../tests/spec_hi.rs), [tests/glass.rs](../tests/glass.rs), [tests/lod_stream.rs](../tests/lod_stream.rs) |
| 96b | **the re-gate's two red threads repaired: the pane census learned to look backwards, and A5's meadow bug was real -- the arm had secretly re-contracted the meadow pass** -- the round's 21/21 bit-exact control and zero-patch build (the first handoff needing none, the element-wise const asserts upstreamed at `1a481da`) stood, and the five reds resolved as: pane (classifier, ours not the fork's), A5 x2 (one engine bug, one test-geography bug), plus the two pre-existing failures that stay untouched. **The pane census now classifies with a 600-char window *preceding* each match**: the failing call was our own `march.wgsl:66` `h2` site, and the scanner had sliced the call text starting at the match so the `let h2 =` that marks A2c's segment camera-class never entered the checked string; the property (camera stops, helpers pass) held in both trees all along, the integer was noise. **A5's `3 -> 19` is filed as a genuine engine bug**: at LOD 2 a tree stamp is one leaf cell directly above its surface column, `coarse_meadow`'s gate asks "cell above == AIR?", the rank arm thins that proxy, and the meadow stamps under the very tree the arm thinned -- two consumers of one cell, the arm re-contracting the second. The repair threads a 32 KiB u64 bitmap of rank-rejected proxy cells from `coarse_trees` into `coarse_meadow`, whose gate now reads *structural* canopy; unarmed the bitmap is all-zero and the unarmed world is bit-identical by construction -- a side bitmap, not a changed world. **A5's `too few pairs` is filed as test geography**: batch 94's transposition fix relocated the fixed grid into a near-treeless 1 km slice of seed 90210 (337 pairs where a clustering test asks four digits per axis), so `coarse_keys` now *locates* the canopy -- a bounded deterministic 12x6 window scan, ranked by leaf count, four fattest chunks pinned -- and the z-bound moved from the slice-calibrated 0.32 to coin-relative 0.8x so a different canopy window keeps its margin; a window with no canopy now names the tree placer as the finding. **The user's look-gap question got its own document** (`look-gap-analysis.md`): six captures against the two references, nine differences ranked by cost-to-close, with sky tonality and the blit grade at the cheap end, animated-ripple water colour/absorption and ground-cover species richness as the highest vibe-per-change middle, and contact shadow plus cumulus volume at the expensive end. `transitions` asymmetry read 2.94x this round (was 5.81x) -- variance across builds, watched, not touched. | **Same evidence class as all sandbox-round repairs: written against the tree, anchor-verified, not compiled here; the four-step gate is the user's** -- `git diff` shows exactly `worldgen.rs` (bitmap + gate), `tests/worldgen.rs` (sampler + bound), `tests/glass.rs` (context window), so the review surface is the whole claim; acceptance is pane green, both A5s green, control still 21/21 (the bitmap is all-zero unarmed: re-ran the reasoning, not the frames) | [worldgen](terrain.md), [look-gap](look-gap-analysis.md) |
| 97 | **"let's do all 5": the look-gap list becomes five flags, every arm default-off with the control as construction** -- the user read `docs/look-gap-analysis.md` and directed: build the cheapest five as flags-only arms, no knobs beyond each flag itself, and test them one at a time after the batch. **97a `--sky-cool`** (SPEC_SKY_COOL, bit 2048) is the tint-balance class one level up: constant swaps on the sky gradient's day pair and the deck's lit/base pair, folding dead off -- the references' cornflower midday against our violet drift, same instructions, other immediates. **97b `--grade cine`** needs no bit at all: the blit selector's spent pad word takes value 2 -- S-curve micro-contrast, 12% desaturated mids, cool shadows against warm highlights, all riding batch 95's luma pivot, and the three BlitUniform writers stop spelling casts (`cfg.grade_value()` is the one constructor, pinned). **97c `--water-look`** (bit 4096): the references' loudest cue as three guarded terms -- animated metre-scale ripple gradients into `nw` only (the traced `nt` never moves, so a look arm cannot become a perf arm), murk absorption graded by the column the refraction leg already paid to trace, and a 45% turquoise bank band below 2.0 blocks, placed before foam so it can never tint it. **97d `--foliage-rich`** (bit 8192) and **97e `--canopy-relief`** (bit 16384): one joint commit, one albedo law -- hashed species variety (straw stands at <0.16, flower pink at >0.97, 0.88-1.10 jitter) on cross-quads and grass/meadow tops, and deck-noise clump grain with sun-facing modulation on leaves; worldgen never moves, pinned by the test that `worldgen.rs` contains neither flag's name (the sanctioned richer-procedural class run shader-side). The word's whole 97 allocation landed with 97c since mask and enumeration must stay self-consistent; `SPEC_HI_MASK` grew four bits to 2048..16384, the constants table grew four entries, `tests/spec_hi.rs` its four sanctioned lines, and the frame struct did not grow a byte. **The user's test protocol is the batch's shape**: five flags, default off -- `cargo run` is vanilla, each `--flag` is the edited version; the by-eye verdict per arm comes back and the arms the eye rejects get swept or scrapped without a revert hunt. New tests: `tests/batch97.rs`, ten tests. | **Sandbox class, stated per CLAUDE.md**: written against the tree, every WGSL guard string read back out of the file it was typed into (the a8_1 rule), two near-misses caught in this batch's own review -- a `\`-continuation typo in a help row and a `git add -A` that silently staged `uploads/` -- both repaired before commit; **not compiled here**, naga validates at device creation, so the gate is the user's: the expectation is build-and-run with the four flags off for the control, then one flag at a time for the five pictures | [look-gap](look-gap-analysis.md), [roadmap](roadmap.md), [batch97](../tests/batch97.rs) |
| 98 | **the lookbook (`--lookbook DIR`): every look setting at once on three views, composed into one labeled PNG the user hands to an AI** -- the batch-97 verdict tooling the user asked for directly: *"a test of images where a bunch of different settings are used at once including fog and godray strength if its still there"*. It is there -- both of them: `--fog-density` (default 5.0e-4) and `--godray-strength` (default 1.0, with 0 as batch 11's own built-in control), so the matrix brackets both with FOG-LOW/FOG-DENSE and GODRAYS-HALF/GODRAYS-OFF. **Three views off the fixture's own cameras** (batch-22 law: verified content, not a guess): SEA-SUNSET = `coastline` at 0.74, SUNRISE-MOUNTAINS = `lod` at 0.26, MIDDAY = spawn at noon 0.50. **Twelve combos**: the five batch-97 arms singly, the G2 pair together, ALL-FIVE, and the four strength tiles. BASE is the control, pinned as a property. **Mechanics**: `screenshot()` split into `capture() -> Option<Vec<u8>>` plus a save wrapper -- a verbatim move, the 21-vantage control's path, so the commit claims structure and nothing else; the lookbook driver runs views x combos through `capture` over config clones, clamps `hud` off and `reference` 0 (a sheet of reference frames answers no question the user asked), and composes through `src/lookbook.rs`'s GPU-free functions. **Labels are burned into the sheet itself** (hand-tabulated 5x7 font, group strips plus per-tile strips, tiles downsampled by 2) because the reader is an AI and a sidecar manifest is a file it never receives. The module keeps every table and every paint function device-free, so `tests/batch98.rs` pins the whole shape of a run without a GPU: dimensions, distinct labels, BASE-is-control, every other combo moves something and only look fields (Debug diff over sanitized configs), views-as-cameras against the channel whitelist, font coverage for every fixed label, compose layout arithmetic with exact downsample means, the mode parse, and the driver's two clamps. **One file out: `DIR/lookbook.png`**, four tiles abreast, three groups stacked -- e.g. 2560x1770 at the default 1280x720 capture. | **Sandbox class**: written against the tree, anchor-verified, two own-review fixes before commit (self-referencing `voxelcraft::` inside the library corrected to `crate::`, a label-strip centering that bled one pixel into the tile image); **not compiled here** -- the gate is the user's, and it doubles as the hardware run: `cargo run --release -- --lookbook out` produces the sheet and the by-eye verdict comes back to me naming which arms ship, sweep, or scrap | [lookbook](../src/lookbook.rs), [batch98](../tests/batch98.rs), [gap list](look-gap-analysis.md) |
| 99 | **the round's verdicts processed and all four reds closed -- every one a pinned assumption, none an engine bug** -- the second hardware round returned build green after two tiny fixes (upstreamed untouched at 99a: `cfg.clouds.cover` in the lookbook driver, `voxelcraft::lookbook` because `headless.rs` is a binary-crate module and the sandbox's own untested pre-submission hunch had moved the correct path to the wrong one), **21/21 control bit-exact for the third consecutive round**, naga clean at device creation, and the suite returning six reds, four of them new. The two pre-existing failures stand untouched (markdown-links; transitions asymmetry at 3.21x this round -- drifting with builds, watched per entry 96's note). **The four new failures, one by one, all in the tests' pinned assumptions:** *(i)* the A5 hole predicate named only `LEAVES` (7) as thinnable, but the stamp writes the biome leaf id (`b.leaf`) -- pine parcels open holes in `PINE_LEAVES` (16), the round's `16 -> 0` was a *legal* pine hole, previously masked by the meadow bug firing first; the predicate now enumerates both foliage species with the why-comment. *(ii)* The A5 white-noise y pin (`0.449` coin) measured **0.000** -- batch 94 pinned 0.6366 as a property of the *rank field* at step 4, and batch 98's sampling repair moved the read onto LOD-2 chunk pixels where `canopy = (trunk_h/s).max(1)` is one voxel tall, so vertical pairs among survivors are structurally absent. The pin now reads each thing where it lives: chunk pixels pin the shell's structural bound (`< 0.10`, loud if `coarse_trees` fattens the proxy), and a new field-lattice section gates `canopy_rank3` against `COARSE_CANOPY_KEEP` (both now `#[doc(hidden)] pub`) on a step-4 lattice, re-earning 94's three axes **with one honest correction** -- on the full 3D lattice z reads 0.391, x's league, because 94's 0.279 was a single canopy slice; simulated in Python before commit: x 0.391, y 0.635, z 0.391 vs coin 0.449. *(iii)* The sky-cool census counted the bare override token and a comment's mention made 6-of-5 -- comments are text too, the pane-census lesson in another register; the test now counts `, SPEC_SKY_COOL)` select-site shapes (four) and keeps the declaration check, and the WGSL comment stops naming the override and says why. *(iv)* The compose test read 204 (the label shade) at the first tile-pixel address; a faithful Python port of the in-tree `compose_group` reproduces both asserted values exactly, so the suspicion is a stale test binary under the handoff's own mtime trap -- the offsets now derive from the layout constants (no hardcoded 26/4), the assert messages name the bleeding culprit (`draw_text`'s vertical span), and a focused re-run is owed by the fork. **The arm verdicts, processed into the roadmap where each entry lives:** `--water-look` **ships** (MAE 4.69, max delta 156, reads *toward* the references; the default-on question stays parked for the user's eye); `--grade cine` **sweeps** (MAE 9.54, works at sunset, darkens an already-dark midday hillside -- a blit-grade strength knob is filed as G1's next small rung); `--sky-cool` **sweeps** (MAE 1.07, nearly invisible -- needs reach); `--foliage-rich` and `--canopy-relief` are **scrap-or-rebuild** (MAE 0.42/0.18, invisible -- the gap against the references is vegetation *density and shape* = a generator-side rebuild, and ranking one needs a waist-height-in-grass lookbook tile the current MIDDAY hillside view cannot supply); ALL-FIVE reads as cine + water-look (12.87), no interference finding. **A5 stands discharged as a G2 blocker**: the plan's demand that G2 first say what A5 got wrong is answered in code -- two test-geography assumptions (a species list that forgot the biome leaf id, a property pinned on the wrong object), no generator defect left standing. | **Hardware round is the evidence for the verdicts** (the fork's own gate, stated numbers); the repairs are sandbox class -- written against the tree, each fix simulated in Python before commit (the compose port reproduces the asserted pixels exactly; the rank lattice reproduces 94's x/y and corrects z), so the compile-and-run gate is owed by the fork's re-gate: expectation 323 pass / 2 pre-existing fail, control re-verify optional with the worldgen diff being comment-and-visibility only | [roadmap G1/G2/G3](roadmap.md), [worldgen](../src/worldgen.rs), [the A5 repairs](../tests/worldgen.rs), [census fix](../tests/batch97.rs), [compose hardening](../tests/batch98.rs) |
| 100 | **the second gate converges: the fork found one real defect the sandbox mis-diagnosed, and the rest of the repairs met independently** -- the fork re-ran the pre-99 tree, fixed the same five errors, and reported **323 passed / 2 failed**, the batch-99 expectation exactly (the two misses are pre-existing and non-code: markdown-links points at assets that only ever existed on the bench machine -- `screenshots/*.png` and `docs/research/` -- and transitions asymmetry read **3.23x**, the ladder's variance confirmed: 5.81 -> 2.94 -> 3.23). **Their find of the round, credited here and upstreamed at 100a**: `put_px` had no x-clip, so glyph ink past `sheet_w` *wrapped into the next row* through the linear index -- on the sheet's natural four-tile width the rightmost label strip's text begins at `x == w` and wrapped two rows down into tile 0's data, producing exactly the compose test's `[204,204,204]`. The sandbox's stale-binary suspicion (committed in 99d's comment, now corrected in-tree) was wrong one rung up: honest hardware, real defect, caught by the test it was authored to serve. **Convergence, not collision, for the other four**: their PINE_LEAVES discriminant repair matches 99b's (the biome leaf id, not a species list); their y-pin replacement states `both[1] == 0` as a stronger-and-exact structural fact (`r = 2/s == 0`, `canopy = 4/s == 1` -- a LOD-2 proxy is one cell, and 94's 0.6366 "cannot have come from a stride-4 chunk", matching this entry's own field-lattice re-pin which keeps measuring the property the fork only comments); their comment-stripped sky-cool census lands beside 99c's shaped-count (both cure comments-as-text); their `VIEW_CHANNELS` pub silenced a dead-code warning, and here the table became the one the cameras test actually reads instead of a second local list. **Bench for the record**: 1920x1080, 122 fps median, GPU 7.36 ms with `resolve` at 54% (down from batch-67's 64% share) -- no action, but the nine shots carry one *new look artifact*: the water-look ripple moires into concentric rings at aerial distances (visible in the medium/maxed sea and aerial frames) -- filed as a G3 note: ripple influence wants a distance/footprint fade before the arm's default-on question opens. | Fork's gate is the evidence (their build/test/bench/shots on hardware); the reconciliation edits are sandbox class -- upstreamed verbatim-by-construction, anchor-verified, owed one more compile gate on the fork with sync of the correction tree | [reconcile commit](../src/lookbook.rs), [roadmap G3](roadmap.md), [the A5 pins](../tests/worldgen.rs) |
| 101 | **closure: a real-revert control, the per-arm pixel gate, and the package is free** -- the fork ran the sweep it offered: a **real revert binary** at `4699890` (pre-97a) against the current build over all 21 vantages came back **0 pixels differing, identical file hashes** -- the strongest form the control has had since the SPEC-discipline started, proving all five SPEC bits stay out of the shipping build and the 99/100 reconciliation moved nothing (their nine flag-state shots are **byte-identical sha256** to the pre-reconciliation ones). **The per-arm pixel-delta gate** then read each arm against its own flag-off clone: sky-cool 19/21 vantages moving (920k px peak at `lattice`, exact zeros at night/deep-water by the day-half-only construction; `cave`/`lamps` move 1 delta through the swapped ambient -- documented, not a leak); cine 21/21 as a blit-stage grade must; water-look 15/21 with max delta **161** at `lod` and zeroes by mechanism where no water exists -- but **exactly 0 at `glass-sea` vs 408,940 at `coastline`**: sea through glass takes the one-lookup path and gets no ripple normals, batch-45's shape wearing a look arm's coat, and it is a named gate before default-on; foliage-rich/canopy-relief fire as measured (330k/79k peak px) and stay invisible in composition -- the sheet read (`look-gap-analysis.md`, batch-100 section) is the record. **All five arms together, paired and interleaved at coastline, six rounds: `resolve` +0.077 ms +/- 0.048 (t = +1.6), total +0.105 +/- 0.071 -- indistinguishable from zero: the look-first contract's bill for the entire package is, at this vantage, nothing.** Bench is heat, not code: 7.62 vs 7.36 ms with the tree bit-exact to pre-97. One tax the run exposed and fixed in-tree: my own prose broke `no_document_transcribes_the_test_count` twice -- a literal count in row 99 and the digit inside "A5" sitting before " tests"; both reworded, here credited as the fork's fix of its own tree's complaint. The sync-manifest gap stands owned by me: `src/bin/` (harness/probe/shaderstats entry points) lives only in the parent tree; the fork ported them unmodified against this tree's `src/harness/` -- **upload those files and they get committed into the manifest, because without them no downstream can run a control sweep at all**. **Batches 97-100 closed: the five arms are accepted work with named open gates, the control is as strong as this project can express it, and the sheet read sets batch 101's bill: the G2 vegetation rebuild plus a waist-height-in-grass lookbook view, then the three small dials (cine strength, water moire+glass, sky-cool re-read under the new framing).** | Transaction class: the fork's own hardware gate, every number in its report transcribed here after cross-checking against previous rounds' measurements; the matching reword fixes are in-tree | [sheet read](look-gap-analysis.md), [roadmap](roadmap.md), [G3 moire + glass gates](roadmap.md) |
| 101b-f | **batch 101 implemented in five pieces, construction-only -- the fork's compile/test rung stands owned for the whole series.** All matches sit dark behind existing off-by-default classes and the unarmed paths are bit-identical by the standing constructions (guarded branches, spec-folded arms, flag-gated worldgen writes), so the 21-vantage control claim survives by the same form of argument A5/97/98 made -- nothing compiled was executed here, per the agreed decoupling. *(101b)* MIDDAY-REEDS lookbook view: waist in the bank grass at `coastline`'s spot (yaw 143, height 2, noon) -- the tile FOLIAGE/CANOPY verdicts needed and the framing that rereads sky-cool. *(101c)* `--grade-strength F` (default 1.0, so the batch-97 frame is exact by the guarded-lerp construction, never `mix` at the control) with CINE-HALF as the matrix's 13th tile -- the cine sweep verdict gets its dial. *(101d)* the ripple footprint cure the fork's moiré finding demanded: one amplitude at every range could only be invisible or moiré, so the two ripple trains take **the wave field's own Nyquist class a level down** -- per-wavelength `smoothstep(2,4, λ/pix)` fades with `pix` grazing-stretched by the aniso branch's own factor, so rings/chevrons die by construction and the short train dies first; `WATER_LOOK_RIPPLE_AMP` untouched, batch-97's spelling pins repaired alongside the honest reason. *(101e)* **the G2 generator rebuild**: tussock stacks (`GRASS_TALL` id 24) as a 24% share of a carpet densified 2.5× (0.95 cap, the same clump field) at LOD 0; A5-lineage blue-noise tuft proxies on the coarse shell at stride 2 under `coarse_meadow`'s own gate discipline incl. batch-96's thinned-canopy rule (batch 14's "foliage is LOD 0 only" fence is now pinned with its one named exception); `REEDS` (id 25) on the tide band beside open water, into air only. Both ids ride the new single-site **`is_tuft`** contract in `common.wgsl` (fast-path exception, marcher skip-one, occluder exclusion -- the question is now asked once, pinned at exactly two `TALL_GRASS_ID` footnotes). Albedo rows derived by Python port of the probe's atlas-mean math before commit. **Not in the lookbook matrix**: `combos_only_move_look_fields` is the law the matrix keeps (one world), so G2 is judged through MIDDAY-REEDS on fork sheets. *(101f)* `--wind-sway`: the cross-quads *lean* -- a shear `(x - s·dy) - (z - q·dy) = c` keeps the diagonal plane family closed-form (one division still), two traveling sines (1.9/3.1, world x/z phase, the clouds' own heading) at 0.09 blk/blk, **per-cell anchor** because a global one displaces the spine by lean×height and blinks blades out of the voxel clip (caught on the page, pre-commit); the accepted price -- an `AMP`-sized bend at stacked seams -- is the bend real grass has. SPEC_WIND_SWAY on hi bit 32768, else-branch keeps legacy arithmetic byte-for-byte. | Evidence class: construction and on-page math alone (derivations double-checked by re-reading; the sway anchor failure and one malformed authoring pass were both caught pre-commit this way). The fork gate is owed the whole series at user's call: compile, suite, 21-vantage control, and the look reads at MIDDAY-REEDS with `--grass-dense`/armed tiles | [tests](../tests/batch101.rs), [lookbook](../src/lookbook.rs), [is_tuft](../src/render/shaders/common.wgsl), [worldgen](../src/worldgen.rs) |
| 101g-h | **the sandbox QC rung meets the tree, and the fork's machine now holds the gates the sandbox cannot close.** After batch 101's five pieces, this QC was the first compile of the series anywhere: `cargo check --all-targets` green (one real bug found and fixed -- batch101's test buffer typed u8 against an u16 BlockId); clippy taken to three pre-existing deliberate shapes (a 12-arg constructor, `9 % 16` and `PREFIX_MASK` documentation pins) by seven zero-semantics repairs at the sites the lint named, two of them in the 101 series' own new text. Focused suites after repair: batch101 12/12 (the arm behavioral test actually executes now), batch97 9/9, batch98 8/8, probe 25/25 (both 101e atlas-mean albedo rows pass at <1e-3), textures 11/11, worldgen 10/10. **Two consequential catches beyond type-and-pin size**: the textures time-census caught `--wind-sway` reading `frame.time` raw -- batch 23's anim-time law is that a headless capture has exactly one animation clock; the sway now multiplies by `frame.cloud_speed`, the weather it is explicitly the field of. And tests/harness' lattice key now wraps deliberately (`wrapping_mul/Wrapping_add`) because debug panics where release silently wraps -- the two profiles must build the *same* lattice or a pass here proves nothing there; this was the fork-passing-test panic that turned out to be a profile difference the code had never said aloud. Full CPU suite: **all targets green except**: `docs` markdown-links (9 links to `screenshots/` + `docs/research/` -- the known bench-machine-assets failure, unchanged), and **lod_stream (6) + shadow_grid (4): every consumer of the seed-1337 `settle()` driver hangs with `world never settled`, reproduced in release too** -- provenance not closed in the sandbox (the worktree repro at batch-101's base was abandoned to the machine's limits, ~2 GB RAM): judged environment-candidate, not code-candidate (batch-100's fork gate had these same targets green; nothing in 101 touches `stream.rs`), **bench verdict is the required resolution**. batch92's appended-list pin repaired to name REEDS as the tail, law semantics preserved. Cleansing: toolchain + caches live under `/home/user/.cache` (snapshot-excluded; keep), release/worktree targets and the crop/upload image payload deleted per the disk-pressure report. | Evidence class: the sandbox's own `cargo check/clippy/test` runs, transcribed after each run with the failure list; the hang's "not code" claim is by *what the diff touches* plus the fork's observation that the same targets passed at batch-100 -- the bench re-run is owed either way | [QC commit](./../tests/batch101.rs), [harness wrap](./../tests/harness.rs), [the clock repair](./../src/render/shaders/common.wgsl) |
| 101-hw | **batch-101 hardware round: everything shipped works; the report's own two finds are now the next batch's fixtures.** Fork's machine (RTX 3050 laptop): sync-gate honored (86 files touched after Jan-1980 mtimes), release build green in 1m49s, **all six armed pipelines compile and render with zero naga errors** -- the three-rounds-running WGSL type-error class did not repeat. Suite `cargo test --release --no-fail-fast`: 2 red targets, both pre-existing and named (`docs` markdown-links at 9 bench-asset links; `lod_stream` transitions asymmetry **3.65x** -- and the sandbox's "world never settled" hang is hereby closed as a **sandbox artifact**: the settle driver completes in ~14 s on hardware, and the asymmetry underneath predates batch 101: 3.67x at pre-101). `probe` green including both 101e albedo rows; `batch101` 12/12; `shadow_grid` green. **Control 21/21 at zero** against a real pre-101 build (`c23268b`), re-verified after their fix below. **THE find of the round, credited to their bench**: `--wind-sway` was *inert* -- `FLAG_HI_WIND_SWAY` rode config and `make_spec` but not `SPEC_HI_MASK`, so `ensure_spec` keyed it away and the override stayed false; batch-94's identical-pixels rule caught it on the first armed diff. Fixed on their `c692ad7` and convergently upstreamed here at **101i** (mask row, const-assert, ALL_HI row, the batch101 pin re-ordered to lead with mask membership). After fix: wind armed moves 108,651 px at `default`, 176,751 over G2, gusts *travel* (t0/t1: 210,166). **101d is the round's clear win: the moire is gone, not dimmer** -- arm reach at `shore` retains 87%, `terraces` 100%, mid/aerial `lattice`/`coastline`/`open-sea` land at 18-24%, exactly the Nyquist shape authored. **G2: works at eye level** (`default`: 433,502 px, MAE 4.88, "a grass mat") **but is invisible at aerial** (`lod`: 7,712 px / 0.0817, near-corner only -- below the 0.372 by-eye rung) -- stride-2-only ceiling measured; the far-band experiment rides at 101j. **MIDDAY-REEDS mis-frames G2** (20,007 px; frame ~97% water -- the tribunal camera is now MIDDAY-MEADOW, 101j). **Cine midday re-judged**: full 8.03/half 3.86 at the water camera is a broad cast, not a crush -- crush claim to be re-taken at `default --time 0.50`. **Cost: `--grass-dense` +0.355 ms total (resolve +0.240, march +0.125), ~4.6% of a 7.8 ms frame -- ship; wind-sway free within error.** Pre-existing note filed: dark triangular spikes in the shallows exist in BASE (not 101); own a backlog row. **Open decision settled by them: `src/bin/` never existed anywhere** -- the fixture driver (harness/probe/shaderstats entry points) is to be *written*, not uploaded -- filed as batch 102 (own planning row: `validate`, `bitexact`, `bench`, `implies`, `reference` stop being per-round improvisations). | Fork's hardware gate is the evidence for every number above (their machine, their log lines, transcribed); 101i/101j are sandbox class (their content for 101i already hardware-covered on their master; 101j owes the usual compile + spec_hi + lookbook targets and a visual verify at `lod`/aerial) | [101i-j commit](./../src/render/mod.rs), [far-band experiment](./../src/worldgen.rs), [MIDDAY-MEADOW](./../src/lookbook.rs) |
| 102 (planned) | **the fixture driver, written at last** -- `src/bin/harness.rs` (vantage sweep + implies + bitexact + DIFF scoring through `harness::metric`), `src/bin/probe.rs`, `src/bin/shaderstats.rs`, closing the "every round re-improvises the sweep" gap the 101 hardware row cross-references; plus the visual verifications 101j asked for and the cine midday re-take. | planned | |
| 102a | **`--soft-shadows`: the sun-disc penumbra, budgeted by geometry instead of by framebuffer.** What the references have and every lookbook tree shadow lacked: `shadow_ray` answers lit-or-not and nothing between, so each edge is a razor. The design inverts the PCF bill -- no screen-space kernel (this renderer has no buffer for one), instead three cone taps as *rays through the same grid*, whose march budgets are anchored by the centre tap's blocker distance: deep umbra early-outs on all four rays, contact pays a tight cone, and the fringe is the only real payer. The miss case has no anchor, so it pays at most `SOFT_MISS_DIST = 64.0` -- **the number the cost story rests on**; beyond it, occluders are far-field (envelope's job) or their fringe is sub-texel. Cone aperture is the sun's own disc by construction (`SUN_ANGLE * 6.0`), so the fringe widens with occluder distance and contact hardens for free. Dither from the *surface*, half-block quantised on `hash_alpha`'s primes: sticks to the ground, asks nothing of TAA. `shadow_ray` left verbatim to the character; the distance-returning walk is a sibling transliteration, because the control's bit-exactness is a construction property. Kill ladder, all one-constant: `SOFT_TAPS` 0 (fold returns the centre bit with the arm still armed), `SOFT_PENUMBRA` 3/6/10, `SOFT_MISS_DIST` 220.0 (full march, must show pixels). Sheet grows to 4x14; `sig` grew the new field. **Acceptance for the bench: within +0.35 ms at `--grass-dense` 1920x1080 (G2's own bar, passed on its round); a miss at that bar drops `SOFT_TAPS` to 1 and re-measures before judging look.** | sandbox class: authored blind with the toolchain wiped, every pinned string grep-verified in its final place (mask row, const-assert, ALL_HI row, parse/default/help, `spec_hi_from`, override, both bodies, call site, control-verbatim, matrix 4x14); fork owes compile, `spec_hi`/`batch102`/`batch98`/`docs`, the 21-vantage control, the re-shot sheet and the cost pass | [flag](./../src/render/mod.rs), [the penumbra](./../src/render/shaders/common.wgsl), [call site](./../src/render/shaders/resolve.wgsl), [pins](./../tests/batch102.rs), [matrix](./../src/lookbook.rs) |
| 102 (planned, restated) | the fixture driver after 102a's hardware verdict -- `src/bin/` remains unwritten; the 101j far-band verify (`lod`), the MIDDAY-MEADOW G2 tribunal and the cine re-take at `default --time 0.50` ride the same pass as 102a's sheet. | planned | |
| 102-driver | **the fixture driver, landed** -- three rounds overdue and no longer re-improvised per round. `src/bin/harness.rs` is the nine subcommands `docs/harness.md` has documented since batch 22 wrote the doc ahead of the code: `list`, `capture`, `validate`, `implies`, `bitexact`, `bench`, `march`, `reference`, `compare`, flags exactly as the doc's block quotes them (`--exe-b`, `--extra-b`, `--vantage`, `--only`, `--samples`, `--mip0`, `--rect`, `--crop`, `--tiles`, `--profile`). **It is wiring with the anti-drift law as a compile property**: zero metrics are implemented in the file -- every reading goes through `harness::render`, `metric::*` and `bench::paired`, so the driver cannot drift from the definitions it drives. **Its own parser is strict**, closing the wart the doc recorded in its defect section (the paragraph is amended in place): an unknown `--flag` exits 2 naming the vocabulary, the outer-call twin of `UNKNOWN_ARG_MARK`'s inner guard. **`validate` is where reading shape meets expectation shape**: the twelve `CHECKS` rows classify as *caused* (`mae`/`max`/`pixels` measured between the two renders, `DeltaAtLeast`) or *levels* (`speckle`/`rg_std`/`rg_speckle`/`coherence.peak` read on each render, ratios) -- and both mismatched combinations are loud table defects, an unknown metric name fails its row rather than passing silently, and `DeltaAtLeast`-on-levels is **signed**, per `RatioAtLeast`'s own doc that an absolute difference passes a metric whose sense inverted. **`bench` is paired, alternated and standard-error'd per the lib's own law, prints all eight `PASSES` cells including total, and runs null-paired (A against itself) with no side B** -- the honest first run of any session, with the pairing state printed in the header. **`reference` is not a screenshot with many samples**: it relays the engine's whole account and *fails* when no `noise floor` line came back; **`march` relays the engine's `march-stats:` lines and ranks in integers**. `src/bin/probe.rs` prints the adapter identity first (G1's law), the full `{:#?}` feature and limits dumps, and the four named rows the ledger quotes (`EXPERIMENTAL_RAY_QUERY`, `RAY_HIT_VERTEX_RETURN`, `RAY_TRACING_PIPELINES`, `COOPERATIVE_MATRIX`) **string-scanned from the dump rather than from wgpu constants -- a future wgpu rename reads `false`, not "does not compile"**. `src/bin/shaderstats.rs` finally spends Cargo.toml's batch-13 comment: naga parses and validates `render::shader_source()` -- **the game's own concatenation, the one rule `docs/gpu.md` set for this tool --** then writes SPIR-V per `render::ENTRY_POINTS` with word/byte counts **labeled as overrides-at-defaults** (a specialized size quoted as a shipping size is a wrong number with good provenance), and then attempts the driver half: reflected-descriptor compute pipelines + `VK_KHR_pipeline_executable_properties`, **degrading loudly to the offline table with exit 3 rather than ever guessing a register count**. `tests/bins.rs` pins the vocabulary class: the nine subcommands, the flag set, the two relay contracts (`march-stats:`, `noise floor`), `render::ENTRY_POINTS`, the named feature rows. | sandbox class: authored with the toolchain absent, so **uncompiled** -- but every library surface the driver calls was grep-verified against the lib at write time (`paired`'s callback shape, `PASSES` at 8 with total appended, `MetricCheck`'s `both` field, `Response`'s three variants, `Img`/`Rect` public fields, the engine's `--march-stats`/`--reference`/`--mip-bias` flags and stdout line spellings); blind-port notes sit inline at the two genuinely unverifiable points (naga 30's `write_vec` by-reference shape, wgpu's `enumerate_adapters` return shape -- both written to fail as obvious one-line fixes, not as behaviour). Fork owes: `cargo check`/`clippy`, the `bins` target, then the first session run -- `bitexact` against a real revert is the run that proves it | [the driver](./../src/bin/harness.rs), [probe](./../src/bin/probe.rs), [shaderstats](./../src/bin/shaderstats.rs), [pins](./../tests/bins.rs) |
| 102-hw | **the bench refused 102a, and the round's mechanism numbers are worth more than the arm was.** Third hardware round (`415670a` + the fork's own `2bc0dba`): build green 24.6 s, naga validates every pipeline including the new override (fourth clean round for the WGSL-error class), control 21/21 exact against a real pre-102 binary, the 101i mirror rule held -- and `batch98`'s *other* mirror missed the new field, caught and repaired fork-side, its text carried here with the G2 widening the same commit owed. **Cost: resolve 4.265 -> 8.815 (+4.550 +/- 0.011), 128 -> 81 fps, 13x the +0.35 ms bar; `SOFT_TAPS = 1` is still 5.4x over -- the fallback does not rescue it.** Effect: under the 0.372 invisible rung at every vantage whole-frame (tribunal 0.1802; aperture ladder 6/10/24/48 -> 0.180/0.250/0.398/0.546 with max delta pinned at 26 -- **do-not-tune, written at the constant**), yet **2.6480 against the 2.87 rung at the strongest near-field crop** -- fix, not revert. **The deciding fact is the sign split: 86% of moved pixels lighten** -- a shadow-lifter, not a penumbra; sign-split is now fixture-law for redistributing effects, per the round's own §5.2. Mechanism, measured to ground and filed in lessons: scaffolding 0.073 ms (not registers, not occupancy), first tap +1.824, taps two/three +1.33 each, an 8x march shortening worth 7% -- **a ray's bill is being fired; the G5 gate is arithmetic reading `shadow_ray_t`'s free distance, never a ray.** §0 sweep done at every site named in the handoff: `G2_COARSE_KEEP_FAR`'s comment rewritten to what it measured (41/36 px; NOT built by batch-94's rule -- a stride cap buys one LOD level, the aerial frame lives at 8/16/32); the three WGSL cost constants carry their ladders/verdicts; the flag doc carries the round; CLAUDE.md's cost column gained the one refused row; **`G2-PAIR` re-pointed to G2's real pair (`grass_dense`+`wind_sway`) with `sig`+`sanitized` widened in the same commit and the look-gap mention amended in place**; cine closed at `--grade-strength 0.5` (9.1930 crush vs 4.3052, land-filling frame); roadmap gained **G5** per batch-93's designation rule. | hardware-measured (the fork's file), my edits authored-uncompiled per the standing evidence class; fork owes the suite re-run (batch98 mirrors, batch102 5/5, the docs link audit re-counted) and no bench of this arm until G5's second attempt lands | [the refused arm's cost row](./../CLAUDE.md), [the finding](./../docs/lessons.md), [the second attempt](./../docs/roadmap.md), [the repair](./../src/lookbook.rs) |
| 102-driver-gate | **the driver's owed list is discharged: check, clippy, `bins`, and the proving run are all green.** Seven compile errors, every one a one-line API-shape fix at the noted points, and the two inline blind-port notes split clean: naga's `write_vec` by-reference was right; the `enumerate_adapters` shape-hedge was wrong (it is a *future* in wgpu 30, so enumeration blocks the same way `request_adapter` does). The five the notes hadn't anticipated, now recorded as settled facts for this pinned set -- wgpu 30: `InstanceDescriptor` has no `Default` (the game's own `new_without_display_handle()` constructor, minus its VULKAN\|DX12 narrowing, is the reporter's unfiltered one), and `Features::bits()` is a `FeatureBits([u64; N])` array wrapper rather than one word; naga 30: `spv::Options` `Default`s its other ten fields, with the five opinionated ones still named; ash 0.38: the executable-properties methods drop the `_khr` suffix (the module is already the KHR-namespaced one), **properties takes `PipelineInfoKHR` while statistics takes `PipelineExecutableInfoKHR` -- the split is the API's**, and an `unsafe`-block comparison in a closure block needed its `==` inside the braces. Clippy: two of the fork's own fixed (`manual_inspect`, `print_literal`); **three remain, all in tracked unmodified files -- `too_many_arguments` in the lib, `assertions_on_constants` in `tests/emitter_gate.rs`, `identity_op` in `tests/tree.rs` -- left deliberately: drift from a newer clippy, a separate call, not this batch's scope.** `cargo test --test bins`: 4/4. **The proving run: revert binary built from `HEAD` (d461b95) in a worktree -- HEAD carries no `src/bin` at all, so it is a real revert and not a flag -- and `bitexact --exe-b ./voxelcraft-pre103.exe` reports 21 of 21 vantages, 0 of 921600 differing, MAE 0.0000, max 0.** Two findings filed as data: `Command::new` does not search the cwd, so the bare-name recipe in `docs/harness.md` is amended to `./voxelcraft-preNN.exe` at every site with the two-sentence why; and `git log --all -- src/bin` confirms `src/bin/` has never been tracked on any ref of this clone, so batch 13's original shaderstats is not recoverable as a cross-check -- the executable-properties/statistics struct split is the one shape an original would still settle. | fork-machine compile and runs, transcribed in their round file; the workspace tree updated to the same seven fixes so the checkouts cannot diverge on them | [the seven fixes](./../src/bin/shaderstats.rs), [bins green](./../tests/bins.rs), [the recipe fix](./../docs/harness.md) |

